<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>九又四分之三字节</title>
  
  <subtitle>是谁来自 Python Java, 却囿于 PHP, Go 与 Lua. (句中提及语言,博主一概不会.单押x3)</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://git.102no.com/"/>
  <updated>2019-07-23T15:47:11.930Z</updated>
  <id>http://git.102no.com/</id>
  
  <author>
    <name>Qianyugang</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>解决在debian 9 中安装 PostgreSQL 9.6 中No such file or directory问题</title>
    <link href="http://git.102no.com/2019/07/23/debian-postgresql/"/>
    <id>http://git.102no.com/2019/07/23/debian-postgresql/</id>
    <published>2019-07-22T16:00:00.000Z</published>
    <updated>2019-07-23T15:47:11.930Z</updated>
    
    <content type="html"><![CDATA[<p>最近需要在debian 9 中安装 PostgreSQL 9.6 数据库，按照常规步骤：<br>先更新源<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">sudo apt-get update</div><div class="line">sudo apt upgrade</div></pre></td></tr></table></figure></p><p>然后安装PostgreSQL<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">sudo apt-get install postgresql-9.6</div></pre></td></tr></table></figure></p><p>就在安装的时候突然报如下错误并中断：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">Setting up postgresql-client-9.6 (9.6.13-0+deb9u1) ...</div><div class="line">update-alternatives: using /usr/share/postgresql/9.6/man/man1/psql.1.gz to provide /usr/share/man/man1/psql.1.gz (psql.1.gz) in auto mode</div><div class="line">update-alternatives: error: error creating symbolic link &apos;/usr/share/man/man1/psql.1.gz.dpkg-tmp&apos;: No such file or directory</div></pre></td></tr></table></figure></p><p>安装失败，然后发现是一个路径找不到，需要手动创建一下，然后google之后找到了相同问题：<a href="https://github.com/debuerreotype/debuerreotype/issues/10" target="_blank" rel="external">https://github.com/debuerreotype/debuerreotype/issues/10</a></p><p>于是乎采纳一下解决方案，执行如下代码新建一下相关文件夹：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">for i in $(seq 1 8); do mkdir -p &quot;/usr/share/man/man$&#123;i&#125;&quot;; done</div></pre></td></tr></table></figure></p><p>执行完之后可以去<code>/usr/share/man/</code>目录下看一下是不是有8个man文件，执行成功就可以继续安装即可。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;最近需要在debian 9 中安装 PostgreSQL 9.6 数据库，按照常规步骤：&lt;br&gt;先更新源&lt;br&gt;&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;div class=&quot;li
      
    
    </summary>
    
      <category term="与代码" scheme="http://git.102no.com/categories/%E4%B8%8E%E4%BB%A3%E7%A0%81/"/>
    
    
      <category term="debian" scheme="http://git.102no.com/tags/debian/"/>
    
      <category term="PostgreSQL" scheme="http://git.102no.com/tags/PostgreSQL/"/>
    
  </entry>
  
  <entry>
    <title>微服务 API 网关 Kong Kubernetes Sidecar 注入插件中文文档</title>
    <link href="http://git.102no.com/2019/07/22/kong-plugin-kubernetes-sidecar-injector/"/>
    <id>http://git.102no.com/2019/07/22/kong-plugin-kubernetes-sidecar-injector/</id>
    <published>2019-07-21T16:00:00.000Z</published>
    <updated>2019-07-24T15:53:15.325Z</updated>
    
    <content type="html"><![CDATA[<p>该插件将注入Kong数据平面节点并在Kubernetes之上形成服务网格</p><h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><p>Kong <code>0.15.0</code> / <code>1.0.0</code>增加了代理和路由原始<code>tcp</code>和<code>tls</code>流的能力，并使用服务网格Sidecar模式和Kong节点之间的相互<code>tls</code>来部署Kong。本教程将引导您使用我们的 <a href="https://github.com/Kong/kubernetes-sidecar-injector" target="_blank" rel="external">Kubernetes Sidecar 注入插件</a>在Kubernetes上设置Kong服务网格。</p><h2 id="准备条件"><a href="#准备条件" class="headerlink" title="准备条件"></a>准备条件</h2><p>您需要在Kubernetes上运行Kong 1.0.0或更高版本，包括存储为可用于Kong控制平面的机密的SSL证书。来自<a href="https://github.com/Kong/kong-dist-kubernetes" target="_blank" rel="external">Kong Kubernetes Repository</a>的Make任务<code>run_cassandra</code>和<code>run_postgres</code>将完全配置必备数据存储，Kong控制平面，Kong数据平面和SSL机密。</p><p>或者，按照<a href="https://docs.konghq.com/install/kubernetes/" target="_blank" rel="external">Kong Kubernetes Install Instructions</a>页面中的任何设置说明进行操作，然后设置SSL证书/密码：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div></pre></td><td class="code"><pre><div class="line">cd $(mktemp -d)</div><div class="line"></div><div class="line">### Create a key+certificate for the control plane</div><div class="line">cat &lt;&lt;EOF | kubectl create -f -</div><div class="line">apiVersion: certificates.k8s.io/v1beta1</div><div class="line">kind: CertificateSigningRequest</div><div class="line">metadata:</div><div class="line">  name: kong-control-plane.kong.svc</div><div class="line">spec:</div><div class="line">  request: $(openssl req -new -nodes -batch -keyout privkey.pem -subj /CN=kong-control-plane.kong.svc | base64 | tr -d &apos;\n&apos;)</div><div class="line">  usages:</div><div class="line">  - digital signature</div><div class="line">  - key encipherment</div><div class="line">  - server auth</div><div class="line">EOF</div><div class="line">kubectl certificate approve kong-control-plane.kong.svc</div><div class="line">kubectl -n kong create secret tls kong-control-plane.kong.svc --key=privkey.pem --cert=&lt;(kubectl get csr kong-control-plane.kong.svc -o jsonpath=&apos;&#123;.status.certificate&#125;&apos; | base64 --decode)</div><div class="line">kubectl delete csr kong-control-plane.kong.svc</div><div class="line">rm privkey.pem</div></pre></td></tr></table></figure></p><p>或使用以下简便脚本：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">curl -fsSL https://raw.githubusercontent.com/Kong/kong-dist-kubernetes/master/setup_certificate.sh | bash</div></pre></td></tr></table></figure></p><h2 id="安装步骤"><a href="#安装步骤" class="headerlink" title="安装步骤"></a>安装步骤</h2><p>导出一些变量以访问Kong Admin API和Proxy：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">$ export HOST=$(kubectl get nodes --namespace default -o jsonpath=&apos;&#123;.items[0].status.addresses[0].address&#125;&apos;)</div><div class="line">$ export ADMIN_PORT=$(kubectl get svc --namespace kong kong-control-plane  -o jsonpath=&apos;&#123;.spec.ports[0].nodePort&#125;&apos;)</div></pre></td></tr></table></figure></p><p>通过Kong Admin API启用Sidecar Injector插件：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">curl $HOST:$ADMIN_PORT/plugins -d name=kubernetes-sidecar-injector -d config.image=kong</div></pre></td></tr></table></figure></p><p>打开Kubernets Sidecar Injection：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div></pre></td><td class="code"><pre><div class="line">cat &lt;&lt;EOF | kubectl create -f -</div><div class="line">apiVersion: admissionregistration.k8s.io/v1beta1</div><div class="line">kind: MutatingWebhookConfiguration</div><div class="line">metadata:</div><div class="line">  name: kong-sidecar-injector</div><div class="line">webhooks:</div><div class="line">- name: kong.sidecar.injector</div><div class="line">  rules:</div><div class="line">  - apiGroups: [&quot;&quot;]</div><div class="line">    apiVersions: [&quot;v1&quot;]</div><div class="line">    resources: [&quot;pods&quot;]</div><div class="line">    operations: [ &quot;CREATE&quot; ]</div><div class="line">  failurePolicy: Fail</div><div class="line">  namespaceSelector:</div><div class="line">    matchExpressions:</div><div class="line">    - key: kong-sidecar-injection</div><div class="line">      operator: NotIn</div><div class="line">      values:</div><div class="line">      - disabled</div><div class="line">  clientConfig:</div><div class="line">    service:</div><div class="line">      namespace: kong</div><div class="line">      name: kong-control-plane</div><div class="line">      path: /kubernetes-sidecar-injector</div><div class="line">    caBundle: $(kubectl config view --raw --minify --flatten -o jsonpath=&apos;&#123;.clusters[].cluster.certificate-authority-data&#125;&apos;)</div><div class="line">EOF</div></pre></td></tr></table></figure></p><p>或使用以下简便脚本：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">curl -fsSL https://raw.githubusercontent.com/Kong/kong-dist-kubernetes/master/setup_sidecar_injector.sh | bash</div></pre></td></tr></table></figure></p><h2 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h2><p>接下来，任何开始使用Kong Sidecar的pods都会自动注入，而来自该pods容器的所有数据都将通过Kong Sidecar。<br>例如，如果我们使用Istio中的bookinfo.yaml示例：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">kubectl apply -f https://raw.githubusercontent.com/istio/istio/release-1.1/samples/bookinfo/platform/kube/bookinfo.yaml</div></pre></td></tr></table></figure></p><p>我们看到所有pods都收到了Kong Sidecar：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">kubectl get all</div><div class="line">NAME                 READY   STATUS</div><div class="line">pod/details-v1       2/2     Running</div><div class="line">pod/productpage      2/2     Running</div><div class="line">pod/ratings-v1       2/2     Running</div><div class="line">pod/reviews-v1       2/2     Running</div><div class="line">pod/reviews-v2       2/2     Running</div><div class="line">pod/reviews-v3       2/2     Running</div></pre></td></tr></table></figure></p><p>继续配置服务。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;该插件将注入Kong数据平面节点并在Kubernetes之上形成服务网格&lt;/p&gt;
&lt;h2 id=&quot;介绍&quot;&gt;&lt;a href=&quot;#介绍&quot; class=&quot;headerlink&quot; title=&quot;介绍&quot;&gt;&lt;/a&gt;介绍&lt;/h2&gt;&lt;p&gt;Kong &lt;code&gt;0.15.0&lt;/code&gt; /
      
    
    </summary>
    
      <category term="与代码" scheme="http://git.102no.com/categories/%E4%B8%8E%E4%BB%A3%E7%A0%81/"/>
    
    
      <category term="Kong教程" scheme="http://git.102no.com/tags/Kong%E6%95%99%E7%A8%8B/"/>
    
      <category term="Kong中文文档" scheme="http://git.102no.com/tags/Kong%E4%B8%AD%E6%96%87%E6%96%87%E6%A1%A3/"/>
    
      <category term="Kong插件" scheme="http://git.102no.com/tags/Kong%E6%8F%92%E4%BB%B6/"/>
    
  </entry>
  
  <entry>
    <title>基于OpenResty 的 WEB 框架 Lor 安装初探</title>
    <link href="http://git.102no.com/2019/07/16/openresty-lor/"/>
    <id>http://git.102no.com/2019/07/16/openresty-lor/</id>
    <published>2019-07-15T16:00:00.000Z</published>
    <updated>2019-07-16T14:19:22.389Z</updated>
    
    <content type="html"><![CDATA[<ul><li><p><strong>项目介绍：</strong></p><ul><li>Lor是一个运行在OpenResty上的基于Lua编写的Web框架.</li><li>路由采用<a href="http://www.sinatrarb.com/" target="_blank" rel="external">Sinatra</a>风格，Sinatra是Ruby小而精的web框架.</li><li>API基本采用了<a href="http://expressjs.com/" target="_blank" rel="external">Express</a>的思路和设计，Node.js跨界开发者可以很快上手.</li><li>支持插件(middleware)，路由可分组，路由匹配支持string/正则模式.</li><li>lor以后会保持核心足够精简，扩展功能依赖<code>middleware</code>来实现. lor本身也是基于<code>middleware</code>构建的.</li><li>推荐使用lor作为HTTP API Server，lor也已支持session/cookie/html template等功能.</li><li>框架简单示例项目<a href="https://github.com/lorlabs/lor-example" target="_blank" rel="external">lor-example</a></li><li>框架全站示例项目<a href="https://github.com/sumory/openresty-china" target="_blank" rel="external">openresty-china</a></li></ul></li><li><p><strong>项目地址：</strong> <a href="https://github.com/sumory/lor" target="_blank" rel="external">https://github.com/sumory/lor</a></p></li><li><strong>文档地址：</strong> <a href="http://lor.sumory.com" target="_blank" rel="external">http://lor.sumory.com</a></li></ul><h2 id="安装lor框架"><a href="#安装lor框架" class="headerlink" title="安装lor框架"></a>安装lor框架</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">git clone https://github.com/sumory/lor</div><div class="line">cd lor </div><div class="line">make install</div></pre></td></tr></table></figure><p>此时可能会有报错如下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">/usr/bin/env: &quot;resty&quot;: 没有那个文件或目录</div></pre></td></tr></table></figure></p><p>原因为lor没有找到resty的执行目录，这个时候只需要找到resty的执行目录并软链过去即可：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">sudo ln -s /usr/local/openresty/bin/resty /usr/bin/resty</div></pre></td></tr></table></figure></p><p>框架自带一个示例项目，运行一下代码<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">lord new lor_demo</div></pre></td></tr></table></figure></p><h2 id="启动"><a href="#启动" class="headerlink" title="启动"></a>启动</h2><p>然后就能在框架中看到lor_demo文件夹，执行如下命令<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">cd lor_demo</div><div class="line">lord start</div></pre></td></tr></table></figure></p><p>打开浏览器，输入<code>http://localhost:8888/</code>正常访问，即正常安装。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;ul&gt;
&lt;li&gt;&lt;p&gt;&lt;strong&gt;项目介绍：&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Lor是一个运行在OpenResty上的基于Lua编写的Web框架.&lt;/li&gt;
&lt;li&gt;路由采用&lt;a href=&quot;http://www.sinatrarb.com/&quot; target=&quot;
      
    
    </summary>
    
      <category term="与代码" scheme="http://git.102no.com/categories/%E4%B8%8E%E4%BB%A3%E7%A0%81/"/>
    
    
      <category term="OpenResty" scheme="http://git.102no.com/tags/OpenResty/"/>
    
      <category term="Lua" scheme="http://git.102no.com/tags/Lua/"/>
    
      <category term="Lor" scheme="http://git.102no.com/tags/Lor/"/>
    
  </entry>
  
  <entry>
    <title>使用 konga 来管理微服务 API 网关 kong</title>
    <link href="http://git.102no.com/2019/07/04/konga/"/>
    <id>http://git.102no.com/2019/07/04/konga/</id>
    <published>2019-07-03T16:00:00.000Z</published>
    <updated>2019-07-07T15:25:11.186Z</updated>
    
    <content type="html"><![CDATA[<p>微服务网关kong有比较多个后台管理面板，比如比较简单的kong-dashboard，还有konga，之前在初探kong的时候，使用的就是比较简单的kong-dashboard，很多功能都没有，而且最近由于kong官方更新比较频繁，1.0之后的kong-dashboard就已经不兼容了，频繁报错，所有今天我就来使用一下另一款kong的后台管理面板：konga</p><ul><li>官方网址：<a href="https://pantsel.github.io/konga/" target="_blank" rel="external">https://pantsel.github.io/konga/</a></li><li>项目地址：<a href="https://github.com/pantsel/konga" target="_blank" rel="external">https://github.com/pantsel/konga</a></li><li>项目demo：<a href="http://139.59.145.231:1337/" target="_blank" rel="external">http://139.59.145.231:1337/</a></li></ul><h2 id="安装konga"><a href="#安装konga" class="headerlink" title="安装konga"></a>安装konga</h2><p>在开始安装之前，需要准备的有：</p><ul><li>一个已经安装好的kong环境</li><li>Nodejs &gt;= 8 (推荐8.11.3 LTS)</li><li>npm</li></ul><p>关于这三者的安装，我这里就不赘述了，kong的安装可以在博客的<a href="http://www.102no.com/archives/tag/kong" target="_blank" rel="external">相关文章</a>查看。在做好准备工作之后，就开始安装：</p><h3 id="安装npm依赖"><a href="#安装npm依赖" class="headerlink" title="安装npm依赖"></a>安装npm依赖</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">$ git clone https://github.com/pantsel/konga.git</div><div class="line">$ cd konga</div><div class="line">$ npm i</div></pre></td></tr></table></figure><p>在执行<code>npm i</code>之后，由于会引入很多的npm包，所以可能会有报错，比如我这里就遇到了一些权限问题，比如：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">Unable to save binary /home/qianyugang/soft/konga/node_modules/node-sass/vendor/linux-x64-64 : &#123; Error: EACCES: permission denied, mkdir &apos;/home/thinkpad/soft/konga/node_modules/node-sass/vendor&apos;</div></pre></td></tr></table></figure></p><p>如果出现了如上错误，可以把最后一个命令修改为<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">sudo npm i --unsafe-perm=true --allow-root</div></pre></td></tr></table></figure></p><p>执行之后，可能会提示：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">bower bootstrap-switch extra-resolution Unnecessary resolution: bootstrap-switch#~3.3.4</div><div class="line">added 69 packages from 77 contributors and audited 3120 packages in 4.654s</div><div class="line">found 275 vulnerabilities (125 low, 24 moderate, 126 high)</div><div class="line">run `npm audit fix` to fix them, or `npm audit` for details</div></pre></td></tr></table></figure></p><p>意思是有一些包没有执行好，那么我们就按照它的提示执行一下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">sudo npm audit fix --unsafe-perm=true --allow-root</div></pre></td></tr></table></figure></p><p>基本就可以解决npm依赖包的问题了。总之这里是有可能出现各种各样的npm问题，依次解决即可。</p><h3 id="初始化数据库"><a href="#初始化数据库" class="headerlink" title="初始化数据库"></a>初始化数据库</h3><p>npm的依赖都安装完成之后，就需要复制一下konga目录下的<code>.env_example</code>文件<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">cp .env_example env</div></pre></td></tr></table></figure></p><p>然后把其中的一些配置项目都填写上去。具体的配置项可以查看 <a href="https://github.com/pantsel/konga#environment-variables" target="_blank" rel="external">https://github.com/pantsel/konga#environment-variables</a> 。这里就主要把以下三项配置好：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">DB_ADAPTER=postgres</div><div class="line">PORT=1337</div><div class="line">DB_URI=postgresql://kong_user:kong_pass@localhost:5432/konga</div></pre></td></tr></table></figure></p><p>注意这个DB_URI一定要填写完整，这里的数据库我用的是postgres，konga其实还支持mysql，mongo等多种数据库，这里就不赘述了。配置完成之后，需要登上你自己的postgres数据库，然后执行如下命令新建数据库：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">CREATE DATABASE &quot;konga&quot; WITH ENCODING=&apos;UTF8&apos;;</div></pre></td></tr></table></figure></p><p>新建了数据库之后，，执行如下命令，来初始化数据库：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">node ./bin/konga.js prepare --adapter postgres --uri postgresql://kong_user:kong_pass@localhost:5432/konga</div></pre></td></tr></table></figure></p><p>出现如下提示之后，数据库这一块就完成：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line">Preparing database...</div><div class="line">debug: Hook:api_health_checks:process() called</div><div class="line">debug: Hook:health_checks:process() called</div><div class="line">debug: Hook:start-scheduled-snapshots:process() called</div><div class="line">debug: Hook:upstream_health_checks:process() called</div><div class="line">debug: Hook:user_events_hook:process() called</div><div class="line">debug: Seeding User...</div><div class="line">debug: User seed planted</div><div class="line">debug: Seeding Kongnode...</div><div class="line">debug: Kongnode seed planted</div><div class="line">debug: Seeding Emailtransport...</div><div class="line">debug: Emailtransport seed planted</div><div class="line">debug: Database migrations completed!</div></pre></td></tr></table></figure></p><h3 id="启动konga"><a href="#启动konga" class="headerlink" title="启动konga"></a>启动konga</h3><p>执行如下命令：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">npm start</div></pre></td></tr></table></figure></p><p>看到如下小帆船的图，成功启动<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"></div><div class="line">  Sails              &lt;|    .-..-.</div><div class="line">  v0.12.14            |\</div><div class="line">                     /|.\</div><div class="line">                    / || \</div><div class="line">                  ,&apos;  |&apos;  \</div><div class="line">               .-&apos;.-==|/_--&apos;</div><div class="line">               `--&apos;-------&apos; </div><div class="line">  __---___--___---___--___---___--___</div><div class="line">____---___--___---___--___---___--___-__</div></pre></td></tr></table></figure></p><p>最后，打开浏览器输入<code>http://localhost:1337/</code>，就可以进入konga的管理界面了。会出现一个让你注册的界面，注册登录一下，然后配置一下kong的admin-api链接，大功告成，打完收工。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;微服务网关kong有比较多个后台管理面板，比如比较简单的kong-dashboard，还有konga，之前在初探kong的时候，使用的就是比较简单的kong-dashboard，很多功能都没有，而且最近由于kong官方更新比较频繁，1.0之后的kong-dashboard就
      
    
    </summary>
    
      <category term="与代码" scheme="http://git.102no.com/categories/%E4%B8%8E%E4%BB%A3%E7%A0%81/"/>
    
    
      <category term="Kong" scheme="http://git.102no.com/tags/Kong/"/>
    
      <category term="Kong中文教程" scheme="http://git.102no.com/tags/Kong%E4%B8%AD%E6%96%87%E6%95%99%E7%A8%8B/"/>
    
      <category term="Kong中文文档" scheme="http://git.102no.com/tags/Kong%E4%B8%AD%E6%96%87%E6%96%87%E6%A1%A3/"/>
    
      <category term="Konga" scheme="http://git.102no.com/tags/Konga/"/>
    
  </entry>
  
  <entry>
    <title>微服务 API 网关 Kong 中文文档发布</title>
    <link href="http://git.102no.com/2019/06/27/kong-docs-cn/"/>
    <id>http://git.102no.com/2019/06/27/kong-docs-cn/</id>
    <published>2019-06-26T16:00:00.000Z</published>
    <updated>2019-07-07T15:21:19.125Z</updated>
    
    <content type="html"><![CDATA[<p>由于项目的原因，最近的几个月一直在学习微服务的API网关 Kong ，在这里做一个简单介绍，是一个云原生，高效，可扩展的分布式 API 网关。 自 2015 年在 github 开源后，广泛受到关注，目前已收获 1.68w+ 的 star，其核心价值在于高性能和可扩展性。由于对项目的积极维护，Kong被广泛用于从初创公司到全球5000强以及政府机构的生产中。从技术角度来说，Kong是基于Openresty的一个莹莹，Openresty是基于Nginx的，使用的语言是Lua。</p><ul><li>官方网站：<a href="https://konghq.com" target="_blank" rel="external">https://konghq.com</a></li><li>官方文档：<a href="https://docs.konghq.com" target="_blank" rel="external">https://docs.konghq.com</a></li><li>项目地址：<a href="https://github.com/Kong/kong" target="_blank" rel="external">https://github.com/Kong/kong</a></li></ul><p>所以在学习过程中，首要是需要看官方文档，由于项目比较新，所以暂时没有中文文档，我就想着，反正文档总是要全部看一遍的，不如自己翻译一份好了，于是乎就有了这个项目：Kong的文档中文版。欢迎大家star&amp;fork。</p><p>Kong中文文档项目地址：<a href="https://github.com/qianyugang/kong-docs-cn" target="_blank" rel="external">https://github.com/qianyugang/kong-docs-cn</a><br>由于自己不是英语专业，而且主要目的是学习Kong，所以采用的是人工+机翻结合的方式，如果有遇到翻译的不够通顺，或者对于翻译的语句有歧义的地方，麻烦一定点击官网英文文档<a href="https://docs.konghq.com/" target="_blank" rel="external">https://docs.konghq.com/</a> 查看，并且欢迎提 PR 提修改意见。另，由于kong的文档本身也在不断增加和完善当中，如果有遇到没有即使更新翻译的状况欢迎提issue，我会不断补充的。</p><p>todo：</p><ul><li>目前文档中的超链接都是链接的英文原文，后续会慢慢改成中文内链。</li><li>会在每一页文档里面附上单独的英文原文链接，以便做对照。</li><li>会添加kong自带的插件文档。</li></ul><p>本文档是基于 <a href="https://docs.konghq.com/1.1.x/" target="_blank" rel="external">https://docs.konghq.com/1.1.x/</a> 1.1.x 版本，目前官网已经更新至 1.2.x 版本，如果使用的最新版本，请查看 <a href="https://docs.konghq.com" target="_blank" rel="external">https://docs.konghq.com</a> 并注意差别。   </p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;由于项目的原因，最近的几个月一直在学习微服务的API网关 Kong ，在这里做一个简单介绍，是一个云原生，高效，可扩展的分布式 API 网关。 自 2015 年在 github 开源后，广泛受到关注，目前已收获 1.68w+ 的 star，其核心价值在于高性能和可扩展性。由
      
    
    </summary>
    
      <category term="与代码" scheme="http://git.102no.com/categories/%E4%B8%8E%E4%BB%A3%E7%A0%81/"/>
    
    
      <category term="Apigateway" scheme="http://git.102no.com/tags/Apigateway/"/>
    
      <category term="api网关" scheme="http://git.102no.com/tags/api%E7%BD%91%E5%85%B3/"/>
    
      <category term="Kong" scheme="http://git.102no.com/tags/Kong/"/>
    
      <category term="Kong中文教程" scheme="http://git.102no.com/tags/Kong%E4%B8%AD%E6%96%87%E6%95%99%E7%A8%8B/"/>
    
      <category term="Kong中文文档" scheme="http://git.102no.com/tags/Kong%E4%B8%AD%E6%96%87%E6%96%87%E6%A1%A3/"/>
    
      <category term="OpenResty" scheme="http://git.102no.com/tags/OpenResty/"/>
    
  </entry>
  
  <entry>
    <title>Lua OpenResty 使用 protobuf 和 rabbitmq AMQP 发送和接收消息</title>
    <link href="http://git.102no.com/2019/06/26/lua-openresty-rabbitmq-protobuf/"/>
    <id>http://git.102no.com/2019/06/26/lua-openresty-rabbitmq-protobuf/</id>
    <published>2019-06-25T16:00:00.000Z</published>
    <updated>2019-06-26T14:29:51.445Z</updated>
    
    <content type="html"><![CDATA[<p>项目中有个需求，需要使用Google的protobuf作为压缩协议，然后使用rabbitmq AMQP来发送和接收消息，在研究使用这两个工具中，遇到了有一些坑，之前有写了两篇来介绍分别使用，《 <a href="http://www.102no.com/archives/1570" target="_blank" rel="external">在 lua 中使用 protobuf</a>》和《<a href="http://www.102no.com/archives/1574" target="_blank" rel="external">Lua OpenResty 使用rabbitmq AMQP协议发送和接收消息</a>》 ，这里我们来结合使用一下，然后顺便解决一下lua的相关库的使用问题。</p><h2 id="protobuf-相关步骤"><a href="#protobuf-相关步骤" class="headerlink" title="protobuf 相关步骤"></a>protobuf 相关步骤</h2><h3 id="1-安装-protoc"><a href="#1-安装-protoc" class="headerlink" title="1.安装  protoc"></a>1.安装  protoc</h3><p>首先安装依赖库<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">sudo apt-get install autoconf automake libtool curl make g++ unzip</div></pre></td></tr></table></figure></p><p>下载、解压安装包<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">curl -L -o protobuf-all-3.6.1.tar.gz  https://github.com/protocolbuffers/protobuf/releases/download/v3.6.1/protobuf-all-3.6.1.tar.gz</div><div class="line">tar -xzvf protobuf-all-3.6.1.tar.gz</div></pre></td></tr></table></figure></p><p>进入安装包安装<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">cd protobuf-all-3.6.1</div><div class="line">./configure</div><div class="line">make</div><div class="line">make check</div><div class="line">sudo make install</div><div class="line">sudo ldconfig</div></pre></td></tr></table></figure></p><p>最后检查是否安装成功<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">$ protoc --version</div><div class="line">libprotoc 3.6.1</div></pre></td></tr></table></figure></p><h3 id="2-生成pb文件"><a href="#2-生成pb文件" class="headerlink" title="2.生成pb文件"></a>2.生成pb文件</h3><p>需要定义你自己的标准proto文件 <code>xxx.proto</code> 文件（可能不止一个文件，不止一个目录），定义好了之后，使用<code>protoc</code>命令生成<code>.pb</code>文件，命令如下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">protoc --proto_path=proto --descriptor_set_out=common.pb proto/xxxx/*.proto proto/xxxxx/*.proto</div></pre></td></tr></table></figure></p><p>其中 <code>--proto_path</code> 参数是你的proto文件目录，<code>--descriptor_set_out</code> 是你需要输出<code>.pb</code>文件的目录，后面几个参数就是具体要引入的<code>.proto</code>文件。这里需要注意的是，有时候会有多个proto文件并且多目录import的情况，这个时候，就需要在参数中都体现出来（命令的最后两个参数），这条命令是把所有的 <code>.proto</code> 文件生成了一个 <code>common.pb</code> 文件方便引入。注意这个<code>common.pb</code>的路径，后面需要用到。</p><h3 id="3-安装lua的protobuf库"><a href="#3-安装lua的protobuf库" class="headerlink" title="3.安装lua的protobuf库"></a>3.安装lua的protobuf库</h3><p>这里我们使用的是lua-protobuf库。 lua-protobuf实际上是一个纯C的protobuf协议实现，和对应的Lua绑定。</p><p>项目地址：<a href="https://github.com/starwing/lua-protobuf" target="_blank" rel="external">https://github.com/starwing/lua-protobuf</a></p><p>可以使用 luarocks 安装lua-protobuf</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">luarocks install lua-protobuf</div></pre></td></tr></table></figure><p>如果没有安装 luarocks 可以安装一下luarocks。</p><h2 id="rabbitmq-AMQP-相关步骤"><a href="#rabbitmq-AMQP-相关步骤" class="headerlink" title="rabbitmq AMQP 相关步骤"></a>rabbitmq AMQP 相关步骤</h2><h3 id="1-安装第三方库"><a href="#1-安装第三方库" class="headerlink" title="1.安装第三方库"></a>1.安装第三方库</h3><p>如果想要在openresty中使用AMQP协议发送和接收消息的话，需要使用到一个第三方库。地址为：<a href="https://github.com/4mig4/lua-amqp" target="_blank" rel="external">https://github.com/4mig4/lua-amqp</a> 。这里你会发现，这个库和《<a href="http://www.102no.com/archives/1574" target="_blank" rel="external">Lua OpenResty 使用rabbitmq AMQP协议发送和接收消息</a>》文章中介绍使用的库不一样，确实是的，因为在使用过程中我发现，之前的那个库还有一些功能不完善，这里使用的这个库是作者fork了之前的那个项目，并改进了里面许多功能。所以最终我选用了这个库来开发。</p><p>安装方式为，使用luarocks安装，由于这个库没有push到<a href="https://luarocks.org/" target="_blank" rel="external">https://luarocks.org/</a> 仓库当中，所以我们需要使用到其中的声明文件安装。把项目clone下来之后，找到这个文件 amqp-1.0-4.rockspec 这个文件，然后执行：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">luarocks build amqp-1.0-4.rockspec</div></pre></td></tr></table></figure></p><p>这里直接执行，可能会报错，报错信息为<code>unrecognized filename extension</code>，意思是识别不了文件格式之类的，这里需要把这个文件中的<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">source = &#123;</div><div class="line">   url = &quot;https://github.com/4mig4/lua-amqp.git&quot;,</div><div class="line">   tag = &quot;&quot;,</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><p>修改为：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">source = &#123;</div><div class="line">   url = &quot;https://github.com/4mig4/lua-amqp&quot;,</div><div class="line">   tag = &quot;&quot;,</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><p>然后继续执行，执行完成之后，就可以在<code>/usr/local/share/lua/5.1</code>目录中看到这个第三方库了，如果整理的文件夹名字不是<code>amqp</code>，而是一个amqp加版本号，可以手动直接把文件夹修改为<code>amqp</code>即可。到此第三方库安装完成。</p><p>在使用过程中，我发现一个问题，作者在fork了项目之后，增加了CQUEUES, NGX.SOCKET, SOCKET一些通讯协议功能，但是我本地环境直接运行会报错，查看了一些源码，发现是我的本地环境的<code>cqueues</code>支持有点问题，而作者把这个作为最优先的协议，那么我就把源码文件<a href="https://github.com/4mig4/lua-amqp/blob/master/amqp/init.lua" target="_blank" rel="external">https://github.com/4mig4/lua-amqp/blob/master/amqp/init.lua</a> 中的<code>local use_cqueues = true</code>改为<code>local use_cqueues = false</code>，即关闭cqueues来使用了。</p><h2 id="发送和接收消息"><a href="#发送和接收消息" class="headerlink" title="发送和接收消息"></a>发送和接收消息</h2><p>假设你的proto结构体是这样的：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line">syntax = &quot;proto3&quot;;</div><div class="line">package aa.bb;</div><div class="line">message EventEnvelope &#123;</div><div class="line">  string id = 1; </div><div class="line">  int64 created_ts = 2; </div><div class="line">  string server_hash = 3;</div><div class="line">  int64 happened_ts = 4; </div><div class="line">  oneof body &#123;</div><div class="line">    aa.bb.LoggedOut logged_out = 1501;</div><div class="line">  &#125;</div><div class="line">message LoggedOut &#123;</div><div class="line">  int64 union_id = 1;</div><div class="line">  string app_key = 2;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><h3 id="发送消息"><a href="#发送消息" class="headerlink" title="发送消息"></a>发送消息</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div><div class="line">40</div><div class="line">41</div><div class="line">42</div><div class="line">43</div><div class="line">44</div><div class="line">45</div><div class="line">46</div><div class="line">47</div><div class="line">48</div><div class="line">49</div><div class="line">50</div></pre></td><td class="code"><pre><div class="line">local function send_pb()</div><div class="line"></div><div class="line">    -- 引入pb库</div><div class="line">local pb   = require &quot;pb&quot;</div><div class="line">-- 加载pb文件</div><div class="line">    assert(pb.loadfile(&quot;xxxxx/common.pb&quot;))</div><div class="line"></div><div class="line">-- 这里是你需要发送的pb消息，注意这里是支持嵌套的，如果你的protobuf文件里面有one of，可以直接使用多层嵌套</div><div class="line">    -- 这里就根据你自己protobuf结构来就好</div><div class="line">    local data    = &#123;</div><div class="line">        created_ts  = ngx.now() * 1000,</div><div class="line">        server_hash = &apos;localhost&apos;,</div><div class="line">        happened_ts = ngx.now() * 1000,</div><div class="line">        id          = 8376548368364,</div><div class="line">        logged_out  = &#123;</div><div class="line">            union_id = 123455666,</div><div class="line">            app_key  = xxxxxxxx</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">    </div><div class="line">    -- 生成需要发送的消息，这个消息是proto压缩之后的，这个`EventEnvelope` 就是最外层的结构体</div><div class="line">local messages = assert(pb.encode(&quot;aa.bb.EventEnvelope&quot;, data))</div><div class="line"></div><div class="line">-- 引入amqp第三方库</div><div class="line">    local amqp        = require &quot;amqp&quot;</div><div class="line"></div><div class="line">-- 里面的一些参数就不再赘述了，都是rabbitmq的一些参数</div><div class="line">    local ctx = amqp.new(&#123;</div><div class="line">        role        = &quot;publisher&quot;,</div><div class="line">        exchange    = &quot;exchangexxxx&quot;,</div><div class="line">        ssl         = false,</div><div class="line">        user        = &quot;guest&quot;,</div><div class="line">        password    = &quot;guest&quot;,</div><div class="line">        auto_delete = false,</div><div class="line">        routing_key = &quot;routing_keyxxxxx&quot;,</div><div class="line">        passive     = true,</div><div class="line">        no_ack      = true,</div><div class="line">        no_wait     = false,</div><div class="line">    &#125;)</div><div class="line">    ctx:connect(&quot;127.0.0.1&quot;, port)</div><div class="line">    ctx:setup()</div><div class="line">    local ok, err = ctx:publish(messages)</div><div class="line">    if not ok then</div><div class="line">        ngx.log(ngx.ERR, &quot;[ -- rabbitmq send failed : -- ] &quot; .. err)</div><div class="line">    else</div><div class="line">        ngx.log(ngx.ERR, &quot;[ -- rabbitmq send success ]&quot;)</div><div class="line">    end</div><div class="line">end</div><div class="line"></div><div class="line">send_pb(&quot;this is a message&quot;)</div></pre></td></tr></table></figure><h3 id="接收消息"><a href="#接收消息" class="headerlink" title="接收消息"></a>接收消息</h3><p>可以新建一个<code>consume_queue.lua</code>文件，然后如下代码：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div><div class="line">40</div><div class="line">41</div><div class="line">42</div></pre></td><td class="code"><pre><div class="line"></div><div class="line">-- 依旧是引入</div><div class="line">local amqp            = require &quot;amqp&quot;</div><div class="line">local pb              = require &quot;pb&quot;</div><div class="line"></div><div class="line">-- 这里是回调函数</div><div class="line">local function consume_local(f)</div><div class="line">    print(f) -- 这里就是消息的所有信息，里面包含了properties ，body，frame等信息 </div><div class="line">    print(f.body) -- 这里就是消息的主体</div><div class="line">    </div><div class="line">   -- 加载pb文件</div><div class="line">    assert(pb.loadfile(&quot;xxxxx/common.pb&quot;))</div><div class="line">    -- 解析消息</div><div class="line">    local data = assert(pb.decode(&quot;aa.bb.EventEnvelope&quot;, f.body))</div><div class="line">print(data) </div><div class="line">    </div><div class="line">end</div><div class="line"></div><div class="line">local amqp        = require &quot;amqp&quot;</div><div class="line"></div><div class="line">local ctx = amqp.new(&#123;</div><div class="line">    role        = &quot;consumer&quot;,</div><div class="line">    queue       = &quot;eventbus1&quot;, -- 这里可以自定义</div><div class="line">    exchange    = &quot;exchangexxxx&quot;,</div><div class="line">    ssl         = false,</div><div class="line">    user        = &quot;guest&quot;,</div><div class="line">    password    = &quot;guest&quot;,</div><div class="line">    no_wait     = false,</div><div class="line">    routing_key = &quot;routing_keyxxxxx&quot;,</div><div class="line">    auto_delete = false, -- 是否自动删除消息</div><div class="line">    no_ack      = true,</div><div class="line">    exclusive   = false, -- 是否为排他队列</div><div class="line">    callback    = consume_local, -- 回调函数</div><div class="line">    durable     = true,</div><div class="line">    passive     = false,</div><div class="line">    type        = &quot;topic&quot;</div><div class="line"></div><div class="line">&#125;)</div><div class="line"></div><div class="line">ctx:connect(&quot;127.0.0.1&quot;, port)</div><div class="line"></div><div class="line">local ok, err = ctx:consume()</div></pre></td></tr></table></figure></p><p>执行命令开始消费消息<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">/usr/local/openresty/bin/resty consume_queue.lua</div></pre></td></tr></table></figure></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;项目中有个需求，需要使用Google的protobuf作为压缩协议，然后使用rabbitmq AMQP来发送和接收消息，在研究使用这两个工具中，遇到了有一些坑，之前有写了两篇来介绍分别使用，《 &lt;a href=&quot;http://www.102no.com/archives/1
      
    
    </summary>
    
      <category term="与代码" scheme="http://git.102no.com/categories/%E4%B8%8E%E4%BB%A3%E7%A0%81/"/>
    
    
      <category term="OpenResty" scheme="http://git.102no.com/tags/OpenResty/"/>
    
      <category term="Lua" scheme="http://git.102no.com/tags/Lua/"/>
    
      <category term="protobuf" scheme="http://git.102no.com/tags/protobuf/"/>
    
      <category term="rabbitmq" scheme="http://git.102no.com/tags/rabbitmq/"/>
    
      <category term="amqp" scheme="http://git.102no.com/tags/amqp/"/>
    
  </entry>
  
  <entry>
    <title>微服务 API 网关 Kong 日志中文文档</title>
    <link href="http://git.102no.com/2019/06/20/kong-logging-docs/"/>
    <id>http://git.102no.com/2019/06/20/kong-logging-docs/</id>
    <published>2019-06-19T16:00:00.000Z</published>
    <updated>2019-06-26T14:30:05.090Z</updated>
    
    <content type="html"><![CDATA[<p>原文链接: <a href="https://docs.konghq.com/1.1.x/logging/" target="_blank" rel="external">https://docs.konghq.com/1.1.x/logging/</a> （不能保证所有的翻译都是准确无误的，所有如有翻译的不准确或错误之处，请一定记得查看原文，并欢迎留言指出）。</p><h2 id="日志等级"><a href="#日志等级" class="headerlink" title="日志等级"></a>日志等级</h2><p>日志级别在Kong的配置中设置。以下是日志级别，按照严重程度顺序递增，<code>debug</code>, <code>info</code>, <code>notice</code>, <code>warn</code>, <code>error</code> and <code>crit</code>。</p><ul><li><code>debug</code>:它提供有关插件的runloop和每个插件或其他组件的调试信息。只是在调试期间使用，因为它的消息量太多了。</li><li><code>info</code>/<code>notice</code>:kong没有在这两个级别上产生很大的差异。提供有关正常行为的信息，其中大多数行为可以忽略。</li><li><code>warn</code>:要记录任何不会导致事务丢失但需要进一步调查的异常行为，应使用警告级别。</li><li><code>error</code>:用于记录导致请求被停止的错误（例如，获取HTTP 500错误）。需要监控此类日志的速率。</li><li><code>crit</code>:当Kong在紧急条件下工作而不能正常工作从而影响多个客户时，使用此级别。</li></ul><p>默认情况下，<code>notice</code>是使用和建议的日志级别。然而，如果日志变得过于繁琐，他们可能会被提升到更高的水平，就像<code>warn</code>一样。</p><h2 id="从Kong日志中删除某些元素"><a href="#从Kong日志中删除某些元素" class="headerlink" title="从Kong日志中删除某些元素"></a>从Kong日志中删除某些元素</h2><p>随着围绕保护私人数据（如GDPR）的新规定，您可能需要改变您的日志记录习惯。如果您使用Kong作为API网关，则可以在一个位置完成此操作以使所有API生效。本指南将引导您完成一个实现此目的的方法，但总有不同的方法来满足不同的需求。请注意，这些更改将影响NGINX访问日志的输出。这对Kong的日志插件没有任何影响。</p><p>举个例子，假设您要从kong日志中删除任何电子邮件地址实例。电子邮件地址可能以不同的方式出现，例如<code>/apiname/v2/verify/alice@example.com</code> 或者 <code>/v3/verify?alice@example.com</code>。为了防止这些被添加到日志中，我们需要使用自定义NGINX模板。</p><p>要开始使用自定义NGINX模板，请先获取我们模板的副本。<br>这可以在 <a href="https://docs.konghq.com/latest/configuration/#custom-nginx-templates-embedding-kong" target="_blank" rel="external">https://docs.konghq.com/latest/configuration/#custom-nginx-templates-embedding-kong</a> 找到或从下面复制<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div></pre></td><td class="code"><pre><div class="line"># ---------------------</div><div class="line"># custom_nginx.template</div><div class="line"># ---------------------</div><div class="line"></div><div class="line">worker_processes $; # can be set by kong.conf</div><div class="line">daemon $;                     # can be set by kong.conf</div><div class="line"></div><div class="line">pid pids/nginx.pid;                      # this setting is mandatory</div><div class="line">error_log logs/error.log $; # can be set by kong.conf</div><div class="line"></div><div class="line">events &#123;</div><div class="line">    use epoll; # custom setting</div><div class="line">    multi_accept on;</div><div class="line">&#125;</div><div class="line"></div><div class="line">http &#123;</div><div class="line">    # include default Kong Nginx config</div><div class="line">    include &apos;nginx-kong.conf&apos;;</div><div class="line"></div><div class="line">    # custom server</div><div class="line">    server &#123;</div><div class="line">        listen 8888;</div><div class="line">        server_name custom_server;</div><div class="line"></div><div class="line">        location / &#123;</div><div class="line">          ... # etc</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><p>为了控制日志中的内容，我们将在模板中使用NGINX 的map模块。有关使用map指令的更多详细信息，<a href="http://nginx.org/en/docs/http/ngx_http_map_module.html" target="_blank" rel="external">请参阅本指南</a>。这将创建一个新变量，其值取决于第一个参数中指定的一个或多个源变量的值。格式为：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">map $paramater_to_look_at $variable_name &#123;</div><div class="line">    pattern_to_look_for 0;</div><div class="line">    second_pattern_to_look_for 0;</div><div class="line"></div><div class="line">    default 1;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><p>举个例子，我们将映射一个名为<code>keeplog</code>的新变量，该变量依赖于<code>$request_uri</code>中出现的某些值。我们将把map指令放在http块的开头，这必须在 <code>include&#39;nginx-kong.conf&#39;</code> 之前。因此，对于我们的示例，我们将添加以下内容：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">map $request_uri $keeplog &#123;</div><div class="line">    ~.+\@.+\..+ 0;</div><div class="line">    ~/servicename/v2/verify 0;</div><div class="line">    ~/v3/verify 0;</div><div class="line"></div><div class="line">    default 1;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><p>您可能会注意到这些行中的每一行都以波形符号开头。这就是NGINX在评估生产线时使用RegEx的原因。<br>在这个例子中我们有三件事需要寻找：</p><ul><li>第一行使用正则表达式查找x@y.z格式的任何电子邮件地址</li><li>第二行查找URI的任何部分，即/servicename/v2/verify</li><li>第三行查看包含/v3/verify的URI的任何部分</li></ul><p>因为所有这些都具有0以外的值，如果请求具有其中一个元素，则不会将其添加到日志中。</p><p>现在，我们需要为日志中保留的内容设置日志格式。我们将使用<code>log_format</code>模块并为我们的新日志指定show_everything的名称。日志的内容可以根据您的需要进行定制，但在这个例子中，我会简单地将一切改回kong标准，要查看可以使用的完整选项列表，请<a href="https://nginx.org/en/docs/http/ngx_http_core_module.html#variables" target="_blank" rel="external">参阅本指南</a>。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">log_format show_everything &apos;$remote_addr - $remote_user [$time_local] &apos;</div><div class="line">    &apos;$request_uri $status $body_bytes_sent &apos;</div><div class="line">    &apos;&quot;$http_referer&quot; &quot;$http_user_agent&quot;&apos;;</div></pre></td></tr></table></figure></p><p>现在，我们的自定义NGINX模板已经可以使用了。如果您一直观察，您的文件现在应该如下所示：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div></pre></td><td class="code"><pre><div class="line"># ---------------------</div><div class="line"># custom_nginx.template</div><div class="line"># ---------------------</div><div class="line"></div><div class="line">worker_processes $; # can be set by kong.conf</div><div class="line">daemon $;                     # can be set by kong.conf</div><div class="line"></div><div class="line">pid pids/nginx.pid;                      # this setting is mandatory</div><div class="line">error_log stderr $; # can be set by kong.conf</div><div class="line"></div><div class="line"></div><div class="line"></div><div class="line">events &#123;</div><div class="line">    use epoll; # custom setting</div><div class="line">    multi_accept on;</div><div class="line">&#125;</div><div class="line"></div><div class="line">http &#123;</div><div class="line"></div><div class="line"></div><div class="line">    map $request_uri $keeplog &#123;</div><div class="line">        ~.+\@.+\..+ 0;</div><div class="line">        ~/v1/invitation/ 0;</div><div class="line">        ~/reset/v1/customer/password/token 0;</div><div class="line">        ~/v2/verify 0;</div><div class="line"></div><div class="line">        default 1;</div><div class="line">    &#125;</div><div class="line">    log_format show_everything &apos;$remote_addr - $remote_user [$time_local] &apos;</div><div class="line">        &apos;$request_uri $status $body_bytes_sent &apos;</div><div class="line">        &apos;&quot;$http_referer&quot; &quot;$http_user_agent&quot;&apos;;</div><div class="line"></div><div class="line">    include &apos;nginx-kong.conf&apos;;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><p>我们需要做的最后一件事是告诉Kong使用新创建的日志，<code>show_everything</code>，为此，我们将改变Kong变量<code>prpxy_access_log</code>。通过打开和编辑<code>etc/kong/kong.conf</code>或使用环境变量<code>KONG_PROXY_ACCESS_LOG=</code>来修改默认位置以显示。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">proxy_access_log=logs/access.log show_everything if=$keeplog</div></pre></td></tr></table></figure></p><p>最后一步，重启kong，使修改东西都生效，你可以使用<code>kong restart</code>命令来操作。</p><p>现在，将不再记录使用其中的电子邮件地址发出的任何请求。<br>当然，我们可以使用此逻辑以条件方式从日志中删除任何我们想要的内容。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;原文链接: &lt;a href=&quot;https://docs.konghq.com/1.1.x/logging/&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;https://docs.konghq.com/1.1.x/logging/&lt;/a&gt; （不能保证所有的
      
    
    </summary>
    
      <category term="与代码" scheme="http://git.102no.com/categories/%E4%B8%8E%E4%BB%A3%E7%A0%81/"/>
    
    
      <category term="Kong" scheme="http://git.102no.com/tags/Kong/"/>
    
      <category term="Kong教程" scheme="http://git.102no.com/tags/Kong%E6%95%99%E7%A8%8B/"/>
    
      <category term="Kong中文文档" scheme="http://git.102no.com/tags/Kong%E4%B8%AD%E6%96%87%E6%96%87%E6%A1%A3/"/>
    
  </entry>
  
  <entry>
    <title>微服务 API 网关 Kong 代理中文文档</title>
    <link href="http://git.102no.com/2019/06/12/kong-proxy-docs/"/>
    <id>http://git.102no.com/2019/06/12/kong-proxy-docs/</id>
    <published>2019-06-11T16:00:00.000Z</published>
    <updated>2019-06-12T14:48:13.850Z</updated>
    
    <content type="html"><![CDATA[<p>原文链接: <a href="https://docs.konghq.com/1.1.x/proxy/" target="_blank" rel="external">https://docs.konghq.com/1.1.x/proxy/</a> （不能保证所有的翻译都是准确无误的，所有如有翻译的不准确或错误之处，请一定记得查看原文，并欢迎留言指出）。</p><h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>在本文档中，我们将通过详细解释其路由功能和内部工作原理，来了解Kong的代理功能。</p><p>Kong公开了几个可以通过两个配置属性调整的接口：</p><ul><li><code>proxy_listen</code>：它定义了一个地址/端口列表，Kong将接受来自客户端的公共流量并将其代理到您的上游服务（默认为<code>8000</code>）。</li><li><code>admin_listen</code>：它还定义了一个地址和端口列表，但是这些应该被限制为仅由管理员访问，因为它们暴露了Kong的配置功能：Admin API（默认为<code>8001</code>）。</li></ul><blockquote><p>注意：从1.0.0开始，API实体已被删除。本文档将介绍使用新路由和服务实体进行代理。<br>如果您使用的是0.12或更低版本，请参阅本文档的旧版本。</p></blockquote><h2 id="相关术语"><a href="#相关术语" class="headerlink" title="相关术语"></a>相关术语</h2><ul><li><code>client</code>：指下游 downstream 客户向Kong的代理端口发出请求。</li><li><code>upstream service</code>：指位于Kong后面的您自己的 API/service ，转发客户端请求。</li><li><code>Service</code>：顾名思义，服务实体是每个上游服务的抽象。比如说服务可以是数据转换微服务，一个计费API等。</li><li><code>Route</code>：这是指Kong Routes实体。路由是进入Kong的入口点，并定义要匹配的请求的规则，并路由到给定的服务。</li><li><code>Plugin</code>：这指的是Kong“插件”，它们是在代理生命周期中运行的业务逻辑。可以通过Admin API配置插件 - 全局（所有传入流量）或特定路由和服务。</li></ul><h2 id="概览"><a href="#概览" class="headerlink" title="概览"></a>概览</h2><p>从高层次的角度来看，Kong在其配置的代理端口上监听HTTP流量（默认情况下为<code>8000</code>和<code>8443</code>）。Kong将根据您配置的路由评估任何传入的HTTP请求，并尝试查找匹配的路由。如果给定的请求与特定Route的规则匹配，Kong将处理代理请求。由于每个Route都链接到一个Service，因此Kong将运行您在Route及其相关服务上配置的插件，然后在上游代理请求。</p><p>您可以通过Kong的Admin API管理Routes。Routes 的机 <code>hosts</code>，<code>psths</code>和 <code>methods</code> 属性定义用于匹配传入HTTP请求的规则。</p><p>如果Kong收到的请求无法与任何已配置的路由匹配（或者如果没有配置路由），它将响应：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">HTTP/1.1 404 Not Found</div><div class="line">Content-Type: application/json</div><div class="line">Server: kong/&lt;x.x.x&gt;</div><div class="line"></div><div class="line">&#123;</div><div class="line">    &quot;message&quot;: &quot;no route and no Service found with those values&quot;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><h2 id="提醒：如何配置服务"><a href="#提醒：如何配置服务" class="headerlink" title="提醒：如何配置服务"></a>提醒：如何配置服务</h2><p><a href="https://docs.konghq.com/1.1.x/getting-started/configuring-a-service" target="_blank" rel="external">配置服务</a>快速入门指南介绍了如何通过[Admin API]配置Kong。</p><p>通过向Admin API发送HTTP请求来向Kong添加服务：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div></pre></td><td class="code"><pre><div class="line">curl -i -X POST http://localhost:8001/services/ \</div><div class="line">    -d &apos;name=foo-service&apos; \</div><div class="line">    -d &apos;url=http://foo-service.com&apos;</div><div class="line">HTTP/1.1 201 Created</div><div class="line">...</div><div class="line"></div><div class="line">&#123;</div><div class="line">    &quot;connect_timeout&quot;: 60000,</div><div class="line">    &quot;created_at&quot;: 1515537771,</div><div class="line">    &quot;host&quot;: &quot;foo-service.com&quot;,</div><div class="line">    &quot;id&quot;: &quot;d54da06c-d69f-4910-8896-915c63c270cd&quot;,</div><div class="line">    &quot;name&quot;: &quot;foo-service&quot;,</div><div class="line">    &quot;path&quot;: &quot;/&quot;,</div><div class="line">    &quot;port&quot;: 80,</div><div class="line">    &quot;protocol&quot;: &quot;http&quot;,</div><div class="line">    &quot;read_timeout&quot;: 60000,</div><div class="line">    &quot;retries&quot;: 5,</div><div class="line">    &quot;updated_at&quot;: 1515537771,</div><div class="line">    &quot;write_timeout&quot;: 60000</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><p>该请求指示Kong注册一个名为“foo-service”的服务，该服务指向<code>http://foo-service.com</code>（上游）。</p><p>注意：<code>url</code>参数是一个简化参数，用于一次性添加<code>protocol</code>，<code>host</code>，<code>port</code>和<code>path</code>。</p><p>现在，为了通过Kong向这个服务发送流量，我们需要指定一个Route，它作为Kong的入口点：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div></pre></td><td class="code"><pre><div class="line">curl -i -X POST http://localhost:8001/routes/ \</div><div class="line">    -d &apos;hosts[]=example.com&apos; \</div><div class="line">    -d &apos;paths[]=/foo&apos; \</div><div class="line">    -d &apos;service.id=d54da06c-d69f-4910-8896-915c63c270cd&apos;</div><div class="line">HTTP/1.1 201 Created</div><div class="line">...</div><div class="line"></div><div class="line">&#123;</div><div class="line">    &quot;created_at&quot;: 1515539858,</div><div class="line">    &quot;hosts&quot;: [</div><div class="line">        &quot;example.com&quot;</div><div class="line">    ],</div><div class="line">    &quot;id&quot;: &quot;ee794195-6783-4056-a5cc-a7e0fde88c81&quot;,</div><div class="line">    &quot;methods&quot;: null,</div><div class="line">    &quot;paths&quot;: [</div><div class="line">        &quot;/foo&quot;</div><div class="line">    ],</div><div class="line">    &quot;preserve_host&quot;: false,</div><div class="line">    &quot;priority&quot;: 0,</div><div class="line">    &quot;protocols&quot;: [</div><div class="line">        &quot;http&quot;,</div><div class="line">        &quot;https&quot;</div><div class="line">    ],</div><div class="line">    &quot;service&quot;: &#123;</div><div class="line">        &quot;id&quot;: &quot;d54da06c-d69f-4910-8896-915c63c270cd&quot;</div><div class="line">    &#125;,</div><div class="line">    &quot;strip_path&quot;: true,</div><div class="line">    &quot;updated_at&quot;: 1515539858</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><p>我们现在已经配置了一个Route来匹配与给定<code>host</code>和<code>path</code>匹配的传入请求，并将它们转发到我们配置的<code>foo-service</code>，从而将此流量代理到<code>http://foo-service.com</code>。</p><h2 id="路由和匹配功能"><a href="#路由和匹配功能" class="headerlink" title="路由和匹配功能"></a>路由和匹配功能</h2><p>现在让我们讨论Kong如何匹配针对路由的已配置<code>host</code>，<code>path</code>和<code>methods</code>属性（或字段）的请求。请注意，所有这三个字段都是可选的，但必须至少指定其中一个。</p><p>对于匹配路线的请求：</p><ul><li>请求必须包含所有已配置的字段</li><li>请求中的字段值必须至少与其中一个配置值匹配（当字段配置接受一个或多个值时，请求只需要其中一个值被视为匹配）</li></ul><p>我们来看几个例子。考虑如下配置的路由：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">&#123;</div><div class="line">    &quot;hosts&quot;: [&quot;example.com&quot;, &quot;foo-service.com&quot;],</div><div class="line">    &quot;paths&quot;: [&quot;/foo&quot;, &quot;/bar&quot;],</div><div class="line">    &quot;methods&quot;: [&quot;GET&quot;]</div><div class="line">&#125;</div></pre></td></tr></table></figure><p>与此Route匹配的一些可能请求如下所示：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">GET /foo HTTP/1.1</div><div class="line">Host: example.com</div></pre></td></tr></table></figure><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">GET /bar HTTP/1.1</div><div class="line">Host: foo-service.com</div></pre></td></tr></table></figure><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">GET /foo/hello/world HTTP/1.1</div><div class="line">Host: example.com</div></pre></td></tr></table></figure><p>所有这三个请求都满足路径定义中设置的所有条件。但是，以下请求与配置的条件不匹配：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">GET / HTTP/1.1</div><div class="line">Host: example.com</div></pre></td></tr></table></figure></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">POST /foo HTTP/1.1</div><div class="line">Host: example.com</div></pre></td></tr></table></figure><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">GET /foo HTTP/1.1</div><div class="line">Host: foo.com</div></pre></td></tr></table></figure><p>所有这三个请求仅满足两个配置条件。第一个请求的路径不匹配配置的路径，第二个请求的HTTP方法和第三个请求的host头也均不匹配。</p><p>现在我们了解了<code>hosts</code>, <code>paths</code>, 和 <code>methods</code>属性如何协同工作，让我们分别来看每个属性。</p><h3 id="host-请求头"><a href="#host-请求头" class="headerlink" title="host 请求头"></a>host 请求头</h3><p>基于其host header 来路由请求是通过Kong代理流量的最直接方式，特别是因为这是HTTP host header 的预期用途。Kong可以通过Route实体的<code>hosts</code>字段轻松完成。</p><p><code>hosts</code>接受多个值，在通过Admin API指定它们时必须以逗号分隔：</p><p><code>hosts</code>接受多个值，这些值很容易在JSON有效负载中表示：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">curl -i -X POST http://localhost:8001/routes/ \</div><div class="line">    -H &apos;Content-Type: application/json&apos; \</div><div class="line">    -d &apos;&#123;&quot;hosts&quot;:[&quot;example.com&quot;, &quot;foo-service.com&quot;]&#125;&apos;</div><div class="line">HTTP/1.1 201 Created</div><div class="line">...</div></pre></td></tr></table></figure></p><p>但由于Admin API还支持form-urlencoded内容类型，因此您可以通过<code>[]</code>表示法指定数组：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">curl -i -X POST http://localhost:8001/routes/ \</div><div class="line">    -d &apos;hosts[]=example.com&apos; \</div><div class="line">    -d &apos;hosts[]=foo-service.com&apos;</div><div class="line">HTTP/1.1 201 Created</div><div class="line">...</div></pre></td></tr></table></figure><p>要满足此Route的<code>hosts</code>条件，来自客户端的任何传入请求现在必须将其Host header 设置为以下之一：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">Host: example.com</div></pre></td></tr></table></figure></p><p>或者</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">Host: foo-service.com</div></pre></td></tr></table></figure><h4 id="使用通配符主机名"><a href="#使用通配符主机名" class="headerlink" title="使用通配符主机名"></a>使用通配符主机名</h4><p>为了提供灵活性，Kong允许您在<code>hosts</code>字段中指定带通配符的主机名。通配符主机名允许任何匹配的host满足条件，从而匹配给定的Route。</p><p>通配符主机名<strong>必须</strong>在域的最左侧或最右侧标签中<strong>仅包含</strong>一个星号。例子：</p><ul><li><code>*.example.com</code>将匹配诸如<code>a.example.com</code> 和 <code>x.y.example.com</code></li><li><code>example.*</code> 将匹配诸如<code>example.com</code> 和 <code>example.org</code></li></ul><p>一个完整的例子如下所示：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">&#123;</div><div class="line">    &quot;hosts&quot;: [&quot;*.example.com&quot;, &quot;service.com&quot;]</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><p>将允许以下请求匹配此路由：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">GET / HTTP/1.1</div><div class="line">Host: an.example.com</div></pre></td></tr></table></figure></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">GET / HTTP/1.1</div><div class="line">Host: service.com</div></pre></td></tr></table></figure><h4 id="preserve-host属性"><a href="#preserve-host属性" class="headerlink" title="preserve_host属性"></a><code>preserve_host</code>属性</h4><p>代理时，Kong的默认行为是将上游请求的主机头设置为服务主机中指定的<code>host</code>。<code>preserve_host</code>字段接受一个布尔标志，指示Kong不要这样做。</p><p>例如，当preserve_host属性未更改且Route配置如下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">&#123;</div><div class="line">    &quot;hosts&quot;: [&quot;service.com&quot;],</div><div class="line">    &quot;service&quot;: &#123;</div><div class="line">        &quot;id&quot;: &quot;...&quot;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><p>client对Kong的可能请求可能是：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">GET / HTTP/1.1</div><div class="line">Host: service.com</div></pre></td></tr></table></figure></p><p>Kong将从Service的主机属性中提取Host头值，并将发送以下上游请求：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">GET / HTTP/1.1</div><div class="line">Host: &lt;my-service-host.com&gt;</div></pre></td></tr></table></figure></p><p>但是，通过使用<code>preserve_host=true</code>配置Route：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">&#123;</div><div class="line">    &quot;hosts&quot;: [&quot;service.com&quot;],</div><div class="line">    &quot;preserve_host&quot;: true,</div><div class="line">    &quot;service&quot;: &#123;</div><div class="line">        &quot;id&quot;: &quot;...&quot;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure><p>并假设来自客户的相同请求：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">GET / HTTP/1.1</div><div class="line">Host: service.com</div></pre></td></tr></table></figure><p>Kong将根据客户端请求保留Host，并将发送以下上游请求：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">GET / HTTP/1.1</div><div class="line">Host: service.com</div></pre></td></tr></table></figure><h3 id="请求路径"><a href="#请求路径" class="headerlink" title="请求路径"></a>请求路径</h3><p>路由匹配的另一种方式是通过请求路径。<br>要满足此路由条件，客户端请求的路径必须以<code>paths</code>属性的值之一为前缀。<br>例如，使用如下配置的Route：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">&#123;</div><div class="line">    &quot;paths&quot;: [&quot;/service&quot;, &quot;/hello/world&quot;]</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><p>以下请求将被匹配：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">GET /service HTTP/1.1</div><div class="line">Host: example.com</div></pre></td></tr></table></figure></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">GET /service/resource?param=value HTTP/1.1</div><div class="line">Host: example.com</div></pre></td></tr></table></figure><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">GET /hello/world/resource HTTP/1.1</div><div class="line">Host: anything.com</div></pre></td></tr></table></figure><p>对于这些请求中的每一个，Kong检测到其URL路径以路由的<code>paths</code>之一为前缀。默认情况下，Kong会在不更改URL路径的情况下代理上游请求。</p><p>使用路径前缀进行代理时，<strong>首先评估最长路径</strong>。这允许您定义两个具有两个路径的Routes：<code>/service</code>和<code>/service/resource</code>，并确保前者不会“遮蔽”后者。</p><h4 id="在路径中使用正则表达式"><a href="#在路径中使用正则表达式" class="headerlink" title="在路径中使用正则表达式"></a>在路径中使用正则表达式</h4><p>Kong通过PCRE（Perl兼容正则表达式）支持Route的路径字段的正则表达式模式匹配。您可以同时将路径作为前缀和正则表达式分配给Route。例如，如果我们考虑以下Route：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">&#123;</div><div class="line">    &quot;paths&quot;: [&quot;/users/\d+/profile&quot;, &quot;/following&quot;]</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><p>此Route将匹配以下请求：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">GET /following HTTP/1.1</div><div class="line">Host: ...</div></pre></td></tr></table></figure></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">GET /users/123/profile HTTP/1.1</div><div class="line">Host: ...</div></pre></td></tr></table></figure><p>使用PCRE标志（<code>PCRE_ANCHORED</code>）评估提供的正则表达式，这意味着它们将被约束为在路径中的第一个匹配点（root<code>/</code>character）匹配。</p><h5 id="评估顺序"><a href="#评估顺序" class="headerlink" title="评估顺序"></a>评估顺序</h5><p>如前所述，Kong按长度评估前缀路径：首先评估最长前缀路径。<br>但是，Kong将根据路由的<code>regex_priority</code>属性从最高优先级到最低优先级来评估正则表达式路径。这意味着考虑以下Routes：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div></pre></td><td class="code"><pre><div class="line">[</div><div class="line">    &#123;</div><div class="line">        &quot;paths&quot;: [&quot;/status/\d+&quot;],</div><div class="line">        &quot;regex_priority&quot;: 0</div><div class="line">    &#125;,</div><div class="line">    &#123;</div><div class="line">        &quot;paths&quot;: [&quot;/version/\d+/status/\d+&quot;],</div><div class="line">        &quot;regex_priority&quot;: 6</div><div class="line">    &#125;,</div><div class="line">    &#123;</div><div class="line">        &quot;paths&quot;: [&quot;/version&quot;],</div><div class="line">    &#125;,</div><div class="line">    &#123;</div><div class="line">        &quot;paths&quot;: [&quot;/version/any/&quot;],</div><div class="line">    &#125;</div><div class="line">]</div></pre></td></tr></table></figure></p><p>在这种情况下，Kong将按以下顺序评估针对以下定义的URI的传入请求：</p><ol><li><code>/version/any/</code></li><li><code>/version</code></li><li><code>/version/\d+/status/\d+</code></li><li><code>/status/\d+</code></li></ol><p>始终在正则表达式路径之前评估前缀路径。</p><p>像往常一样，请求仍然必须匹配Route的机<code>hosts</code>和<code>methods</code>属性，并且Kong将遍历您的Routes，直到找到匹配最多规则的路由（请参阅[路由优先级] [代理路由优先级]）。</p><h5 id="捕获组"><a href="#捕获组" class="headerlink" title="捕获组"></a>捕获组</h5><p>也支持正则的捕获组，匹配的组将从路径中提取并可用于插件使用。<br>如果我们考虑以下正则表达式：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">/version/(?&lt;version&gt;\d+)/users/(?&lt;user&gt;\S+)</div></pre></td></tr></table></figure></p><p>以及以下请求路径：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">/version/1/users/john</div></pre></td></tr></table></figure></p><p>Kong会将请求路径视为匹配，如果匹配整个Route（考虑<code>hosts</code>和<code>methods</code>字段），则可以从<code>ngx.ctx</code>变量中的插件获取提取的捕获组：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">local router_matches = ngx.ctx.router_matches</div><div class="line"></div><div class="line">-- router_matches.uri_captures is:</div><div class="line">-- &#123; &quot;1&quot;, &quot;john&quot;, version = &quot;1&quot;, user = &quot;john&quot; &#125;</div></pre></td></tr></table></figure><h5 id="规避特殊字符"><a href="#规避特殊字符" class="headerlink" title="规避特殊字符"></a>规避特殊字符</h5><p>接下来，值得注意的是，根据<a href="https://tools.ietf.org/html/rfc3986" target="_blank" rel="external">RFC 3986</a>，在正则表达式中找到的字符通常是保留字符，因此应该是百分比编码（URL编码）。<strong>通过Admin API配置具有正则表达式路径的路由时，请务必在必要时对您的有效负载进行URL编码</strong>。例如，使用<code>curl</code>并使用<code>application/x-www-form-urlencode</code>MIME类型：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">curl -i -X POST http://localhost:8001/routes \</div><div class="line">    --data-urlencode &apos;uris[]=/status/\d+&apos;</div><div class="line">HTTP/1.1 201 Created</div><div class="line">...</div></pre></td></tr></table></figure></p><p>请注意，<code>curl</code>不会自动对您的有效负载进行URL编码，并注意使用<code>--data-urlencode</code>，它可以防止Kong的Admin API对<code>+</code>字符进行URL解码，并将其解码为一个空的``。</p><h4 id="strip-path属性"><a href="#strip-path属性" class="headerlink" title="strip_path属性"></a><code>strip_path</code>属性</h4><p>可能需要指定路径前缀以匹配路由，但不将其包括在上游请求中。为此，请通过配置如下所示的Route来使用<code>strip_path</code>布尔属性：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">&#123;</div><div class="line">    &quot;paths&quot;: [&quot;/service&quot;],</div><div class="line">    &quot;strip_path&quot;: true,</div><div class="line">    &quot;service&quot;: &#123;</div><div class="line">        &quot;id&quot;: &quot;...&quot;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><p>启用此标志会指示Kong在匹配此路由并继续代理服务时，不应在上游请求的URL中包含URL路径的匹配部分。例如，以下客户端对上述路由的请求：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">GET /service/path/to/resource HTTP/1.1</div><div class="line">Host: ...</div></pre></td></tr></table></figure></p><p>将导致Kong发送以下上游请求：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">GET /path/to/resource HTTP/1.1</div><div class="line">Host: ...</div></pre></td></tr></table></figure></p><p>同样，如果在启用了<code>strip_path</code>的Route上定义了正则表达式路径，则将剥离整个请求URL匹配序列。<br>例：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div></pre></td><td class="code"><pre><div class="line">&#123;</div><div class="line">    &quot;paths&quot;: [&quot;/version/\d+/service&quot;],</div><div class="line">    &quot;strip_path&quot;: true,</div><div class="line">    &quot;service&quot;: &#123;</div><div class="line">        &quot;id&quot;: &quot;...&quot;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><p>以下HTTP请求与提供的正则表达式路径匹配：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">GET /version/1/service/path/to/resource HTTP/1.1</div><div class="line">Host: ...</div></pre></td></tr></table></figure><h3 id="请求HTTP方法"><a href="#请求HTTP方法" class="headerlink" title="请求HTTP方法"></a>请求HTTP方法</h3><p><code>methods</code>字段允许根据HTTP方法匹配请求。它接受多个值。其默认值为空（HTTP方法不用于路由）。<br>以下路由允许通过<code>GET</code>和<code>HEAD</code>进行路由：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">&#123;</div><div class="line">    &quot;methods&quot;: [&quot;GET&quot;, &quot;HEAD&quot;],</div><div class="line">    &quot;service&quot;: &#123;</div><div class="line">        &quot;id&quot;: &quot;...&quot;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><p>这样的Route将符合以下要求：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">GET / HTTP/1.1</div><div class="line">Host: ...</div></pre></td></tr></table></figure></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">HEAD /resource HTTP/1.1</div><div class="line">Host: ...</div></pre></td></tr></table></figure><p>但它与<code>POST</code>或<code>DELETE</code>请求不匹配。在路由上配置插件时，这允许更多粒度。例如，可以想象两个指向同一服务的路由：一个具有无限制的未经身份验证的<code>GET</code>请求，另一个仅允许经过身份验证和速率限制的<code>POST</code>请求（通过将身份验证和速率限制插件应用于此类请求）。</p><h2 id="匹配优先事项"><a href="#匹配优先事项" class="headerlink" title="匹配优先事项"></a>匹配优先事项</h2><p>Route可以基于其<code>hosts</code>, <code>paths</code>, 和 <code>methods</code>字段定义匹配规则。要使Kong匹配到路由的传入请求，必须满足所有现有字段。<br>但是，通过允许两个或多个路由配置包含相同值的字段，Kong允许相当大的灵活性 - 当发生这种情况时，Kong应用优先级规则。</p><p>规则是：<strong>在评估请求时，Kong将首先尝试匹配具有最多规则的路由。</strong></p><p>例如，如果两个路由配置如下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line">&#123;</div><div class="line">    &quot;hosts&quot;: [&quot;example.com&quot;],</div><div class="line">    &quot;service&quot;: &#123;</div><div class="line">        &quot;id&quot;: &quot;...&quot;</div><div class="line">    &#125;</div><div class="line">&#125;,</div><div class="line">&#123;</div><div class="line">    &quot;hosts&quot;: [&quot;example.com&quot;],</div><div class="line">    &quot;methods&quot;: [&quot;POST&quot;],</div><div class="line">    &quot;service&quot;: &#123;</div><div class="line">        &quot;id&quot;: &quot;...&quot;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><p>第二个Route有一个<code>hosts</code>字段和一个<code>methods</code>字段，因此它将首先由Kong评估。通过这样做，我们避免了第一个用于第二个路径的“阴影”调用。</p><p>因此，此请求将匹配第一个Route</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">GET / HTTP/1.1</div><div class="line">Host: example.com</div></pre></td></tr></table></figure><p>这个请求将匹配第二个：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">POST / HTTP/1.1</div><div class="line">Host: example.com</div></pre></td></tr></table></figure><p>遵循这个逻辑，如果要使用<code>hosts</code>字段，<code>methods</code>字段和<code>uris</code>字段配置第三个Route，它将首先由Kong评估。</p><h2 id="代理行为"><a href="#代理行为" class="headerlink" title="代理行为"></a>代理行为</h2><p>上面的代理规则详细说明了Kong如何将传入请求转发到您的上游服务。下面，我们详细说明Kong与HTTP请求与注册路由<em>匹配</em>的时间与请求的实际<em>转发</em>之间内部发生的情况。</p><h3 id="1-负载均衡"><a href="#1-负载均衡" class="headerlink" title="1.负载均衡"></a>1.负载均衡</h3><p>Kong实现负载平衡功能，以跨上游服务实例池分发代理请求。</p><p>您可以通过查看<a href="https://docs.konghq.com/1.1.x/loadbalancing" target="_blank" rel="external">负载平衡</a>来查找有关配置负载平衡的更多信息</p><h3 id="2-插件执行"><a href="#2-插件执行" class="headerlink" title="2.插件执行"></a>2.插件执行</h3><p>Kong可通过“插件”进行扩展，这些“插件”将自己挂载在代理请求的请求/响应生命周期中。插件可以在您的环境中执行各种操作 和/或 在代理请求上进行转换。</p><p>可以将插件配置为全局（针对所有代理流量）或特定 Routes 和 Services运行。<br>在这两种情况下，您都必须通过Admin API创建<a href="https://docs.konghq.com/1.1.x/admin-api#plugin-object" target="_blank" rel="external">插件配置</a>。</p><p>一旦路由匹配（及其关联的服务实体），Kong将运行与这些实体中的任何一个相关联的插件。在路由上配置的插件在服务上配置的插件之前运行，否则，通常的<a href="https://docs.konghq.com/1.1.x/admin-api/#precedence" target="_blank" rel="external">插件关联</a>规则适用。</p><p>这些配置的插件将运行其<code>access</code>阶段，您可以在<a href="https://docs.konghq.com/1.1.x/plugin-development" target="_blank" rel="external">插件开发指南</a>中找到更多相关信息。</p><h3 id="3-代理和上游超时"><a href="#3-代理和上游超时" class="headerlink" title="3.代理和上游超时"></a>3.代理和上游超时</h3><p>一旦Kong执行了所有必要的逻辑（包括插件），它就可以将请求转发给您的上游服务。这是通过Nginx的<a href="http://nginx.org/en/docs/http/ngx_http_proxy_module.html" target="_blank" rel="external">ngx_http_proxy_module</a>完成的。<br>您可以通过以下服务属性为Kong和给定上游之间的连接配置所需的超时：</p><ul><li><code>upstream_connect_timeout</code>:以毫秒为单位定义建立与上游服务的连接的超时。默认为<code>60000</code>。</li><li><code>upstream_send_timeout</code>:以毫秒为单位定义用于向上游服务发送请求的两个连续写入操作之间的超时。默认为<code>60000</code>。</li><li><code>upstream_read_timeout</code>:以毫秒为单位定义用于接收来自上游服务的请求的两个连续读取操作之间的超时。默认为<code>60000</code>。</li></ul><p>Kong将通过 HTTP/1.1 发送请求，并设置以下headers：</p><ul><li><code>Host: &lt;your_upstream_host&gt;</code>，如前文所述。</li><li><code>Connection: keep-alive</code>，允许重用上游连接。</li><li><code>X-Real-IP: &lt;remote_addr&gt;</code>，其中<code>$remote_addr是</code>与<a href="http://nginx.org/en/docs/http/ngx_http_core_module.html#var_remote_addr" target="_blank" rel="external">ngx_http_core_module</a>提供的名称相同的变量。请注意，<code>$remote_addr</code>可能被<a href="http://nginx.org/en/docs/http/ngx_http_realip_module.html" target="_blank" rel="external">ngx_http_realip_module</a>覆盖。</li><li><code>X-Forwarded-For: &lt;address&gt;</code>，其中<code>&lt;address&gt;</code>是由附加到具有相同名称的请求标头的<a href="http://nginx.org/en/docs/http/ngx_http_realip_module.html" target="_blank" rel="external">ngx_http_realip_module</a>提供的<code>$realip_remote_addr</code>的内容。</li><li><code>X-Forwarded-Proto: &lt;protocol&gt;</code>，其中<code>&lt;protocol&gt;</code>是客户端使用的协议。在<code>$realip_remote_addr</code>是可信地址之一的情况下，如果提供，则转发具有相同名称的请求头。否则，将使用<a href="http://nginx.org/en/docs/http/ngx_http_core_module.html#var_scheme" target="_blank" rel="external">ngx_http_core_module</a>提供的<code>$scheme</code>变量的值。</li><li><code>X-Forwarded-Host: &lt;host&gt;</code>，其中<code>&lt;host&gt;</code>是客户端发送的主机名。在<code>$realip_remote_addr</code>是可信地址之一的情况下，如果提供，则转发具有相同名称的请求头。否则，将使用<a href="http://nginx.org/en/docs/http/ngx_http_core_module.html#var_host" target="_blank" rel="external">ngx_http_core_module</a>提供的<code>$host</code>变量的值。</li><li><code>X-Forwarded-Port: &lt;port&gt;</code>，其中<code>&lt;port&gt;</code>是接受请求的服务器的端口。在<code>$realip_remote_addr</code>是可信地址之一的情况下，如果提供，则转发具有相同名称的请求头。否则，将使用<a href="http://nginx.org/en/docs/http/ngx_http_core_module.html#var_server_port" target="_blank" rel="external">ngx_http_core_module</a>提供的<code>$server_port</code>变量的值。</li></ul><p>所有其他请求headers都由Kong转发。</p><p>使用WebSocket协议时会出现一个例外。如果是这样，Kong将设置以下标头以允许升级客户端和上游服务之间的协议：</p><ul><li><code>Connection: Upgrade</code></li><li><code>Upgrade: websocket</code></li></ul><p>有关此主题的更多信息，请参见[Proxy WebSocket流量] [proxy-websocket]部分。</p><h3 id="4-错误和重试"><a href="#4-错误和重试" class="headerlink" title="4.错误和重试"></a>4.错误和重试</h3><p>每当代理期间发生错误时，Kong将使用底层的Nginx<a href="http://nginx.org/en/docs/http/ngx_http_proxy_module.html#proxy_next_upstream_tries" target="_blank" rel="external">重试机制</a>将请求传递给下一个上游。</p><p>这里有两个可配置元素：</p><ol><li>重试次数：可以使用<code>retries</code>属性为每个服务配置。有关详细信息，请参阅<a href="https://docs.konghq.com/1.1.x/admin-api" target="_blank" rel="external">Admin API</a>。</li><li>究竟是什么构成错误：这里Kong使用Nginx默认值，这意味着在与服务器建立连接，向其传递请求或读取响应头时发生错误或超时。</li></ol><p>第二个选项基于Nginx的<code>proxy_next_upstream</code>指令。此选项不能通过Kong直接配置，但可以使用自定义Nginx配置添加。有关详细信息，请参阅配置参考。</p><h3 id="5-响应"><a href="#5-响应" class="headerlink" title="5.响应"></a>5.响应</h3><p>Kong接收来自上游服务的响应，并以流方式将其发送回下游客户端。此时，Kong将执行添加到 Route 和/或 Service 的后续插件，这些插件在<code>header_filter</code>阶段实现一个钩子。</p><p>一旦执行了所有已注册插件的<code>header_filter</code>阶段，Kong将添加以下headers，并将完整的headers发送到客户端：</p><ul><li><code>Via: kong/x.x.x</code>，其中<code>x.x.x</code>是正在使用的Kong版本。</li><li><code>X-Kong-Proxy-Latency: &lt;latency&gt;</code>，其中<code>latency</code>是Kong收到客户端请求和向上游服务发送请求之间的时间（以毫秒为单位）。</li><li><code>X-Kong-Upstream-Latency: &lt;latency&gt;</code>，其中<code>latency</code>是Kong等待上游服务响应的第一个字节的时间（以毫秒为单位）。</li></ul><p>将标题发送到客户端后，Kong将开始为实现<code>body_filter</code>钩子的 Route和/或Service 执行已注册的插件。由于Nginx的流媒体特性，可以多次调用此钩子。由这样的<code>body_filter</code>挂钩成功处理的上游响应的每个块被发送回客户端。您可以在<a href="https://docs.konghq.com/1.1.x/plugin-development" target="_blank" rel="external">插件开发指南</a>中找到有关<code>body_filter</code>钩子的更多信息。</p><h2 id="配置一个备用路由"><a href="#配置一个备用路由" class="headerlink" title="配置一个备用路由"></a>配置一个备用路由</h2><p>作为Kong的代理功能提供的灵活性的实际用例和示例，让我们尝试实现“后备路线”，因此，为了避免Kong响应HTTP <code>404</code>，“找不到路由”，我们可以捕获这些请求并将它们代理到特殊的上游服务，或者向它应用插件（例如，这样的插件可以使用不同的状态代码或响应终止请求，而不代理请求）。</p><p>以下是此类后备路由的示例：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">&#123;</div><div class="line">    &quot;paths&quot;: [&quot;/&quot;],</div><div class="line">    &quot;service&quot;: &#123;</div><div class="line">        &quot;id&quot;: &quot;...&quot;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><p>正如您所猜测的，任何向Kong发出的HTTP请求实际上都会匹配此Route，因为所有URI都以根字符<code>/</code>为前缀。正如我们从[请求路径] [代理请求路径]部分所知，最长的URL路径首先由Kong评估，因此<code>/</code>路径最终将由Kong最后评估，并有效地提供“后备”路由，仅作为最后的手段。</p><h2 id="为路由配置SSL"><a href="#为路由配置SSL" class="headerlink" title="为路由配置SSL"></a>为路由配置SSL</h2><p>Kong提供了一种基于每个连接动态提供SSL证书的方法。SSL证书由核心直接处理，并可通过Admin API进行配置。通过TLS连接到Kong的客户端必须支持服务器名称指示扩展才能使用此功能。</p><p>SSL证书由Kong Admin API中的两个资源处理：</p><ul><li><code>/certificates</code>，存储您的密钥和证书。</li><li><code>/snis</code>，将注册证书与Server Name 指示相关联。</li></ul><p>您可以在<a href="https://docs.konghq.com/1.1.x/admin-api" target="_blank" rel="external">Admin API参考</a>中找到这两种资源的文档。</p><p>以下是在给定路由上配置SSL证书的方法：首先，通过Admin API上传您的SSL证书和密钥：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">curl -i -X POST http://localhost:8001/certificates \</div><div class="line">    -F &quot;cert=@/path/to/cert.pem&quot; \</div><div class="line">    -F &quot;key=@/path/to/cert.key&quot; \</div><div class="line">    -F &quot;snis=ssl-example.com,other-ssl-example.com&quot;</div><div class="line">HTTP/1.1 201 Created</div><div class="line">...</div></pre></td></tr></table></figure><p><code>snis</code>表单参数是糖参数，直接插入SNI并将上传的证书与其关联。</p><p>您现在必须在Kong内注册以下Route。<br>为方便起见，我们仅使用Hos header 匹配对此Route的请求：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">curl -i -X POST http://localhost:8001/routes \</div><div class="line">    -d &apos;hosts=ssl-example.com,other-ssl-example.com&apos; \</div><div class="line">    -d &apos;service.id=d54da06c-d69f-4910-8896-915c63c270cd&apos;</div><div class="line">HTTP/1.1 201 Created</div><div class="line">...</div></pre></td></tr></table></figure></p><p>您现在可以期望Kong通过HTTPS提供路由：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">curl -i https://localhost:8443/ \</div><div class="line">  -H &quot;Host: ssl-example.com&quot;</div><div class="line">HTTP/1.1 200 OK</div><div class="line">...</div></pre></td></tr></table></figure></p><p>建立连接并协商SSL握手时，如果您的客户端发送<code>ssl-example.com</code>作为SNI扩展的一部分，Kong将提供先前配置的<code>cert.pem</code>证书。</p><h3 id="限制客户端协议（HTTP-HTTPS-TCP-TLS）"><a href="#限制客户端协议（HTTP-HTTPS-TCP-TLS）" class="headerlink" title="限制客户端协议（HTTP/HTTPS/TCP/TLS）"></a>限制客户端协议（HTTP/HTTPS/TCP/TLS）</h3><p>路由具有<code>protocols</code>属性，以限制他们应该侦听的客户端协议。此属性接受一组值，可以是<code>“http”</code>，<code>“https”</code>，<code>“tcp”</code>或<code>“tls”</code>。</p><p>具有<code>http</code>和<code>https</code>的路由将接受两种协议中的流量。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line">&#123;</div><div class="line">    &quot;hosts&quot;: [&quot;...&quot;],</div><div class="line">    &quot;paths&quot;: [&quot;...&quot;],</div><div class="line">    &quot;methods&quot;: [&quot;...&quot;],</div><div class="line">    &quot;protocols&quot;: [&quot;http&quot;, &quot;https&quot;],</div><div class="line">    &quot;service&quot;: &#123;</div><div class="line">        &quot;id&quot;: &quot;...&quot;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><p>未指定任何协议具有相同的效果，因为路由默认为<code>[“http”，“https”]</code>。</p><p>但是，仅使用<code>https</code>的路由<em>只</em>接受通过HTTPS的流量。如果以前从受信任的IP发生SSL终止，它也会接受未加密的流量。当请求来自<a href="https://docs.konghq.com/1.1.x/configuration/#trusted_ips" target="_blank" rel="external">trusted_ip</a>中的一个配置的IP并且如果设置了<code>X-Forwarded-Proto:https</code> header时，SSL终止被认为是有效的：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line">&#123;</div><div class="line">    &quot;hosts&quot;: [&quot;...&quot;],</div><div class="line">    &quot;paths&quot;: [&quot;...&quot;],</div><div class="line">    &quot;methods&quot;: [&quot;...&quot;],</div><div class="line">    &quot;protocols&quot;: [&quot;https&quot;],</div><div class="line">    &quot;service&quot;: &#123;</div><div class="line">        &quot;id&quot;: &quot;...&quot;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><p>如果上述路由与请求匹配，但该请求是纯文本而没有有效的先前SSL终止，则Kong响应：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">HTTP/1.1 426 Upgrade Required</div><div class="line">Content-Type: application/json; charset=utf-8</div><div class="line">Transfer-Encoding: chunked</div><div class="line">Connection: Upgrade</div><div class="line">Upgrade: TLS/1.2, HTTP/1.1</div><div class="line">Server: kong/x.y.z</div><div class="line"></div><div class="line">&#123;&quot;message&quot;:&quot;Please use HTTPS protocol&quot;&#125;</div></pre></td></tr></table></figure><p>从Kong 1.0开始，可以使用<code>protocols</code>属性中的<code>“tcp”</code>为原始TCP（不一定是HTTP）连接创建路由：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line">&#123;</div><div class="line">    &quot;hosts&quot;: [&quot;...&quot;],</div><div class="line">    &quot;paths&quot;: [&quot;...&quot;],</div><div class="line">    &quot;methods&quot;: [&quot;...&quot;],</div><div class="line">    &quot;protocols&quot;: [&quot;tcp&quot;],</div><div class="line">    &quot;service&quot;: &#123;</div><div class="line">        &quot;id&quot;: &quot;...&quot;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><p>同样，我们可以使用<code>“tls”</code>值创建接受原始TLS流量（不一定是HTTPS）的路由：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line">&#123;</div><div class="line">    &quot;hosts&quot;: [&quot;...&quot;],</div><div class="line">    &quot;paths&quot;: [&quot;...&quot;],</div><div class="line">    &quot;methods&quot;: [&quot;...&quot;],</div><div class="line">    &quot;protocols&quot;: [&quot;tls&quot;],</div><div class="line">    &quot;service&quot;: &#123;</div><div class="line">        &quot;id&quot;: &quot;...&quot;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><p>仅具有<code>TLS</code>的路由仅接受通过TLS的流量。<br>也可以同时接受TCP和TLS：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line">&#123;</div><div class="line">    &quot;hosts&quot;: [&quot;...&quot;],</div><div class="line">    &quot;paths&quot;: [&quot;...&quot;],</div><div class="line">    &quot;methods&quot;: [&quot;...&quot;],</div><div class="line">    &quot;protocols&quot;: [&quot;tcp&quot;, &quot;tls&quot;],</div><div class="line">    &quot;service&quot;: &#123;</div><div class="line">        &quot;id&quot;: &quot;...&quot;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><h2 id="代理WebSocket流量"><a href="#代理WebSocket流量" class="headerlink" title="代理WebSocket流量"></a>代理WebSocket流量</h2><p>由于底层的Nginx实现，Kong支持WebSocket流量。如果希望通过Kong在客户端和上游服务之间建立WebSocket连接，则必须建立WebSocket握手。这是通过HTTP升级机制完成的。这是您的客户要求对Kong的看法：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">GET / HTTP/1.1</div><div class="line">Connection: Upgrade</div><div class="line">Host: my-websocket-api.com</div><div class="line">Upgrade: WebSocket</div></pre></td></tr></table></figure></p><p>这将使Kong将<code>Connection</code>和<code>Upgrade</code> header 转发到您的上游服务，而不是由于标准HTTP代理的逐跳特性而将其解除。</p><h3 id="WebSocket和TLS"><a href="#WebSocket和TLS" class="headerlink" title="WebSocket和TLS"></a>WebSocket和TLS</h3><p>Kong将在其各自的<code>http</code>和<code>https</code>端口接受<code>ws</code>和<code>wss</code>连接。要从客户端强制执行TLS连接，请将Route的<code>protocols</code>属性设置为<strong>仅</strong><code>https</code>。</p><p>将 Service 设置为指向上游WebSocket服务时，应仔细选择要在Kong和上游之间使用的协议。如果要使用TLS（<code>wss</code>），则必须使用服务<code>protocol</code>属性中的<code>https</code>协议和正确的端口（通常为443）定义上游WebSocket服务。要在没有TLS（<code>ws</code>）的情况下进行连接，则应在协议中使用<code>http</code>协议和端口（通常为80）。</p><p>如果您希望Kong终止SSL/TLS，您只能从客户端接受<code>wss</code>，而是通过纯文本或<code>ws</code>代理上游服务。</p><h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><p>通过本指南，我们希望您了解Kong的基础代理机制，从请求如何匹配路由到其关联的服务，到如何允许使用WebSocket协议或设置动态SSL证书。</p><p>该网站是开源的，可以在 <a href="https://github.com/Kong/docs.konghq.com" target="_blank" rel="external">https://github.com/Kong/docs.konghq.com</a> 找到。<br>如果您还没有，我们建议您还阅读<a href="https://docs.konghq.com/1.1.x/loadbalancing" target="_blank" rel="external">负载平衡参考</a>，因为它与我们刚刚介绍的主题密切相关。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;原文链接: &lt;a href=&quot;https://docs.konghq.com/1.1.x/proxy/&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;https://docs.konghq.com/1.1.x/proxy/&lt;/a&gt; （不能保证所有的翻译都是
      
    
    </summary>
    
      <category term="与代码" scheme="http://git.102no.com/categories/%E4%B8%8E%E4%BB%A3%E7%A0%81/"/>
    
    
      <category term="Kong" scheme="http://git.102no.com/tags/Kong/"/>
    
      <category term="Kong教程" scheme="http://git.102no.com/tags/Kong%E6%95%99%E7%A8%8B/"/>
    
      <category term="Kong中文文档" scheme="http://git.102no.com/tags/Kong%E4%B8%AD%E6%96%87%E6%96%87%E6%A1%A3/"/>
    
  </entry>
  
  <entry>
    <title>Lua OpenResty 使用rabbitmq AMQP协议发送和接收消息</title>
    <link href="http://git.102no.com/2019/05/30/lua-openresty-rabbitmq/"/>
    <id>http://git.102no.com/2019/05/30/lua-openresty-rabbitmq/</id>
    <published>2019-05-29T16:00:00.000Z</published>
    <updated>2019-05-31T16:50:50.052Z</updated>
    
    <content type="html"><![CDATA[<p>如果想要在openresty中使用AMQP协议发送和接收消息的话，需要使用到一个第三方库。地址为：<a href="https://github.com/mengz0/amqp" target="_blank" rel="external">https://github.com/mengz0/amqp</a> 。<br>当然，首先你得在自己的环境中安装一个rabbitmq，或者使用远程的rabbitmq也可以。<br>openresty也是要安装一下的，这里就不啰嗦了。</p><h2 id="第一步：安装第三方库"><a href="#第一步：安装第三方库" class="headerlink" title="第一步：安装第三方库"></a>第一步：安装第三方库</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">luarocks install amqp</div></pre></td></tr></table></figure><p>如果没有安装 luarocks 可以安装一下luarocks。</p><h2 id="第二步：使用amqp发送-amp-接收消息"><a href="#第二步：使用amqp发送-amp-接收消息" class="headerlink" title="第二步：使用amqp发送&amp;接收消息"></a>第二步：使用amqp发送&amp;接收消息</h2><p>可以直接参考<a href="https://github.com/mengz0/amqp" target="_blank" rel="external">https://github.com/mengz0/amqp</a> 中的例子，但是这个库的例子写的非常简略，导致很多参数需要你自己去源码中查看，我在这个例子中列举了一些常用的参数，可以参考使用一下。</p><h3 id="发送消息"><a href="#发送消息" class="headerlink" title="发送消息"></a>发送消息</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div></pre></td><td class="code"><pre><div class="line">local function send_pb(messages)</div><div class="line">local amqp        = require &quot;amqp&quot;</div><div class="line"></div><div class="line">local ctx = amqp.new(&#123;</div><div class="line">        role        = &quot;publisher&quot;,</div><div class="line">        exchange    = &quot;exchangexxxx&quot;,</div><div class="line">        ssl         = false,</div><div class="line">        user        = &quot;guest&quot;,</div><div class="line">        password    = &quot;guest&quot;,</div><div class="line">        auto_delete = false,</div><div class="line">        routing_key = &quot;routing_keyxxxxx&quot;,</div><div class="line">        passive     = true,</div><div class="line">        no_ack      = true,</div><div class="line">        no_wait     = false,</div><div class="line">    &#125;)</div><div class="line">    ctx:connect(&quot;127.0.0.1&quot;, port)</div><div class="line">    ctx:setup()</div><div class="line">    local ok, err = ctx:publish(messages)</div><div class="line">    if not ok then</div><div class="line">        ngx.log(ngx.ERR, &quot;[ -- rabbitmq send failed : -- ] &quot; .. err)</div><div class="line">    else</div><div class="line">        ngx.log(ngx.ERR, &quot;[ -- rabbitmq send success ]&quot;)</div><div class="line">    end</div><div class="line">end</div><div class="line"></div><div class="line">send_pb(&quot;this is a message&quot;)</div></pre></td></tr></table></figure><h3 id="接收消息-amp-消费队列"><a href="#接收消息-amp-消费队列" class="headerlink" title="接收消息&amp;消费队列"></a>接收消息&amp;消费队列</h3><p>可以新建一个<code>consume_queue.lua</code>文件，然后如下代码：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div></pre></td><td class="code"><pre><div class="line">local function consume_local(body)</div><div class="line">print(body) -- 这里就是消息的主体</div><div class="line">end</div><div class="line"></div><div class="line">local amqp        = require &quot;amqp&quot;</div><div class="line"></div><div class="line">local ctx = amqp.new(&#123;</div><div class="line">    role        = &quot;consumer&quot;,</div><div class="line">    queue       = &quot;eventbus1&quot;, -- 这里可以自定义</div><div class="line">    exchange    = &quot;exchangexxxx&quot;,</div><div class="line">    ssl         = false,</div><div class="line">    user        = &quot;guest&quot;,</div><div class="line">    password    = &quot;guest&quot;,</div><div class="line">    no_wait     = false,</div><div class="line">    routing_key = &quot;routing_keyxxxxx&quot;,</div><div class="line">    auto_delete = false, -- 是否自动删除消息</div><div class="line">    no_ack      = true,</div><div class="line">    exclusive   = false, -- 是否为排他队列</div><div class="line">    callback    = consume_local,</div><div class="line">    durable     = true,</div><div class="line">    passive     = false,</div><div class="line">    type        = &quot;topic&quot;</div><div class="line"></div><div class="line">&#125;)</div><div class="line"></div><div class="line">ctx:connect(&quot;127.0.0.1&quot;, port)</div><div class="line"></div><div class="line">local ok, err = ctx:consume()</div></pre></td></tr></table></figure></p><p>然后使用命令<code>/usr/local/openresty/bin/resty  consume_queue.lua</code>，就可以看到发送的消息了。</p><h3 id="发现问题"><a href="#发现问题" class="headerlink" title="发现问题"></a>发现问题</h3><p>在使用这个库的时候，发现了一点小问题，就是在消费这个队列的时候，发现诸如<code>passive</code>、<code>auto_delete</code>、<code>durable</code>等等参数设置的全都无效了，但是感觉自己的使用姿势没有问题啊，按照 <a href="https://github.com/mengz0/amqp#typical-use-cases" target="_blank" rel="external">https://github.com/mengz0/amqp#typical-use-cases</a> 给的例子来也没问题。随后我就查看并断点了一下源码的<a href="https://github.com/mengz0/amqp/blob/master/amqp.lua" target="_blank" rel="external">https://github.com/mengz0/amqp/blob/master/amqp.lua</a> 文件，发现在执行到 664行的<code>function amqp:queue_declare(opts)</code> 函数和 687行的<code>function amqp:queue_bind(opts)</code>函数的时候，<code>opts</code>参数根本没有传进来。</p><h3 id="解决问题"><a href="#解决问题" class="headerlink" title="解决问题"></a>解决问题</h3><p>所以要把<code>function amqp:queue_declare(opts)</code>函数中的<code>opts.</code>改为<code>self.opts.</code>，如下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">f.method = &#123;</div><div class="line">      queue = opts.queue or self.opts.queue,</div><div class="line">      passive = opts.passive or false,</div><div class="line">      durable = opts.durable or false,</div><div class="line">      exclusive = opts.exclusive or false,</div><div class="line">      auto_delete = opts.auto_delete or true,</div><div class="line">      no_wait = self.opts.no_wait or true</div><div class="line">   &#125;</div></pre></td></tr></table></figure></p><p>修改为</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">f.method = &#123;</div><div class="line">      queue = opts.queue or self.opts.queue,</div><div class="line">      passive = self.opts.passive or false,</div><div class="line">      durable = self.opts.durable or false,</div><div class="line">      exclusive = self.opts.exclusive or false,</div><div class="line">      auto_delete = self.opts.auto_delete or true,</div><div class="line">      no_wait = self.opts.no_wait or true</div><div class="line">   &#125;</div></pre></td></tr></table></figure><p><code>function amqp:queue_bind(opts)</code>函数也同理，这里就不再写一次了，修改完了之后，传进去的参数都生效了。</p><p>打完收工。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;如果想要在openresty中使用AMQP协议发送和接收消息的话，需要使用到一个第三方库。地址为：&lt;a href=&quot;https://github.com/mengz0/amqp&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;https://github.c
      
    
    </summary>
    
      <category term="与代码" scheme="http://git.102no.com/categories/%E4%B8%8E%E4%BB%A3%E7%A0%81/"/>
    
    
      <category term="OpenResty" scheme="http://git.102no.com/tags/OpenResty/"/>
    
      <category term="Lua" scheme="http://git.102no.com/tags/Lua/"/>
    
      <category term="RabbitMQ" scheme="http://git.102no.com/tags/RabbitMQ/"/>
    
      <category term="消息队列" scheme="http://git.102no.com/tags/%E6%B6%88%E6%81%AF%E9%98%9F%E5%88%97/"/>
    
  </entry>
  
  <entry>
    <title>在 lua 中使用 protobuf</title>
    <link href="http://git.102no.com/2019/05/23/lua-protobuf/"/>
    <id>http://git.102no.com/2019/05/23/lua-protobuf/</id>
    <published>2019-05-22T16:00:00.000Z</published>
    <updated>2019-05-31T16:51:58.751Z</updated>
    
    <content type="html"><![CDATA[<h2 id="安装protobuf"><a href="#安装protobuf" class="headerlink" title="安装protobuf"></a>安装protobuf</h2><p>具体步骤可以参考官方文档<a href="https://github.com/protocolbuffers/protobuf/blob/master/src/README.md" target="_blank" rel="external">https://github.com/protocolbuffers/protobuf/blob/master/src/README.md</a> </p><p>这里只列出一些其中比较的重要的步骤。</p><p>先安装一些依赖的库<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">sudo apt-get install autoconf automake libtool curl make g++ unzip</div></pre></td></tr></table></figure></p><p>然后下载需要的安装包<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">curl -L -o protobuf-all-3.6.1.tar.gz  https://github.com/protocolbuffers/protobuf/releases/download/v3.6.1/protobuf-all-3.6.1.tar.gz</div></pre></td></tr></table></figure></p><p>接着解压<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">tar -xzvf protobuf-all-3.6.1.tar.gz</div></pre></td></tr></table></figure></p><p>解压完成后进目录<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">cd protobuf-all-3.6.1</div></pre></td></tr></table></figure></p><p>执行安装命令<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">./configure</div><div class="line">make</div><div class="line">make check</div><div class="line">sudo make install</div><div class="line">sudo ldconfig</div></pre></td></tr></table></figure></p><p>最后检查是否安装成功<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">$ protoc --version</div><div class="line">libprotoc 3.6.1</div></pre></td></tr></table></figure></p><h2 id="安装lua-protobuf"><a href="#安装lua-protobuf" class="headerlink" title="安装lua-protobuf"></a>安装lua-protobuf</h2><p><a href="https://zhuanlan.zhihu.com/p/26014103" target="_blank" rel="external">lua-protobuf</a> 实际上是一个纯C的protobuf协议实现，和对应的Lua绑定。</p><p>项目地址：<a href="https://github.com/starwing/lua-protobuf" target="_blank" rel="external">https://github.com/starwing/lua-protobuf</a></p><p>可以使用 luarocks 安装lua-protobuf<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">luarocks install lua-protobuf</div></pre></td></tr></table></figure></p><p>如果没有安装 luarocks 可以安装一下luarocks。</p><h2 id="使用prortobuf"><a href="#使用prortobuf" class="headerlink" title="使用prortobuf"></a>使用prortobuf</h2><p>首先，需要定义你自己的标准proto文件 xxx.proto 文件（可能不止一个文件，不止一个目录），定义好了之后，使用protoc生成pb文件，命令如下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">protoc --proto_path=proto --descriptor_set_out=common.pb proto/xxxx/*.proto proto/xxxxx/*.proto</div></pre></td></tr></table></figure></p><p>这里需要注意的是，有时候会有多个proto文件并且多目录import的情况，这个时候，就需要在参数中都体现出来（命令的最后两个参数），这条命令是把所有的 .proto 文件生成了一个 common.pb 文件方便引入。</p><p>最后就可以在lua代码中如下使用<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div></pre></td><td class="code"><pre><div class="line">-- 引入pb库</div><div class="line">local pb = require &quot;pb&quot;</div><div class="line"></div><div class="line">-- load pb文件</div><div class="line">assert(pb.loadfile &quot;common.pb&quot; )</div><div class="line"></div><div class="line">-- lua table data</div><div class="line">local data = &#123;</div><div class="line">    aaa = 123456,</div><div class="line">    bbb = &quot;bbbbb&quot;</div><div class="line">&#125;</div><div class="line"></div><div class="line">-- 把一个lua table数据encode成二进制文件</div><div class="line">local bytes = assert(pb.encode(&quot;dd01.account&quot;, data))</div><div class="line">print(pb.tohex(bytes))</div><div class="line"></div><div class="line">-- 把一个二进制文件decode为lua table</div><div class="line">local data2 = assert(pb.decode(&quot;dd01.account&quot;, bytes))</div><div class="line">print(cjson_encode(data2))</div></pre></td></tr></table></figure></p><p>如果能看到正常打印出来，说明就成功了。</p><h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><ul><li><a href="https://zhuanlan.zhihu.com/p/26014103" target="_blank" rel="external">lua-protobuf 使用说明</a></li><li><a href="https://zhuanlan.zhihu.com/p/33379595" target="_blank" rel="external">新的lua-protobuf发布啦</a></li><li><a href="https://blog.codingnow.com/2010/08/proto_buffers_in_lua.html" target="_blank" rel="external">Proto Buffers in Lua - 云风的blog</a></li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;安装protobuf&quot;&gt;&lt;a href=&quot;#安装protobuf&quot; class=&quot;headerlink&quot; title=&quot;安装protobuf&quot;&gt;&lt;/a&gt;安装protobuf&lt;/h2&gt;&lt;p&gt;具体步骤可以参考官方文档&lt;a href=&quot;https://github.co
      
    
    </summary>
    
      <category term="与代码" scheme="http://git.102no.com/categories/%E4%B8%8E%E4%BB%A3%E7%A0%81/"/>
    
    
      <category term="OpenResty" scheme="http://git.102no.com/tags/OpenResty/"/>
    
      <category term="Lua" scheme="http://git.102no.com/tags/Lua/"/>
    
      <category term="protocolbuffer" scheme="http://git.102no.com/tags/protocolbuffer/"/>
    
      <category term="lua-protobuf" scheme="http://git.102no.com/tags/lua-protobuf/"/>
    
  </entry>
  
  <entry>
    <title>微服务 API 网关 Kong 插件开发 - 文件结构</title>
    <link href="http://git.102no.com/2019/05/16/kong-plugin-file-structure/"/>
    <id>http://git.102no.com/2019/05/16/kong-plugin-file-structure/</id>
    <published>2019-05-15T16:00:00.000Z</published>
    <updated>2019-05-16T16:28:33.820Z</updated>
    
    <content type="html"><![CDATA[<p>原文地址：<a href="https://docs.konghq.com/1.1.x/plugin-development/file-structure/" target="_blank" rel="external">https://docs.konghq.com/1.1.x/plugin-development/file-structure/</a> （不能保证所有的翻译都是准确无误的，所有如有翻译的不准确或错误之处，请一定记得查看原文，并欢迎留言指出）。</p><blockquote><p>本章假定你已经会使用Lua语言</p></blockquote><h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><p>将您的插件视为一组<a href="http://www.lua.org/manual/5.1/manual.html#6.3" target="_blank" rel="external">Lua模块</a>。本章中描述的每个文件都被视为一个单独的模块。如果他们的名字遵循这个约定，Kong将检测并加载你的插件的模块：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">kong.plugins.&lt;plugin_name&gt;.&lt;module_name&gt;</div></pre></td></tr></table></figure><p><em>您的模块当然需要通过<a href="http://www.lua.org/manual/5.1/manual.html#pdf-package.path" target="_blank" rel="external">package.path</a>变量访问，可以通过<a href="https://docs.konghq.com/1.1.x/configuration/#development-miscellaneous-section" target="_blank" rel="external">lua_package_path</a>配置属性调整您的需求。但是，安装插件的首选方法是通过<a href="https://luarocks.org/" target="_blank" rel="external">LuaRocks</a>，它与Kong本身集成。有关LuaRocks安装的插件的更多信息，请参阅本指南后面的内容。</em></p><p>为了让Kong意识到必须查找插件的模块，你必须将它添加到配置文件中的plugins属性中，这是一个以逗号分隔的列表。例如<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">plugins = bundled,my-custom-plugin # 你的插件名称</div></pre></td></tr></table></figure></p><p>或者，如果您不想加载任何自带的插件：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">plugins = my-custom-plugin  # 你的插件名称</div></pre></td></tr></table></figure></p><p>现在，Kong将尝试从以下命名空间加载几个Lua模块：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">kong.plugins.my-custom-plugin.&lt;module_name&gt;</div></pre></td></tr></table></figure></p><p>其中一些模块是必需的（例如<code>handler.lua</code>），有些是可选的，并且允许插件实现一些额外的功能（例如<code>api.lua</code>以扩展Admin API）。现在让我们准确描述您可以实现的模块以及它们的用途。</p><h2 id="基本插件模块"><a href="#基本插件模块" class="headerlink" title="基本插件模块"></a>基本插件模块</h2><p>在最基本的形式中，插件包含两个必需的模块：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">simple-plugin</div><div class="line">├── handler.lua</div><div class="line">└── schema.lua</div></pre></td></tr></table></figure></p><ul><li>每个函数将在请求的生命周期中的所需时刻运行。</li><li>由用户。此模块保存该配置的模式并在其上定义规则，以便用户只能输入有效的配置值。</li></ul><h2 id="高级插件模块"><a href="#高级插件模块" class="headerlink" title="高级插件模块"></a>高级插件模块</h2><p>有些插件可能需要与Kong更深入地集成：在数据库中拥有自己的表，在Admin API中公开端点等等……每个插件都可以通过向插件添加新模块来完成。如果它实现了所有可选模块，那么插件的结构如下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">complete-plugin</div><div class="line">├── api.lua</div><div class="line">├── daos.lua</div><div class="line">├── handler.lua</div><div class="line">├── migrations</div><div class="line">│   ├── cassandra.lua</div><div class="line">│   └── postgres.lua</div><div class="line">└── schema.lua</div></pre></td></tr></table></figure></p><p>以下是要实施的可能模块的完整列表以及其目的的简要说明。<br>本指南将详细介绍，让您掌握其中的每一个文件。</p><table><thead><tr><th>模块文件名称</th><th>是否必须</th><th>描述</th></tr></thead><tbody><tr><td>api.lua</td><td>No</td><td>定义Admin API中可用的端点列表，以与插件处理的实体自定义实体进行交互。</td></tr><tr><td>daos.lua</td><td>No</td><td>定义DAO（数据库访问对象）列表，这些DAO是插件所需并存储在数据存储区中的自定义实体的抽象。</td></tr><tr><td>handler.lua</td><td>Yes</td><td>一个接口的实现。每个函数都由Kong在请求的生命周期中的所需时刻运行。</td></tr><tr><td>migrations/xxxx.lua</td><td>No</td><td>给定数据存储的相应迁移。只有当您的插件必须在数据库中存储自定义实体并通过daos.lua定义的其中一个DAO与它们进行交互时，才需要进行迁移。</td></tr><tr><td>schema.lua</td><td>Yes</td><td>保存插件配置的架构，以便用户只能输入有效的配置值。</td></tr></tbody></table><p><a href="https://docs.konghq.com/plugins/key-authentication/" target="_blank" rel="external">Key-Auth 插件</a>是具有此文件结构的插件的示例。<br>有关详细信息，请参阅其<a href="https://github.com/Kong/kong/tree/master/kong/plugins/key-auth" target="_blank" rel="external">源代码</a>。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;原文地址：&lt;a href=&quot;https://docs.konghq.com/1.1.x/plugin-development/file-structure/&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;https://docs.konghq.com/1.
      
    
    </summary>
    
      <category term="与代码" scheme="http://git.102no.com/categories/%E4%B8%8E%E4%BB%A3%E7%A0%81/"/>
    
    
      <category term="Kong" scheme="http://git.102no.com/tags/Kong/"/>
    
      <category term="Kong教程" scheme="http://git.102no.com/tags/Kong%E6%95%99%E7%A8%8B/"/>
    
      <category term="Kong中文文档" scheme="http://git.102no.com/tags/Kong%E4%B8%AD%E6%96%87%E6%96%87%E6%A1%A3/"/>
    
      <category term="Kong插件开发" scheme="http://git.102no.com/tags/Kong%E6%8F%92%E4%BB%B6%E5%BC%80%E5%8F%91/"/>
    
  </entry>
  
  <entry>
    <title>微服务 API 网关 Kong 插件开发 – 插件配置</title>
    <link href="http://git.102no.com/2019/05/15/kong-plugin-configuration/"/>
    <id>http://git.102no.com/2019/05/15/kong-plugin-configuration/</id>
    <published>2019-05-14T16:00:00.000Z</published>
    <updated>2019-05-16T16:28:42.460Z</updated>
    
    <content type="html"><![CDATA[<p>原文地址：<a href="https://docs.konghq.com/1.1.x/plugin-development/plugin-configuration/" target="_blank" rel="external">https://docs.konghq.com/1.1.x/plugin-development/plugin-configuration/</a> （不能保证所有的翻译都是准确无误的，所有如有翻译的不准确或错误之处，请一定记得查看原文，并欢迎留言指出）。</p><h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>大多数情况下，您的插件可以配置为满足您的所有用户需求。当插件被执行的时候，您的插件的配置存储在Kong的数据存储区中，以检索它并将其传递给<a href="https://docs.konghq.com/1.1.x/plugin-development/custom-logic/" target="_blank" rel="external">handler.lua</a>方法。</p><p>配置由Kong中的Lua表组成，我们称之为 <strong>schema</strong>。它包含用户在通过<a href="https://docs.konghq.com/1.1.x/admin-api" target="_blank" rel="external">Admin API</a>启用插件时将设置的键/值属性。Kong为您提供了一种验证用户插件配置的方法。</p><p>当用户向Admin API发出请求以启用或更新给定Service，Route和/或Consumer上的插件时，将根据您的架构schema插件的配置。</p><p>例如，用户执行以下请求：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">$ curl -X POST http://kong:8001/services/&lt;service-name-or-id&gt;/plugins/ \</div><div class="line">    -d &quot;name=my-custom-plugin&quot; \</div><div class="line">    -d &quot;config.foo=bar&quot;</div></pre></td></tr></table></figure></p><p>如果配置对象的所有<code>config</code>都根据您的模式有效，则API将返回<code>201 Created</code>，并且插件将与其配置一起存储在数据库中（在这种情况下为<code>{foo =“bar”}</code>）。如果配置无效，Admin API将返回<code>400 Bad Request</code>和相应的错误消息。</p><h2 id="模块"><a href="#模块" class="headerlink" title="模块"></a>模块</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">kong.plugins.&lt;plugin_name&gt;.schema</div></pre></td></tr></table></figure><h2 id="schema-lua规范"><a href="#schema-lua规范" class="headerlink" title="schema.lua规范"></a>schema.lua规范</h2><p>此模块将返回一个Lua表，其中包含将定义用户以后如何配置插件的属性的属性。<br>可用的属性是：</p><table><thead><tr><th>属性名称</th><th>Lua type</th><th>默认值</th><th>描述</th></tr></thead><tbody><tr><td><code>no_consumer</code></td><td>Boolen</td><td><code>false</code></td><td>如果为true，则无法将此插件应用于特定的Consumer。此插件必须仅应用于服务和路由。例如：身份验证插件。</td></tr><tr><td><code>fields</code></td><td>Table</td><td><code>{}</code></td><td>你插件的schema，可用属性及其规则的键/值表。</td></tr><tr><td><code>self_check</code></td><td>Function</td><td>nil</td><td>如果要在接受插件配置之前执行任何自定义验证，则要实现的功能。</td></tr></tbody></table><p>self_check函数必须按如下方式实现：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">-- @param `schema` 描述插件配置的架构（规则）的表。</div><div class="line">-- @param `config` 当前插件配置的键/值表。</div><div class="line">-- @param `dao` DAO的一个实例 (查看 DAO 章节).</div><div class="line">-- @param `is_updating` 一个布尔值，指示是否在更新的上下文中执行此检查。</div><div class="line">-- @return `valid` 一个布尔值，指示插件的配置是否有效。</div><div class="line">-- @return `error` 一个 DAO 错误 (查看 DAO 章节)</div></pre></td></tr></table></figure></p><p>以下是一个可能的schema.lua文件的示例：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line">return &#123;</div><div class="line">  no_consumer = true, -- 此插件仅适用于服务或路由，</div><div class="line">  fields = &#123;</div><div class="line">    -- 在此处描述您的插件配置架构。</div><div class="line">  &#125;,</div><div class="line">  self_check = function(schema, plugin_t, dao, is_updating)</div><div class="line">    -- 执行任何自定义验证</div><div class="line">    return true</div><div class="line">  end</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><h2 id="描述您的配置schema"><a href="#描述您的配置schema" class="headerlink" title="描述您的配置schema"></a>描述您的配置schema</h2><p><code>schema.lua</code>文件的<code>fields</code>自选描述了插件配置的schema。它是一个灵活的键/值表，其中每个键都是插件的有效配置属性，每个键都是一个描述该属性规则的表。例如：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">fields = &#123;</div><div class="line">   some_string = &#123;type = &quot;string&quot;, required = true&#125;,</div><div class="line">   some_boolean = &#123;type = &quot;boolean&quot;, default = false&#125;,</div><div class="line">   some_array = &#123;type = &quot;array&quot;, enum = &#123;&quot;GET&quot;, &quot;POST&quot;, &quot;PUT&quot;, &quot;DELETE&quot;&#125;&#125;</div><div class="line"> &#125;</div></pre></td></tr></table></figure></p><p>以下是属性的规则列表：</p><table><thead><tr><th>规则</th><th>LUA TYPE(S)</th><th>可使用的值</th><th>描述</th></tr></thead><tbody><tr><td><code>type</code></td><td>string</td><td>“id”, “number”, “boolean”, “string”, <br>“table”, “array”, “url”, “timestamp”</td><td>验证属性的类型。</td></tr><tr><td><code>required</code></td><td>boolean</td><td></td><td>默认值：false。<br> 如果为true，则该属性必须存在于配置中。</td></tr><tr><td><code>unique</code></td><td>boolean</td><td></td><td>默认值：false。<br> 如果为true，则该值必须是唯一的（请参阅下面的注释）。</td></tr><tr><td><code>default</code></td><td>any</td><td></td><td>如果未在配置中指定该属性，则将该属性设置为给定值。</td></tr><tr><td><code>immutable</code></td><td>boolean</td><td></td><td>默认值：false。<br> 如果为true，则在创建插件配置后将不允许更新该属性。</td></tr><tr><td><code>enum</code></td><td>table</td><td></td><td>属性的可接受值列表。不接受此列表中未包含的任何值。</td></tr><tr><td><code>regex</code></td><td>string</td><td></td><td>用于验证属性值的正则表达式。</td></tr><tr><td><code>schema</code></td><td>table</td><td></td><td>如果属性的类型是table，则定义用于验证这些子属性的模式。</td></tr><tr><td><code>func</code></td><td>function</td><td></td><td>用于对属性执行任何自定义验证的函数。请参阅后面的示例，了解其参数和返回值。</td></tr></tbody></table><ul><li><strong>type:</strong>将转换从请求参数中检索的值。如果类型不是本机Lua类型之一，则会对其执行自定义验证：<ul><li>id:必须是string</li><li>timestamp:必须是nember</li><li>uri:必须是有效的URL</li><li>array:必须是整数索引表（相当于Lua中的数组）。在Admin API中，可以通过在请求的正文中使用不同值的属性键的多次来发送这样的数组，或者通过单个body参数以逗号分隔。</li></ul></li><li><strong>unique:</strong>此属性对插件配置没有意义，但在插件需要在数据存储区中存储自定义实体时使用。</li><li><strong>schema:</strong>如果您需要对嵌套属性进行深化验证，则此字段允许您创建嵌套模式。模式验证是递归的。任何级别的嵌套都是有效的，但请记住，这会影响插件的可用性。</li><li><strong>附加到配置对象但schema中不存在的任何属性也将使所述配置无效。</strong> </li></ul><h2 id="例子"><a href="#例子" class="headerlink" title="例子"></a>例子</h2><p><a href="https://docs.konghq.com/plugins/key-authentication/" target="_blank" rel="external">key-auth</a>插件的<code>schema.lua</code>文件定义了API密钥的可接受参数名称的默认列表，以及默认设置为<code>false</code>的布尔值：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">-- schema.lua</div><div class="line">return &#123;</div><div class="line">  no_consumer = true,</div><div class="line">  fields = &#123;</div><div class="line">    key_names = &#123;type = &quot;array&quot;, required = true, default = &#123;&quot;apikey&quot;&#125;&#125;,</div><div class="line">    hide_credentials = &#123;type = &quot;boolean&quot;, default = false&#125;</div><div class="line">  &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure><p>于是，当在<a href="https://docs.konghq.com/1.1.x/plugin-development/custom-logic/" target="_blank" rel="external">handler.lua</a>中实现插件的<code>access()</code>函数并且用户使用默认值启用插件时，您可以如下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div></pre></td><td class="code"><pre><div class="line">-- handler.lua</div><div class="line">local BasePlugin = require &quot;kong.plugins.base_plugin&quot;</div><div class="line">local CustomHandler = BasePlugin:extend()</div><div class="line"></div><div class="line">function CustomHandler:new()</div><div class="line">  CustomHandler.super.new(self, &quot;my-custom-plugin&quot;)</div><div class="line">end</div><div class="line"></div><div class="line">function CustomHandler:access(config)</div><div class="line">  CustomHandler.super.access(self)</div><div class="line"></div><div class="line">  kong.log.inspect(config.key_names)        -- &#123;&quot;apikey&quot;&#125;</div><div class="line">  kong.log.inspect(config.hide_credentials) -- false</div><div class="line">end</div><div class="line"></div><div class="line">return CustomHandler</div></pre></td></tr></table></figure></p><p>请注意，上面的示例使用<a href="https://docs.konghq.com/1.1.x/pdk" target="_blank" rel="external">插件开发工具包(PDK)</a>的<a href="http://localhost:3000/docs/0.14.x/pdk/kong.log/#kong_log_inspect" target="_blank" rel="external">kong.log.inspect</a>函数将这些值打印到Kong日志中。</p><p>一个更复杂的示例，可用于最终日志记录插件：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div></pre></td><td class="code"><pre><div class="line">-- schema.lua</div><div class="line"></div><div class="line">local function server_port(given_value, given_config)</div><div class="line">  -- 自定义验证</div><div class="line">  if given_value &gt; 65534 then</div><div class="line">    return false, &quot;port value too high&quot;</div><div class="line">  end</div><div class="line"></div><div class="line">  -- 如果环境是“开发”，8080将是默认端口</div><div class="line">  if given_config.environment == &quot;development&quot; then</div><div class="line">    return true, nil, &#123;port = 8080&#125;</div><div class="line">  end</div><div class="line">end</div><div class="line"></div><div class="line">return &#123;</div><div class="line">  fields = &#123;</div><div class="line">    environment = &#123;type = &quot;string&quot;, required = true, enum = &#123;&quot;production&quot;, &quot;development&quot;&#125;&#125;</div><div class="line">    server = &#123;</div><div class="line">      type = &quot;table&quot;,</div><div class="line">      schema = &#123;</div><div class="line">        fields = &#123;</div><div class="line">          host = &#123;type = &quot;url&quot;, default = &quot;http://example.com&quot;&#125;,</div><div class="line">          port = &#123;type = &quot;number&quot;, func = server_port, default = 80&#125;</div><div class="line">        &#125;</div><div class="line">      &#125;</div><div class="line">    &#125;</div><div class="line">  &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><p>这样的配置将允许用户将配置发布到您的插件，如下所示：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">curl -X POST http://kong:8001/services/&lt;service-name-or-id&gt;/plugins \</div><div class="line">    -d &quot;name=my-custom-plugin&quot; \</div><div class="line">    -d &quot;config.environment=development&quot; \</div><div class="line">    -d &quot;config.server.host=http://localhost&quot;</div></pre></td></tr></table></figure></p><p>以下内容将在<a href="https://docs.konghq.com/1.1.x/plugin-development/custom-logic/" target="_blank" rel="external">handler.lua</a>中提供：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div></pre></td><td class="code"><pre><div class="line">-- handler.lua</div><div class="line">local BasePlugin = require &quot;kong.plugins.base_plugin&quot;</div><div class="line">local CustomHandler = BasePlugin:extend()</div><div class="line"></div><div class="line">function CustomHandler:new()</div><div class="line">  CustomHandler.super.new(self, &quot;my-custom-plugin&quot;)</div><div class="line">end</div><div class="line"></div><div class="line">function CustomHandler:access(config)</div><div class="line">  CustomHandler.super.access(self)</div><div class="line"></div><div class="line">  kong.log.inspect(config.environment) -- &quot;development&quot;</div><div class="line">  kong.log.inspect(config.server.host) -- &quot;http://localhost&quot;</div><div class="line">  kong.log.inspect(config.server.port) -- 8080</div><div class="line">end</div><div class="line"></div><div class="line">return CustomHandler</div></pre></td></tr></table></figure></p><p>您还可以在<a href="https://github.com/Kong/kong/blob/master/kong/plugins/key-auth/schema.lua" target="_blank" rel="external">Key-Auth插件源代码</a>中查看schema的真实示例。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;原文地址：&lt;a href=&quot;https://docs.konghq.com/1.1.x/plugin-development/plugin-configuration/&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;https://docs.konghq.
      
    
    </summary>
    
      <category term="与代码" scheme="http://git.102no.com/categories/%E4%B8%8E%E4%BB%A3%E7%A0%81/"/>
    
    
      <category term="Kong" scheme="http://git.102no.com/tags/Kong/"/>
    
      <category term="Kong教程" scheme="http://git.102no.com/tags/Kong%E6%95%99%E7%A8%8B/"/>
    
      <category term="Kong中文文档" scheme="http://git.102no.com/tags/Kong%E4%B8%AD%E6%96%87%E6%96%87%E6%A1%A3/"/>
    
      <category term="Kong插件开发" scheme="http://git.102no.com/tags/Kong%E6%8F%92%E4%BB%B6%E5%BC%80%E5%8F%91/"/>
    
  </entry>
  
  <entry>
    <title>微服务 API 网关 Kong 插件开发 - 安装/卸载插件</title>
    <link href="http://git.102no.com/2019/05/05/kong-plugin-distribution/"/>
    <id>http://git.102no.com/2019/05/05/kong-plugin-distribution/</id>
    <published>2019-05-04T16:00:00.000Z</published>
    <updated>2019-05-05T15:18:44.872Z</updated>
    
    <content type="html"><![CDATA[<p>原文地址：<a href="https://docs.konghq.com/1.1.x/plugin-development/distribution/" target="_blank" rel="external">https://docs.konghq.com/1.1.x/plugin-development/distribution/</a> （不能保证所有的翻译都是准确无误的，所有如有翻译的不准确或错误之处，请一定记得查看原文，并欢迎留言指出）。</p><h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><p>Kong的自定义插件由Lua源文件组成，这些源文件需要位于每个Kong节点的文件系统中。本指南将为您提供逐步说明，使Kong节点了解您的自定义插件。这些步骤应该应用于Kong集群中的每个节点，以确保每个节点上都有自定义插件。</p><h2 id="打包源"><a href="#打包源" class="headerlink" title="打包源"></a>打包源</h2><p>您可以使用常规打包策略（例如tar），也可以使用LuaRocks包管理器为您执行此操作。我们推荐使用LuaRocks，因为它在使用其中一个官方分发包时与Kong一起安装。</p><p>使用LuaRocks时，您必须创建一个<code>rockspec</code>文件，用来指定包的内容。有关示例，请参阅<a href="https://github.com/Kong/kong-plugin" target="_blank" rel="external">Kong插件模板</a>，有关该格式的更多信息，请参阅有关<a href="https://github.com/keplerproject/luarocks/wiki/Creating-a-rock" target="_blank" rel="external">rockspecs的LuaRocks文档</a>。</p><p>使用以下命令打包你的rock（来自插件仓库）：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"># install it locally (based on the `.rockspec` in the current directory)</div><div class="line">$ luarocks make</div><div class="line"></div><div class="line"># 打包已安装的rock</div><div class="line">$ luarocks pack &lt;plugin-name&gt; &lt;version&gt;</div></pre></td></tr></table></figure></p><p>假设你的插件rockspec的名字为<code>kong-plugin-myPlugin-0.1.0-1.rockspec</code>，上面就会变成：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$ luarocks pack kong-plugin-myPlugin 0.1.0-1</div></pre></td></tr></table></figure></p><p>LuaRocks <code>pack</code>命令现在已经创建了一个<code>.rock</code>文件（这只是一个包含安装rock所需内容的zip文件）。</p><p>如果您不使用或不能使用LuaRocks，则使用<code>tar</code>将插件所包含的<code>.lua</code>文件打包到<code>.tar.gz</code>存档中。<br>如果目标系统上有LuaRocks，也可以包含<code>.rockspec</code>文件。</p><p>该插件的内容应该接近以下内容：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line">$ tree &lt;plugin-name&gt;</div><div class="line">&lt;plugin-name&gt;</div><div class="line">├── INSTALL.txt</div><div class="line">├── README.md</div><div class="line">├── kong</div><div class="line">│   └── plugins</div><div class="line">│       └── &lt;plugin-name&gt;</div><div class="line">│           ├── handler.lua</div><div class="line">│           └── schema.lua</div><div class="line">└── &lt;plugin-name&gt;-&lt;version&gt;.rockspec</div></pre></td></tr></table></figure></p><h2 id="安装插件"><a href="#安装插件" class="headerlink" title="安装插件"></a>安装插件</h2><p>要使Kong节点能够使用自定义插件，必须在主机的文件系统上安装自定义插件的Lua源。有多种方法：通过LuaRocks，或手动。<br>选择一个，然后跳转到第3部分。</p><ol><li><p>来自新建的’rock’的LuaRocks。<br> <code>.rock</code>文件是一个自包含的软件包，可以在本地安装，也可以从远程服务器安装。<br> 如果您的系统中安装了<code>luarocks</code>实用程序（如果使用其中一个官方安装包，可能就是这种情况），您可以在LuaRocks树（LuaRocks安装Lua模块的目录）中安装“rock”。<br> 它可以通过以下方式安装：  </p> <figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$ luarocks install &lt;rock-filename&gt;</div></pre></td></tr></table></figure><p> 文件名可以是本地名称，或任何支持的方法。<br> 例如：<code>http://myrepository.lan/rocks/myplugin-0.1.0-1.all.rock</code></p></li><li><p>从源档案中通过LuaRocks安装。<br> 如果您的系统中安装了luarocks实用程序（如果使用其中一个官方安装包，可能就是这种情况），您可以在LuaRocks树（LuaRocks安装Lua模块的目录）中安装Lua源代码。<br> 您可以通过将当前目录更改为提取的存档来实现，其中rockspec文件是：  </p> <figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$ cd &lt;plugin-name&gt;</div></pre></td></tr></table></figure><p> 然后运行以下命令：</p> <figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$ luarocks make</div></pre></td></tr></table></figure><p> 这将在系统的LuaRocks树中的<code>kong/plugins/&lt;plugin-name&gt;</code>中安装Lua源代码，其中所有的Kong源都已存在。</p></li><li><p>手动<br> 安装插件源的一种更保守的方法是避免“污染”LuaRocks树，而是将Kong指向包含它们的目录。<br> 这是通过调整Kong配置的<code>lua_package_path</code>属性来完成的。如果你熟悉它，那么这个属性是Lua VM的<code>LUA_PATH</code>变量的别名。<br> 这些属性包含以分号分隔的目录列表，用于搜索Lua源。它应该在您的Kong配置文件中设置如下：</p> <figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">lua_package_path = /&lt;path-to-plugin-location&gt;/?.lua;</div></pre></td></tr></table></figure><p> 继续:<br> 4.<code>/&lt;path-to-plugin-location&gt;</code>是包含提取的存档的目录的路径。它应该是归档中<code>kong</code>目录的位置。<br> 5.<code>?</code>是一个占位符，将被<code>kong.plugins</code>替换。<code>&lt;plugin-name&gt;</code>当Kong将尝试加载你的插件。<br> 6.<code>;;</code>“默认Lua路径”的占位符。不要改变它。<br> 例如:<br> 插件位于文件系统上，使处理程序文件为：</p> <figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">/usr/local/custom/kong/plugins/&lt;something&gt;/handler.lua</div></pre></td></tr></table></figure><p> kong目录的位置是：<code>/usr/local/custom</code>，因此正确的路径设置将是：</p> <figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">lua_package_path = /usr/local/custom/?.lua;;</div></pre></td></tr></table></figure></li></ol><pre><code>多个插件：如果您希望以这种方式安装两个或更多自定义插件，可以将变量设置为：<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">lua_package_path = /path/to/plugin1/?.lua;/path/to/plugin2/?.lua;;</div></pre></td></tr></table></figure>    7.`;`是目录之间的分隔符。    8.`;;`仍然意味着“默认的Lua路径”。注意：您还可以通过其等效的环境变量`KONG_LUA_PACKAGE_PATH`设置此属性。</code></pre><p>提醒：无论您使用哪种方法来安装插件的源，您仍必须为Kong群集中的每个节点执行此操作。</p><h2 id="加载插件"><a href="#加载插件" class="headerlink" title="加载插件"></a>加载插件</h2><p>您现在必须将自定义插件的名称添加到Kong配置中的插件列表中（在每个Kong节点上）：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">plugins = bundled,&lt;plugin-name&gt;</div></pre></td></tr></table></figure></p><p>或者，如果您不想包含默认捆绑的插件：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">plugins = &lt;plugin-name&gt;</div></pre></td></tr></table></figure></p><p>或者<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">plugins = plugin1,plugin2</div></pre></td></tr></table></figure></p><p>注意：您还可以通过其等效的环境变量<code>KONG_PLUGINS</code>来设置此属性。<br>提醒：不要忘记更新Kong群集中每个节点的plugins指令。<br>提醒：插件重启后会生效：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">kong restart</div></pre></td></tr></table></figure></p><p>但是，如果你想在kong永不停止时应用插件，你可以使用：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">kong prepare</div><div class="line">kong reload</div></pre></td></tr></table></figure></p><h2 id="验证加载插件"><a href="#验证加载插件" class="headerlink" title="验证加载插件"></a>验证加载插件</h2><p>你现在应该能够毫无问题地启动Kong。<br>请参阅自定义插件有关如何在服务，路由或消费者实体上启用/配置插件的说明。  </p><p>为确保您的插件由Kong加载，您可以使用调试日志级别启动Kong：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">log_level = debug</div></pre></td></tr></table></figure></p><p>或者<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">KONG_LOG_LEVEL=debug</div></pre></td></tr></table></figure></p><p>然后，您应该看到正在加载的每个插件的以下日志：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">[debug] Loading plugin &lt;plugin-name&gt;</div></pre></td></tr></table></figure></p><h2 id="删除插件"><a href="#删除插件" class="headerlink" title="删除插件"></a>删除插件</h2><p>完全删除插件有三个步骤。</p><ol><li>从您的Kong Service或Route配置中删除插件。确保它不再适用于全局，也不适用于任何服务，路由或使用者。对于整个Kong集群，只需执行一次，不需要重新启动/重新加载。此步骤本身将使插件不再使用。但它仍然可用，仍然可以重新应用插件。</li><li>从<code>plugins</code>指令中删除插件（在每个Kong节点上）。确保在执行此操作之前已完成步骤1。在此步骤之后，任何人都无法将插件重新应用于任何Kong Service，Route，Consumer甚至全局。此步骤需要重新启动/重新加载Kong节点才能生效。</li><li>要彻底删除插件，请从每个Kong节点中删除与插件相关的文件。在删除文件之前，请确保已完成步骤2，包括重新启动/重新加载Kong。如果你使用LuaRocks来安装插件，你可以使用<code>luarocks remove &lt;plugin-name&gt;</code>来删除它。</li></ol><h2 id="分发插件"><a href="#分发插件" class="headerlink" title="分发插件"></a>分发插件</h2><p>这样做的首选方法是使用<a href="https://luarocks.org/" target="_blank" rel="external">LuaRocks</a>，Lua模块的包管理器。它称这些模块为“rocks”。<br><strong>您的模块不必存在于Kong存储库中</strong>，但如果您希望维护Kong设置，则可能就是这样。</p><p>通过在<a href="https://github.com/keplerproject/luarocks/wiki/Creating-a-rock" target="_blank" rel="external">rockspec</a>文件中定义模块（及其最终依赖项），您可以通过LuaRocks在您的平台上安装这些模块。</p><p>您也可以在LuaRocks上传模块并将其提供给所有人！  </p><p>有关示例，请参阅<a href="https://github.com/Kong/kong-plugin" target="_blank" rel="external">Kong插件模板</a>，有关该格式的更多信息，请参阅有关<a href="https://github.com/keplerproject/luarocks/wiki/Creating-a-rock" target="_blank" rel="external">rockspecs的LuaRocks文档</a>。</p><h2 id="故障排除"><a href="#故障排除" class="headerlink" title="故障排除"></a>故障排除</h2><p>由于以下几个原因，配置错误的自定义插件可能无法启动：</p><ul><li>“plugin is in use but not enabled” -&gt; 您从另一个节点配置了一个自定义插件，并且该插件配置在数据库中，但您尝试启动的当前节点在其<code>plugins</code>指令中没有它。要解决此问题，请将插件的名称添加到节点的<code>plugins</code>指令中。</li><li>“plugin is enabled but not installed” -&gt; 插件的名称出现在<code>plugins</code>指令中，但是Kong无法从文件系统加载<code>handler.lua</code>源文件。要解决此问题，请确保正确设置<a href="https://docs.konghq.com/1.1.x/configuration/#development-miscellaneous-section" target="_blank" rel="external">lua_package_path</a>指令以加载此插件的Lua源。</li><li>“no configuration schema found for plugin” -&gt; 插件已在<code>plugins</code>指令中安装，但是Kong无法从文件系统加载<code>schema.lua</code>源文件。要解决此问题，请确保<code>schema.lua</code>文件与插件的<code>handler.lua</code>文件一起存在。</li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;原文地址：&lt;a href=&quot;https://docs.konghq.com/1.1.x/plugin-development/distribution/&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;https://docs.konghq.com/1.1.
      
    
    </summary>
    
      <category term="与代码" scheme="http://git.102no.com/categories/%E4%B8%8E%E4%BB%A3%E7%A0%81/"/>
    
    
      <category term="Kong" scheme="http://git.102no.com/tags/Kong/"/>
    
      <category term="Kong教程" scheme="http://git.102no.com/tags/Kong%E6%95%99%E7%A8%8B/"/>
    
      <category term="Kong中文文档" scheme="http://git.102no.com/tags/Kong%E4%B8%AD%E6%96%87%E6%96%87%E6%A1%A3/"/>
    
      <category term="Kong插件开发" scheme="http://git.102no.com/tags/Kong%E6%8F%92%E4%BB%B6%E5%BC%80%E5%8F%91/"/>
    
  </entry>
  
  <entry>
    <title>微服务 API 网关 Kong 插件开发 - 缓存自定义实体</title>
    <link href="http://git.102no.com/2019/04/29/kong-plugin-entities-cache/"/>
    <id>http://git.102no.com/2019/04/29/kong-plugin-entities-cache/</id>
    <published>2019-04-28T16:00:00.000Z</published>
    <updated>2019-04-29T15:29:12.067Z</updated>
    
    <content type="html"><![CDATA[<p>原文地址：<a href="https://docs.konghq.com/1.1.x/plugin-development/entities-cache/" target="_blank" rel="external">https://docs.konghq.com/1.1.x/plugin-development/entities-cache/</a> （不能保证所有的翻译都是准确无误的，所有如有翻译的不准确或错误之处，请一定记得查看原文，并欢迎留言指出）。</p><h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><p>您的插件可能需要经常访问每个请求 和/或 响应的自定义实体（在<a href="https://docs.konghq.com/1.1.x/plugin-development/custom-entities/" target="_blank" rel="external">前一章</a>中可见）。通常，加载它们一次并将它们缓存在内存中会显着提高性能，同时确保数据存储区不会因负载增加而受到压力。</p><p>考虑一个api-key身份验证插件，需要在每个请求上验证api-key，从而在每个请求中从数据存储区加载自定义凭据对象。当客户端提供api-key以及请求时，通常你会查询数据存储区以检查该密钥是否存在，然后，阻止请求或检索 Consumer ID以识别用户，这将在每个请求上发生，并且效率非常低：</p><ul><li>查询数据存储会增加每个请求的延迟，使请求处理速度变慢。</li><li>数据存储区也会受到负载增加，可能崩溃或减速的影响，这反过来会影响每个Kong节点。</li></ul><p>为避免每次都查询数据存储区，我们可以在节点上缓存内存中的自定义实体，这样频繁的实体查找不会每次都触发数据存储区查询（仅限第一次），而是在内存中查询，从数据存储区查询它（特别是在高负载下）更快更可靠。</p><h2 id="Modules"><a href="#Modules" class="headerlink" title="Modules"></a>Modules</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">kong.plugins.&lt;plugin_name&gt;.daos</div></pre></td></tr></table></figure><h2 id="缓存自定义实体"><a href="#缓存自定义实体" class="headerlink" title="缓存自定义实体"></a>缓存自定义实体</h2><p>一旦定义了自定义实体，就可以使用<a href="https://docs.konghq.com/1.1.x/pdk" target="_blank" rel="external">插件开发工具包</a>提供的 <a href="https://docs.konghq.com/1.1.x/pdk/#kong-cache" target="_blank" rel="external">kong.cache</a> 模块将它们缓存在代码中的内存中：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">local cache = kong.cache</div></pre></td></tr></table></figure></p><p>有两级缓存：</p><ol><li>L1：Lua内存缓存 - nginx worker的本地缓存这可以包含任何类型的Lua值。</li><li>L2：共享内存缓存（SHM） - nginx节点的本地缓存，但在所有工作者之间共享。这只能保存标量值，因此需要（反）序列化。</li></ol><p>从数据库中提取数据时，它将存储在两个缓存中。现在，如果同一个工作进程再次请求数据，它将从Lua内存缓存中检索以前反序列化的数据。如果同一Nginx节点中的另一个工作程序请求该数据，它将在SHM中找到数据，对其进行反序列化（并将其存储在自己的Lua内存缓存中），然后将其返回。</p><p>该模块公开以下功能：</p><table><thead><tr><th>FUNCTION NAME</th><th>DESCRIPTION</th></tr></thead><tbody><tr><td><code>value, err = cache:get(key, opts?, cb, ...)</code></td><td>从缓存中检索值。如果缓存没有值（未命中）,在保护模式下调用<code>cb</code>。<code>cb</code>必须返回一个（并且只有一个）将被缓存的值。它可能会抛出错误，因为这些错误会被Kong捕获并正确记录在<code>ngx.ERR</code>级别。 此函数会缓存否定结果（nil）。因此，在检查错误时必须依赖其第二个参数<code>err</code>。</td></tr><tr><td><code>ttl, err, value = cache:probe(key)</code></td><td>检查是否缓存了值。如果是，则返回其剩余的TTL。它没有，返回零。</td></tr></tbody></table><p>缓存的值也可以是负缓存。第三个返回值是被缓存的值本身。 |<br>| <code>cache:invalidate_local(key)</code>| 从节点缓存中移除一个值。 |<br>| <code>cache:invalidate(key)</code> | 从节点的缓存中删除一个值，并将删除事件传播到集群中的所有其他节点。 |<br>| <code>cache:purge()</code> | 从节点缓存中删除所有值。 |</p><p>回到我们的身份验证插件示例，要查找具有特定api-key的凭据，我们将编写类似于:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div></pre></td><td class="code"><pre><div class="line">-- access.lua</div><div class="line"></div><div class="line">local function load_entity_key(api_key)</div><div class="line">  -- 重要: 回调是在锁中执行的，因此我们不能在这里终止请求，我们必须始终返回。</div><div class="line">  local apikeys, err = kong.dao.apikeys:find_all(&#123;key = api_key&#125;) -- Lookup in the datastore</div><div class="line">  if err then</div><div class="line">    error(err) -- 被kong.cache捕获并记录</div><div class="line">  end</div><div class="line"></div><div class="line">  if not apikeys then</div><div class="line">    return nil -- 没有找到 (cached for `neg_ttl`)</div><div class="line">  end</div><div class="line"></div><div class="line">  -- 假设键是唯一的，我们总是只有一个值…</div><div class="line">  return apikeys[1] -- cache the credential (cached for `ttl`)</div><div class="line">end</div><div class="line"></div><div class="line">-- 从请求querystring检索apikey</div><div class="line">local querystring = kong.request.get_query()</div><div class="line">local apikey = querystring.apikey</div><div class="line"></div><div class="line">-- 我们使用缓存。首先检查apikey是否已经存在</div><div class="line">-- 存储在内存缓存中的键值为:“apikeys”。. .apikey</div><div class="line">-- 如果不是，则查找数据存储并返回凭据对象。</div><div class="line">-- 内部缓存。get将把值保存在内存中，然后返回凭据。</div><div class="line">local credential, err = kong.cache:get(&quot;apikeys.&quot; .. apikey, nil,</div><div class="line">                                       load_entity_key, apikey)</div><div class="line">if err then</div><div class="line">  return kong.response.exit(500, &quot;Unexpected error: &quot; .. err)</div><div class="line">end</div><div class="line"></div><div class="line">if not credential then</div><div class="line">  -- 缓存和数据存储中没有凭据</div><div class="line">  return kong.response.exit(403, &quot;Invalid authentication credentials&quot;)</div><div class="line">end</div><div class="line"></div><div class="line">-- 如果凭据存在且有效，则设置上游标头</div><div class="line">kong.service.request.set_header(&quot;X-API-Key&quot;, credential.apikey)</div></pre></td></tr></table></figure><p>注意，在上面的示例中，我们使用插件开发工具包中的各种组件与请求、缓存模块交互，甚至从插件生成响应。<br>现在，有了上面的机制，一旦使用者使用API键发出请求，缓存就会被认为是热的，随后的请求不会导致数据库查询。<br>在Key-Auth插件处理程序中，缓存用于多个位置。让我们来看看官方插件是如何使用缓存的。</p><h3 id="更新或者删除一个缓存实体"><a href="#更新或者删除一个缓存实体" class="headerlink" title="更新或者删除一个缓存实体"></a>更新或者删除一个缓存实体</h3><p>每次在数据存储中更新或删除缓存的自定义实体(即使用Admin API)时，都会在数据存储中的数据与缓存在Kong节点内存中的数据之间产生不一致。为了避免这种不一致，我们需要从内存存储中删除缓存的实体，并强制Kong从数据存储中再次请求它。我们将此过程称为缓存失效。</p><h2 id="缓存失效"><a href="#缓存失效" class="headerlink" title="缓存失效"></a>缓存失效</h2><p>如果希望缓存的实体在CRUD操作时失效，而不是等待它们到达TTL，则必须执行以下步骤。对于大多数实体，这个过程都可以自动化，但是手动订阅一些CRUD事件可能需要使一些具有更复杂关系的实体失效。</p><h3 id="自动缓存失效"><a href="#自动缓存失效" class="headerlink" title="自动缓存失效"></a>自动缓存失效</h3><p>如果依赖于实体模式的cache_key属性，则可以为实体提供开箱即用的缓存失效。例如，在下面的模式中:<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line">local SCHEMA = &#123;</div><div class="line">  primary_key = &#123; &quot;id&quot; &#125;,</div><div class="line">  table = &quot;keyauth_credentials&quot;,</div><div class="line">  cache_key = &#123; &quot;key&quot; &#125;, -- 此实体的缓存键</div><div class="line">  fields = &#123;</div><div class="line">    id = &#123; type = &quot;id&quot; &#125;,</div><div class="line">    created_at = &#123; type = &quot;timestamp&quot;, immutable = true &#125;,</div><div class="line">    consumer_id = &#123; type = &quot;id&quot;, required = true, foreign = &quot;consumers:id&quot;&#125;,</div><div class="line">    key = &#123; type = &quot;string&quot;, required = false, unique = true &#125;</div><div class="line">  &#125;</div><div class="line">&#125;</div><div class="line"></div><div class="line">return &#123; keyauth_credentials = SCHEMA &#125;</div></pre></td></tr></table></figure></p><p>我们可以看到，我们将这个<code>API key</code>实体的缓存键声明为它的<code>key</code>属性。这里使用<code>key</code>是因为它有一个唯一的约束。因此，添加到<code>cache_key</code>的属性应该产生唯一的组合，这样就不会有两个实体产生相同的缓存键。<br>添加此值允许您在该实体的DAO上使用以下函数:<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">cache_key = kong.db.&lt;dao&gt;:cache_key(arg1, arg2, arg3, ...)</div></pre></td></tr></table></figure></p><p>其中参数必须是模式的<code>cache_key</code>属性中指定的属性，按照指定的顺序，然后，此函数计算确保唯一的字符串值<code>cache_key</code>。<br>例如，如果我们要生成API密钥的cache_key：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">local cache_key = kong.db.keyauth_credentials:cache_key(&quot;abcd&quot;)</div></pre></td></tr></table></figure></p><p>这将为API密钥<code>“abcd”</code>（从查询的一个参数中检索）生成一个cache_key，我们可以使用它来从缓存中检索密钥（如果缓存是未命中，则从数据库中获取）：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line">local apikey = kong.request.get_query().apikey</div><div class="line">local cache_key = kong.db.keyauth_credentials:cache_key(apikey)</div><div class="line"></div><div class="line">local credential, err = kong.cache:get(cache_key, nil, load_entity_key, apikey)</div><div class="line">if err then</div><div class="line">  return kong.response.exit(500, &quot;Unexpected error: &quot; .. err)</div><div class="line">end</div><div class="line"></div><div class="line">-- do something with the credential</div></pre></td></tr></table></figure></p><p>如果<code>cache_key</code>是这样生成的并且在实体的模式中指定，则缓存失效将是一个自动过程：影响此API密钥的每个CRUD操作都将使make生成受影响的<code>cache_key</code>，并将其广播到群集上的所有其他节点，以便他们可以从缓存中逐出该特定值，并在下一个请求中从数据存储区中获取新值。</p><p>当父实体正在接收CRUD操作时（例如，拥有此API密钥的消费者，根据我们的模式的consumer_id属性），Kong为父实体和子实体执行缓存失效机制。</p><p>注意：请注意Kong提供的负面缓存。在上面的示例中，如果给定密钥的数据存储区中没有API密钥，则缓存模块将存储未命中，就像它是命中一样。这意味着Kong也会传播“创建”事件（使用此给定密钥创建API密钥的事件），以便存储未命中的所有节点都可以驱逐它，并从数据存储中正确地获取新创建的API密钥。</p><p>请参阅<a href="https://docs.konghq.com/1.1.x/clustering/" target="_blank" rel="external">群集指南</a>以确保为此类失效事件正确配置了群集。</p><h3 id="手动缓存失效"><a href="#手动缓存失效" class="headerlink" title="手动缓存失效"></a>手动缓存失效</h3><p>在某些情况下，实体架构的<code>cache_key</code>属性不够灵活，并且必须手动使其缓存无效。原因可能是插件没有通过传统的<code>foreign =“parent_entity：parent_attribute”</code>语法定义与另一个实体的关系，或者因为它没有使用来自其DAO的<code>cache_key</code>方法，或者甚至因为它以某种方式滥用缓存机制。</p><p>在这些情况下，您可以手动将自己的订户设置为Kong正在收听的相同失效频道，并执行您自己的自定义失效工作。</p><p>要监听Kong内部的失效通道，请在插件的init_worker处理程序中实现以下内容：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div></pre></td><td class="code"><pre><div class="line">function MyCustomHandler:init_worker()</div><div class="line">  -- listen to all CRUD operations made on Consumers</div><div class="line">  kong.worker_events.register(function(data)</div><div class="line"></div><div class="line">  end, &quot;crud&quot;, &quot;consumers&quot;)</div><div class="line"></div><div class="line">  -- or, listen to a specific CRUD operation only</div><div class="line">  kong.worker_events.register(function(data)</div><div class="line">    kong.log.inspect(data.operation)  -- &quot;update&quot;</div><div class="line">    kong.log.inspect(data.old_entity) -- old entity table (only for &quot;update&quot;)</div><div class="line">    kong.log.inspect(data.entity)     -- new entity table</div><div class="line">    kong.log.inspect(data.schema)     -- entity&apos;s schema</div><div class="line">  end, &quot;crud&quot;, &quot;consumers:update&quot;)</div><div class="line">end</div></pre></td></tr></table></figure></p><p>一旦上述侦听器适用于所需的实体，您就可以根据需要对插件已缓存的任何实体执行手动失效。<br>例如：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line">kong.worker_events.register(function(data)</div><div class="line">  if data.operation == &quot;delete&quot; then</div><div class="line">    local cache_key = data.entity.id</div><div class="line">    kong.cache:invalidate(&quot;prefix:&quot; .. cache_key)</div><div class="line">  end</div><div class="line">end, &quot;crud&quot;, &quot;consumers&quot;)</div></pre></td></tr></table></figure></p><h2 id="扩展Admin-API"><a href="#扩展Admin-API" class="headerlink" title="扩展Admin API"></a>扩展Admin API</h2><p>您可能已经知道，Admin API是Kong用户与Kong通信以设置其API和插件的地方。他们可能还需要能够与您为插件实现的自定义实体进行交互（例如，创建和删除API密钥）。这样做的方法是扩展Admin API，我们将在下一章详细介绍：<a href="https://docs.konghq.com/1.1.x/plugin-development/admin-api/" target="_blank" rel="external">扩展Admin API</a>。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;原文地址：&lt;a href=&quot;https://docs.konghq.com/1.1.x/plugin-development/entities-cache/&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;https://docs.konghq.com/1.
      
    
    </summary>
    
      <category term="与代码" scheme="http://git.102no.com/categories/%E4%B8%8E%E4%BB%A3%E7%A0%81/"/>
    
    
      <category term="Kong" scheme="http://git.102no.com/tags/Kong/"/>
    
      <category term="Kong教程" scheme="http://git.102no.com/tags/Kong%E6%95%99%E7%A8%8B/"/>
    
      <category term="Kong中文文档" scheme="http://git.102no.com/tags/Kong%E4%B8%AD%E6%96%87%E6%96%87%E6%A1%A3/"/>
    
      <category term="Kong插件开发" scheme="http://git.102no.com/tags/Kong%E6%8F%92%E4%BB%B6%E5%BC%80%E5%8F%91/"/>
    
  </entry>
  
  <entry>
    <title>微服务 API 网关 Kong 插件开发 - 实现自定义逻辑</title>
    <link href="http://git.102no.com/2019/04/17/kong-plugin-custom-logic/"/>
    <id>http://git.102no.com/2019/04/17/kong-plugin-custom-logic/</id>
    <published>2019-04-16T16:00:00.000Z</published>
    <updated>2019-04-16T16:11:29.992Z</updated>
    
    <content type="html"><![CDATA[<p>原文地址：<a href="https://docs.konghq.com/1.1.x/plugin-development/custom-logic/" target="_blank" rel="external">https://docs.konghq.com/1.1.x/plugin-development/custom-logic/</a> （不能保证所有的翻译都是准确无误的，所有如有翻译的不准确或错误之处，请一定记得查看原文，并欢迎留言指出）。</p><h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>Kong插件允许您在 request/response 的生命周期中的几个入口点注入自定义逻辑（使用 Lua 语言），因为它由Kong代理。为此，必须实现<code>base_plugin.lua</code> 接口的一个或多个方法。这些方法将在命名空间下的模块中实现：<code>kong.plugins。&lt;plugin_name&gt; .handler</code>。</p><h2 id="Module"><a href="#Module" class="headerlink" title="Module"></a>Module</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">kong.plugins.&lt;plugin_name&gt;.handler</div></pre></td></tr></table></figure><h2 id="可用的请求上下文"><a href="#可用的请求上下文" class="headerlink" title="可用的请求上下文"></a>可用的请求上下文</h2><p>插件接口允许您覆盖handler.lua文件中的以下任何方法，以在Kong的执行生命周期的各个入口点实现自定义逻辑：</p><table><thead><tr><th>函数名</th><th>LUA-NGINX-MODULE Context</th><th>描述</th></tr></thead><tbody><tr><td><code>:init_worker()</code></td><td><a href="https://github.com/openresty/lua-nginx-module#init_worker_by_lua" target="_blank" rel="external">init_worker_by_lua</a></td><td>在每个 Nginx 工作进程启动时执行</td></tr><tr><td><code>:certificate()</code></td><td><a href="https://github.com/openresty/lua-nginx-module#ssl_certificate_by_lua_block" target="_blank" rel="external">ssl_certificate_by_lua</a></td><td>在SSL握手阶段的SSL证书服务阶段执行</td></tr><tr><td><code>:rewrite()</code></td><td><a href="https://github.com/openresty/lua-nginx-module#rewrite_by_lua_block" target="_blank" rel="external">rewrite_by_lua</a></td><td>从客户端接收作为重写阶段处理程序的每个请求执行。在这个阶段，无论是API还是消费者都没有被识别，因此这个处理器只在插件被配置为全局插件时执行</td></tr><tr><td><code>:access()</code></td><td><a href="https://github.com/openresty/lua-nginx-module#access_by_lua" target="_blank" rel="external">access_by_lua</a></td><td>为客户的每一个请求而执行，并在它被代理到上游服务之前执行</td></tr><tr><td><code>:header_filter()</code></td><td><a href="https://github.com/openresty/lua-nginx-module#header_filter_by_lua" target="_blank" rel="external">header_filter_by_lua</a></td><td>从上游服务接收到所有响应头字节时执行</td></tr><tr><td><code>:body_filter()</code></td><td><a href="https://github.com/openresty/lua-nginx-module#body_filter_by_lua" target="_blank" rel="external">body_filter_by_lua</a></td><td>从上游服务接收的响应体的每个块时执行。由于响应流回客户端，它可以超过缓冲区大小，因此，如果响应较大，该方法可以被多次调用</td></tr><tr><td><code>:log()</code></td><td><a href="https://github.com/openresty/lua-nginx-module#log_by_lua" target="_blank" rel="external">log_by_lua</a></td><td>当最后一个响应字节已经发送到客户端时执行</td></tr></tbody></table><p>所有这些函数都使用一个参数，该参数由Kong在调用时给出：插件的配置。此参数是Lua表，并包含用户根据插件的架构（在<code>schema.lua</code>模块中描述）定义的值。有关插件模式的更多信息将在<a href="https://docs.konghq.com/1.1.x/plugin-development/plugin-configuration/" target="_blank" rel="external">下一章</a>中介绍。</p><h2 id="handler-lua规范"><a href="#handler-lua规范" class="headerlink" title="handler.lua规范"></a>handler.lua规范</h2><p>handler.lua文件必须返回一个表，该表实现了您希望执行的函数。为简洁起见，这里有一个注释示例模块，它实现了所有可用的方法：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div><div class="line">40</div><div class="line">41</div><div class="line">42</div><div class="line">43</div><div class="line">44</div><div class="line">45</div><div class="line">46</div><div class="line">47</div><div class="line">48</div><div class="line">49</div><div class="line">50</div><div class="line">51</div><div class="line">52</div><div class="line">53</div><div class="line">54</div><div class="line">55</div><div class="line">56</div><div class="line">57</div><div class="line">58</div><div class="line">59</div><div class="line">60</div><div class="line">61</div><div class="line">62</div><div class="line">63</div><div class="line">64</div><div class="line">65</div><div class="line">66</div><div class="line">67</div><div class="line">68</div><div class="line">69</div><div class="line">70</div><div class="line">71</div><div class="line">72</div></pre></td><td class="code"><pre><div class="line">-- 扩展基本插件处理程序是可选的，因为Lua中没有真正的接口概念，</div><div class="line">-- 但是基本插件处理程序的方法可以从子实现中调用，</div><div class="line">-- 并将在“error.log”中打印日志(其中打印所有日志)。</div><div class="line">local BasePlugin = require &quot;kong.plugins.base_plugin&quot;</div><div class="line">local CustomHandler = BasePlugin:extend()</div><div class="line"></div><div class="line">-- 你的插件处理程序的构造函数。</div><div class="line">-- 如果要扩展基本插件处理程序，它的唯一作用就是用名称实例化自己。</div><div class="line">-- 该名称是您的插件名称，因为它将打印在日志中</div><div class="line">function CustomHandler:new()</div><div class="line">  CustomHandler.super.new(self, &quot;my-custom-plugin&quot;)</div><div class="line">end</div><div class="line"></div><div class="line">function CustomHandler:init_worker()</div><div class="line">  -- 最终，执行父实现</div><div class="line">  -- (将记录您的插件正在进入此上下文)</div><div class="line">  CustomHandler.super.init_worker(self)</div><div class="line"></div><div class="line">  -- 在此实现任何自定义逻辑</div><div class="line">end</div><div class="line"></div><div class="line">function CustomHandler:certificate(config)</div><div class="line">  -- 最终，执行父实现</div><div class="line">  -- (将记录您的插件正在进入此上下文)</div><div class="line">  CustomHandler.super.certificate(self)</div><div class="line"></div><div class="line">  -- Implement any custom logic here</div><div class="line">end</div><div class="line"></div><div class="line">function CustomHandler:rewrite(config)</div><div class="line">  -- Eventually, execute the parent implementation</div><div class="line">  -- (will log that your plugin is entering this context)</div><div class="line">  CustomHandler.super.rewrite(self)</div><div class="line"></div><div class="line">  -- Implement any custom logic here</div><div class="line">end</div><div class="line"></div><div class="line">function CustomHandler:access(config)</div><div class="line">  -- Eventually, execute the parent implementation</div><div class="line">  -- (will log that your plugin is entering this context)</div><div class="line">  CustomHandler.super.access(self)</div><div class="line"></div><div class="line">  -- Implement any custom logic here</div><div class="line">end</div><div class="line"></div><div class="line">function CustomHandler:header_filter(config)</div><div class="line">  -- Eventually, execute the parent implementation</div><div class="line">  -- (will log that your plugin is entering this context)</div><div class="line">  CustomHandler.super.header_filter(self)</div><div class="line"></div><div class="line">  -- Implement any custom logic here</div><div class="line">end</div><div class="line"></div><div class="line">function CustomHandler:body_filter(config)</div><div class="line">  -- Eventually, execute the parent implementation</div><div class="line">  -- (will log that your plugin is entering this context)</div><div class="line">  CustomHandler.super.body_filter(self)</div><div class="line"></div><div class="line">  -- Implement any custom logic here</div><div class="line">end</div><div class="line"></div><div class="line">function CustomHandler:log(config)</div><div class="line">  -- Eventually, execute the parent implementation</div><div class="line">  -- (will log that your plugin is entering this context)</div><div class="line">  CustomHandler.super.log(self)</div><div class="line"></div><div class="line">  -- Implement any custom logic here</div><div class="line">end</div><div class="line"></div><div class="line">-- 该模块需要返回创建的表</div><div class="line">-- 让Kong 可以执行这些功能。</div><div class="line">return CustomHandler</div></pre></td></tr></table></figure></p><p>当然，插件本身的逻辑可以抽象到另一个模块中，并从处理程序模块调用。许多现有的插件在逻辑冗长时已经选择了这种模式，但它是完全可选的:<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div></pre></td><td class="code"><pre><div class="line">local BasePlugin = require &quot;kong.plugins.base_plugin&quot;</div><div class="line"></div><div class="line">-- 实际的逻辑是在这些模块中实现的</div><div class="line">local access = require &quot;kong.plugins.my-custom-plugin.access&quot;</div><div class="line">local body_filter = require &quot;kong.plugins.my-custom-plugin.body_filter&quot;</div><div class="line"></div><div class="line">local CustomHandler = BasePlugin:extend()</div><div class="line"></div><div class="line">function CustomHandler:new()</div><div class="line">  CustomHandler.super.new(self, &quot;my-custom-plugin&quot;)</div><div class="line">end</div><div class="line"></div><div class="line">function CustomHandler:access(config)</div><div class="line">  CustomHandler.super.access(self)</div><div class="line"></div><div class="line">  -- 从“access”中加载的模块执行任何函数，</div><div class="line">  -- 例如，`execute()`并将插件的配置传递给它。</div><div class="line">  access.execute(config)</div><div class="line">end</div><div class="line"></div><div class="line">function CustomHandler:body_filter(config)</div><div class="line">  CustomHandler.super.body_filter(self)</div><div class="line"></div><div class="line">  -- Execute any function from the module loaded in `body_filter`,</div><div class="line">  -- for example, `execute()` and passing it the plugin&apos;s configuration.</div><div class="line">  body_filter.execute(config)</div><div class="line">end</div><div class="line"></div><div class="line">return CustomHandler</div></pre></td></tr></table></figure></p><p>有关实际处理程序代码的示例，请参阅<a href="https://github.com/Kong/kong/blob/master/kong/plugins/key-auth/handler.lua" target="_blank" rel="external">Key-Auth插件的源代码</a>。</p><h2 id="插件开发套件"><a href="#插件开发套件" class="headerlink" title="插件开发套件"></a>插件开发套件</h2><p>在这些阶段中实现的逻辑很可能必须与请求/响应对象或核心组件交互(例如访问缓存、数据库……)。Kong提供了一个<a href="https://docs.konghq.com/1.1.x/pdk" target="_blank" rel="external">插件开发套件</a>（简称PSK），一组Lua函数和变量，插件可以使用这些Lua函数和变量来执行各种网关操作，确保它们与将来的Kong版本向前兼容。</p><p>当您试图实现一些需要与Kong交互的逻辑时(例如检索请求头、从插件生成响应、记录一些错误或调试信息……)，您应该参考<a href="https://docs.konghq.com/1.1.x/pdk" target="_blank" rel="external">插件开发工具包</a>。</p><h2 id="插件执行顺序"><a href="#插件执行顺序" class="headerlink" title="插件执行顺序"></a>插件执行顺序</h2><p>一些插件可能依赖于其他插件的执行来执行一些操作。例如，依赖于消费者身份的插件必须在身份验证插件之后运行。考虑到这一点，Kong定义了插件执行之间的优先级，以确保顺序得到遵守。</p><p>你的插件的优先级可以通过一个属性来配置，在返回的handler table 中一个数字:<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">CustomHandler.PRIORITY = 10</div></pre></td></tr></table></figure></p><p>优先级越高，相对于其他插件的阶段(如:access()、:log()等)，插件的阶段执行得越快。<br>已有捆绑插件的当前执行顺序为:</p><table><thead><tr><th>插件</th><th>优先级</th></tr></thead><tbody><tr><td>pre-function</td><td><code>+inf</code></td></tr><tr><td>zipkin</td><td>100000</td></tr><tr><td>ip-restriction</td><td>3000</td></tr><tr><td>bot-detection</td><td>2500</td></tr><tr><td>cors</td><td>25000</td></tr><tr><td>jwt</td><td>1005</td></tr><tr><td>oauth2</td><td>1004</td></tr><tr><td>key-auth</td><td>1003</td></tr><tr><td>ldap-auth</td><td>1002</td></tr><tr><td>basic-auth</td><td>1001</td></tr><tr><td>hmac-auth</td><td>1000</td></tr><tr><td>request-size-limiting</td><td>951</td></tr><tr><td>acl</td><td>950</td></tr><tr><td>rate-limiting</td><td>901</td></tr><tr><td>response-ratelimiting</td><td>900</td></tr><tr><td>request-transformer</td><td>801</td></tr><tr><td>response-transformer</td><td>800</td></tr><tr><td>aws-lambda</td><td>750</td></tr><tr><td>azure-functions</td><td>749</td></tr><tr><td>prometheus</td><td>13</td></tr><tr><td>http-log</td><td>12</td></tr><tr><td>statsd</td><td>11</td></tr><tr><td>datadog</td><td>10</td></tr><tr><td>file-log</td><td>9</td></tr><tr><td>udp-log</td><td>8</td></tr><tr><td>tcp-log</td><td>7</td></tr><tr><td>loggly</td><td>6</td></tr><tr><td>syslog</td><td>4</td></tr><tr><td>galileo</td><td>3</td></tr><tr><td>request-termination</td><td>2</td></tr><tr><td>correlation-id</td><td>1</td></tr><tr><td>post-function</td><td>-1000</td></tr></tbody></table>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;原文地址：&lt;a href=&quot;https://docs.konghq.com/1.1.x/plugin-development/custom-logic/&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;https://docs.konghq.com/1.1.
      
    
    </summary>
    
      <category term="与代码" scheme="http://git.102no.com/categories/%E4%B8%8E%E4%BB%A3%E7%A0%81/"/>
    
    
      <category term="Kong" scheme="http://git.102no.com/tags/Kong/"/>
    
      <category term="Kong教程" scheme="http://git.102no.com/tags/Kong%E6%95%99%E7%A8%8B/"/>
    
      <category term="Kong中文文档" scheme="http://git.102no.com/tags/Kong%E4%B8%AD%E6%96%87%E6%96%87%E6%A1%A3/"/>
    
      <category term="Kong插件开发" scheme="http://git.102no.com/tags/Kong%E6%8F%92%E4%BB%B6%E5%BC%80%E5%8F%91/"/>
    
  </entry>
  
  <entry>
    <title>在Kong的插件中使用自建数据表的问题</title>
    <link href="http://git.102no.com/2019/04/11/kong-table-issuse/"/>
    <id>http://git.102no.com/2019/04/11/kong-table-issuse/</id>
    <published>2019-04-10T16:00:00.000Z</published>
    <updated>2019-04-11T15:37:54.580Z</updated>
    
    <content type="html"><![CDATA[<p>目前正在开发的kong 插件中需要用到自己新建表，然后对其进行读写，但是由于kong的版本升级原因，有一些问题在官方文档中没有提到，如果按照官方文档开发，很容易掉坑里,导致一直读取失败，这里把其中一些可能会遇到的问题列一下，有一些代码是参考了升级之后，官方自带的插件中的代码，主要注意的点如下：</p><h2 id="1-迁移文件的差异"><a href="#1-迁移文件的差异" class="headerlink" title="1.迁移文件的差异"></a>1.迁移文件的差异</h2><h3 id="kong的版本-gt-1-0"><a href="#kong的版本-gt-1-0" class="headerlink" title="kong的版本&gt;= 1.0"></a>kong的版本&gt;= 1.0</h3><p>迁移文件的名字为<code>000_base_xxx.lua</code>，其中格式格式如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div></pre></td><td class="code"><pre><div class="line"> return &#123;</div><div class="line">    postgres = &#123;</div><div class="line">        up = [[</div><div class="line">          CREATE TABLE IF NOT EXISTS &quot;xxxxx&quot; (</div><div class="line">            &quot;id&quot;uuidNOT NULLDEFAULT uuid_generate_v4 ()PRIMARY KEY,</div><div class="line">            &quot;xx&quot;intNOT NULL,</div><div class="line">            &quot;xx&quot;bool,</div><div class="line">            &quot;xx&quot;json,</div><div class="line">            &quot;xx&quot;json</div><div class="line">          );</div><div class="line">        ]],</div><div class="line">    &#125;,</div><div class="line">    cassandra =&#123;</div><div class="line">        up =[[</div><div class="line"></div><div class="line">        ]]</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure><h3 id="kong的版本小于1-0版本"><a href="#kong的版本小于1-0版本" class="headerlink" title="kong的版本小于1.0版本"></a>kong的版本小于1.0版本</h3><p>迁移文件的名字为<code>postgres.lua</code>和<code>cassandra.lua</code>，其中格式如下：<br>此为postgres.lua</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div></pre></td><td class="code"><pre><div class="line"> return &#123;</div><div class="line">  &#123;</div><div class="line">    name = &quot;2015-07-31-172400_init_keyauth&quot;,</div><div class="line">    up = [[</div><div class="line">      CREATE TABLE IF NOT EXISTS keyauth_credentials(</div><div class="line">        id uuid,</div><div class="line">        consumer_id uuid REFERENCES consumers (id) ON DELETE CASCADE,</div><div class="line">        key text UNIQUE,</div><div class="line">        created_at timestamp without time zone default (CURRENT_TIMESTAMP(0) at time zone &apos;utc&apos;),</div><div class="line">        PRIMARY KEY (id)</div><div class="line">      );</div><div class="line"></div><div class="line">      DO $$</div><div class="line">      BEGIN</div><div class="line">        IF (SELECT to_regclass(&apos;public.keyauth_key_idx&apos;)) IS NULL THEN</div><div class="line">          CREATE INDEX keyauth_key_idx ON keyauth_credentials(key);</div><div class="line">        END IF;</div><div class="line">        IF (SELECT to_regclass(&apos;public.keyauth_consumer_idx&apos;)) IS NULL THEN</div><div class="line">          CREATE INDEX keyauth_consumer_idx ON keyauth_credentials(consumer_id);</div><div class="line">        END IF;</div><div class="line">      END$$;</div><div class="line">    ]],</div><div class="line">    down = [[</div><div class="line">      DROP TABLE keyauth_credentials;</div><div class="line">    ]]</div><div class="line">  &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure><h2 id="2-daos-lua的差异"><a href="#2-daos-lua的差异" class="headerlink" title="2.daos.lua的差异"></a>2.daos.lua的差异</h2><p>daos.lua的格式也是有差异的，但是这部分的差异在文档中是没有体现的</p><h3 id="kong的版本-gt-1-0-1"><a href="#kong的版本-gt-1-0-1" class="headerlink" title="kong的版本&gt;= 1.0"></a>kong的版本&gt;= 1.0</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div></pre></td><td class="code"><pre><div class="line">local typedefs = require &quot;kong.db.schema.typedefs&quot;</div><div class="line"></div><div class="line">local xxx = &#123;</div><div class="line">    primary_key = &#123; &quot;id&quot; &#125;,</div><div class="line">    name = &quot;app_config&quot;,</div><div class="line">    fields = &#123;</div><div class="line">        &#123; id = typedefs.uuid &#125;,</div><div class="line">        &#123; xx = &#123; type = &quot;string&quot;, required = false, unique = true &#125;, &#125;,</div><div class="line">        &#123; xx = &#123; type = &quot;string&quot;, required = false, unique = true &#125; &#125;,</div><div class="line">        &#123; xx = &#123; type = &quot;string&quot;, required = false &#125; &#125;,</div><div class="line">        &#123; xx = &#123; type = &quot;string&quot;, required = false &#125; &#125;,</div><div class="line">        &#123; xx = &#123; type = &quot;string&quot;, required = false &#125; &#125;</div><div class="line">    &#125;</div><div class="line">&#125;</div><div class="line">return &#123;</div><div class="line">    zz = xxx,</div><div class="line">&#125;</div></pre></td></tr></table></figure><h3 id="kong的版本小于1-0版本-1"><a href="#kong的版本小于1-0版本-1" class="headerlink" title="kong的版本小于1.0版本"></a>kong的版本小于1.0版本</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line">-- daos.lua</div><div class="line">local SCHEMA = &#123;</div><div class="line">  primary_key = &#123;&quot;id&quot;&#125;,</div><div class="line">  table = &quot;keyauth_credentials&quot;, -- the actual table in the database</div><div class="line">  fields = &#123;</div><div class="line">    id = &#123;type = &quot;id&quot;, dao_insert_value = true&#125;, -- a value to be inserted by the DAO itself (think of serial ID and the uniqueness of such required here)</div><div class="line">    created_at = &#123;type = &quot;timestamp&quot;, immutable = true, dao_insert_value = true&#125;, -- also interted by the DAO itself</div><div class="line">    consumer_id = &#123;type = &quot;id&quot;, required = true, foreign = &quot;consumers:id&quot;&#125;, -- a foreign key to a Consumer&apos;s id</div><div class="line">    key = &#123;type = &quot;string&quot;, required = false, unique = true&#125; -- a unique API key</div><div class="line">  &#125;</div><div class="line">&#125;</div><div class="line">return &#123;keyauth_credentials = SCHEMA&#125; -- this plugin only results in one custom DAO, named &apos;keyauth_credentials&apos;</div></pre></td></tr></table></figure><p>尤其注意<code>fields</code>字段的结构差异，不然很容易弄混</p><h2 id="3-调用差异"><a href="#3-调用差异" class="headerlink" title="3.调用差异"></a>3.调用差异</h2><h3 id="kong的版本-gt-1-0-2"><a href="#kong的版本-gt-1-0-2" class="headerlink" title="kong的版本&gt;= 1.0"></a>kong的版本&gt;= 1.0</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">-- 通过某个字段查询</div><div class="line">local app_configs, err = kong.db.app_config:select_by_app_key(app_key)</div><div class="line">-- 通过主键查询</div><div class="line">local app_group_configs, err = kong.db.app_group_config:select(&#123; id = group_id &#125;)</div></pre></td></tr></table></figure><h3 id="kong的版本小于1-0版本-2"><a href="#kong的版本小于1-0版本-2" class="headerlink" title="kong的版本小于1.0版本"></a>kong的版本小于1.0版本</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">local app_configs, err = kong.dao.app_config:find()</div><div class="line">local app_configs, err = kong.dao.app_config:find_all()</div></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;目前正在开发的kong 插件中需要用到自己新建表，然后对其进行读写，但是由于kong的版本升级原因，有一些问题在官方文档中没有提到，如果按照官方文档开发，很容易掉坑里,导致一直读取失败，这里把其中一些可能会遇到的问题列一下，有一些代码是参考了升级之后，官方自带的插件中的代码
      
    
    </summary>
    
      <category term="与代码" scheme="http://git.102no.com/categories/%E4%B8%8E%E4%BB%A3%E7%A0%81/"/>
    
    
      <category term="Kong" scheme="http://git.102no.com/tags/Kong/"/>
    
      <category term="Kong教程" scheme="http://git.102no.com/tags/Kong%E6%95%99%E7%A8%8B/"/>
    
      <category term="Kong中文文档" scheme="http://git.102no.com/tags/Kong%E4%B8%AD%E6%96%87%E6%96%87%E6%A1%A3/"/>
    
  </entry>
  
  <entry>
    <title>微服务 API 网关 Kong 插件开发 - 访问数据存储区</title>
    <link href="http://git.102no.com/2019/04/08/kong-plugin-access-datastore-doc/"/>
    <id>http://git.102no.com/2019/04/08/kong-plugin-access-datastore-doc/</id>
    <published>2019-04-07T16:00:00.000Z</published>
    <updated>2019-04-08T15:42:01.919Z</updated>
    
    <content type="html"><![CDATA[<p>原文地址：<a href="https://docs.konghq.com/1.1.x/plugin-development/access-the-datastore/" target="_blank" rel="external">https://docs.konghq.com/1.1.x/plugin-development/access-the-datastore/</a> （不能保证所有的翻译都是准确无误的，所有如有翻译的不准确或错误之处，请一定记得查看原文，并欢迎留言指出）。</p><h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>Kong通过我们称为“DAOs”的类与模型层交互。本章将详细介绍与数据存储交互的可用API。Kong支持两个主数据存储：<a href="http://cassandra.apache.org/" target="_blank" rel="external">Cassandra 3.x.x</a>和<a href="http://www.postgresql.org/" target="_blank" rel="external">PostgreSQL 9.5+</a>。</p><h1 id="kong-db"><a href="#kong-db" class="headerlink" title="kong.db"></a>kong.db</h1><p>Kong 的所有实例表示为：</p><ul><li>描述实体在数据存储区中与哪个表相关的模式，对其字段的约束，如外键，非空约束等…此schema是<a href="https://docs.konghq.com/1.1.x/plugin-development/plugin-configuration/" target="_blank" rel="external">插件配置</a>章节中描述的表。</li><li>DAO类的一个实例映射到当前正在使用的数据库（Cassandra或PostgreSQL）。此类的方法使用模式并公开方法来插入，更新，查找和删除该类型的实体。</li></ul><p>Kong的核心实体是：Services, Routes, Consumers 和 Plugins。所有这些都可以作为数据访问对象（DAO）通过kong.db全局单例访问：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">-- Core DAOs</div><div class="line">local services_dao = kong.db.services</div><div class="line">local routes_dao = kong.db.routes</div><div class="line">local consumers_dao = kong.db.consumers</div><div class="line">local plugins_dao = kong.dao.plugins</div></pre></td></tr></table></figure><p>来自Kong的核心实体和插件中的自定义实体都可以通过<code>kong.db.*</code>。</p><h2 id="The-DAO-Lua-API"><a href="#The-DAO-Lua-API" class="headerlink" title="The DAO Lua API"></a>The DAO Lua API</h2><p>DAO类负责在数据存储区中的给定表上执行的操作，通常映射到Kong的实体。所有底层支持的数据库（目前是Cassandra和PostgreSQL）都遵循相同的接口，从而使DAO与所有这些兼容。<br>例如，插入服务和插件非常简单：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div></pre></td><td class="code"><pre><div class="line">local inserted_service, err = kong.db.services:insert(&#123;</div><div class="line">  name = &quot;mockbin&quot;,</div><div class="line">  url = &quot;http://mockbin.org&quot;,</div><div class="line">&#125;)</div><div class="line"></div><div class="line">local inserted_plugin, err = kong.db.plugins:insert(&#123;</div><div class="line">  name = &quot;key-auth&quot;,</div><div class="line">  service_id = &#123; id = inserted_service.id &#125;,</div><div class="line">&#125;)</div></pre></td></tr></table></figure></p><p>有关在插件中使用的DAO的实际示例，请参阅 <a href="https://github.com/Kong/kong/blob/master/kong/plugins/key-auth/handler.lua" target="_blank" rel="external">Key-Auth plugin 的源代码</a>。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;原文地址：&lt;a href=&quot;https://docs.konghq.com/1.1.x/plugin-development/access-the-datastore/&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;https://docs.konghq.
      
    
    </summary>
    
      <category term="与代码" scheme="http://git.102no.com/categories/%E4%B8%8E%E4%BB%A3%E7%A0%81/"/>
    
    
      <category term="Kong" scheme="http://git.102no.com/tags/Kong/"/>
    
      <category term="Kong教程" scheme="http://git.102no.com/tags/Kong%E6%95%99%E7%A8%8B/"/>
    
      <category term="Kong中文文档" scheme="http://git.102no.com/tags/Kong%E4%B8%AD%E6%96%87%E6%96%87%E6%A1%A3/"/>
    
      <category term="Kong插件开发" scheme="http://git.102no.com/tags/Kong%E6%8F%92%E4%BB%B6%E5%BC%80%E5%8F%91/"/>
    
  </entry>
  
  <entry>
    <title>微服务 API 网关 Kong 插件开发 - 存储自定义实体</title>
    <link href="http://git.102no.com/2019/04/04/kong-plugin-custom-entities-doc/"/>
    <id>http://git.102no.com/2019/04/04/kong-plugin-custom-entities-doc/</id>
    <published>2019-04-03T16:00:00.000Z</published>
    <updated>2019-04-08T15:43:18.757Z</updated>
    
    <content type="html"><![CDATA[<p>原文地址：<a href="https://docs.konghq.com/1.1.x/plugin-development/custom-entities/" target="_blank" rel="external">https://docs.konghq.com/1.1.x/plugin-development/custom-entities/</a> （不能保证所有的翻译都是准确无误的，所有如有翻译的不准确或错误之处，请一定记得查看原文，并欢迎留言指出）。</p><h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>虽然不是所有插件都需要它，但是您的插件可能需要在数据库中存储比配置更多的东西。在这种情况下，Kong在其主要数据存储之上提供了一个抽象，允许您存储自定义实体。</p><p>如<a href="https://docs.konghq.com/1.1.x/plugin-development/access-the-datastore/" target="_blank" rel="external">前一章节访问数据存储区</a>所述。Kong通过我们称之为“DAOs”的类与模型层交互，并且可以在通常被称为“DAO Factory”的单例上使用。本章将解释如何为您自己的实体提供一个抽象体。</p><h2 id="Modules"><a href="#Modules" class="headerlink" title="Modules"></a>Modules</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line">kong.plugins.&lt;plugin_name&gt;.daos</div><div class="line">kong.plugins.&lt;plugin_name&gt;.migrations.init</div><div class="line">kong.plugins.&lt;plugin_name&gt;.migrations.000_base</div><div class="line">kong.plugins.&lt;plugin_name&gt;.migrations.001_xxx</div><div class="line">kong.plugins.&lt;plugin_name&gt;.migrations.002_yyy</div></pre></td></tr></table></figure><h2 id="创建migrations文件夹"><a href="#创建migrations文件夹" class="headerlink" title="创建migrations文件夹"></a>创建migrations文件夹</h2><p>一旦定义了model，您必须创建将由Kong执行的migration modules，创建将存储您的实体记录的表。迁移文件包含一系列迁移，并返回它们。<br>如果您的插件旨在支持Cassandra和PostgreSQL，那么必须编写两个migrations。<br>如果您的插件还没有migrations文件夹，你应该添加一个<code>&lt;plugin_name&gt;/migrations</code>文件夹。如果里面没有<code>init.lua</code>文件，你应该创建一个。这是引导插件所有迁移的地方。<br><code>migrations/init.lua</code>文件的初始版本将指向单个迁移。<br>在这种情况下，我们称之为<code>000_base</code>。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">-- `migrations/init.lua`</div><div class="line">return &#123;</div><div class="line">  &quot;000_base&quot;,</div><div class="line">&#125;</div></pre></td></tr></table></figure></p><p>这意味着<code>&lt;plugin_name&gt;/migrations/000_base.lua</code>中将包含一个包含初始迁移的文件。<br>我们将在一分钟内看到这是如何完成的。</p><h2 id="迁移文件的语法"><a href="#迁移文件的语法" class="headerlink" title="迁移文件的语法"></a>迁移文件的语法</h2><p>虽然Kong的核心迁移支持PostgreSQL和Cassandra，但自定义插件可以选择支持它们或只支持一个。</p><p>迁移文件是一个Lua文件，它返回一个具有以下结构的表：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div></pre></td><td class="code"><pre><div class="line">-- `&lt;plugin_name&gt;/migrations/000_base.lua`</div><div class="line">return &#123;</div><div class="line">  postgresql = &#123;</div><div class="line">    up = [[</div><div class="line">      CREATE INDEX IF NOT EXISTS &quot;routes_name_idx&quot; ON &quot;routes&quot; (&quot;name&quot;);</div><div class="line">    ]],</div><div class="line">    teardown = function(connector, helpers)</div><div class="line">      assert(connector:connect_migrations())</div><div class="line">      assert(connector:query(&apos;DROP TABLE IF EXISTS &quot;schema_migrations&quot; CASCADE;&apos;))</div><div class="line">    end,</div><div class="line">  &#125;,</div><div class="line"></div><div class="line">  cassandra = &#123;</div><div class="line">    up = [[</div><div class="line">      CREATE INDEX IF NOT EXISTS routes_name_idx ON routes(name);</div><div class="line">    ]],</div><div class="line">    teardown = function(connector, helpers)</div><div class="line">      assert(connector:connect_migrations())</div><div class="line">      assert(connector:query(&quot;DROP TABLE IF EXISTS schema_migrations&quot;))</div><div class="line">    end,</div><div class="line">  &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure><p>如果插件仅支持PostgreSQL或Cassandra，则只需要这个策略的一部分。每个策略部分都有两个部分，<code>up</code>和<code>teardown</code>。</p><ul><li><code>up</code>是一个可选的原始SQL / CQL语句字符串。当执行<code>ong migrations up</code>时，将执行这些语句。</li><li><code>teardown</code>是一个可选的Lua函数，它接受一个<code>connector</code>参数。此类连接器可以调用查询方法来执行SQL/CQL查询。删除是由<code>kong migrations finish</code>触发。</li></ul><p>建议在<code>up</code>部分上完成所有非破坏性操作，例如创建新表和添加新记录。而在进行破坏性操作（例如删除数据，更改行类型，插入新数据）时候在<code>teardown</code>部分。</p><p>在这两种情况下，建议编写所有SQL/CQL语句，以使它们尽可能重入。<code>DROP TABLE IF EXISTS</code>代替<code>DROP TABLE</code>，<code>CREATE INDEX IF NOT EXIST</code>代替<code>CREATE INDEX</code>等等。如果迁移由于某种原因而失败，则预计解决问题的第一次尝试将只是重新运行迁移。</p><p>虽然PostgreSQL没有，但Cassandra不支持诸如“NOT NULL”，“UNIQUE”或“FOREIGN KEY”之类的约束，但是，当您定义模型的模式时，Kong会为您提供此类功能。请记住，这个模式对于PostgreSQL和Cassandra都是相同的，因此，您可能会为使用Cassandra的模式权衡纯SQL模式。</p><blockquote><p>重要：如果您的<code>schema</code>使用<code>unique</code>约束，那么Kong将为Cassandra强制执行它，但对于PostgreSQL，您必须在迁移中设置此约束。</p></blockquote><h2 id="从DAO-Factory检索您的自定义DAO"><a href="#从DAO-Factory检索您的自定义DAO" class="headerlink" title="从DAO Factory检索您的自定义DAO"></a>从DAO Factory检索您的自定义DAO</h2><p>要使DAO Factory加载自定义DAO，您需要定义实体的架构（就像描述<a href="https://docs.konghq.com/1.1.x/plugin-development/plugin-configuration/" target="_blank" rel="external">插件配置</a>的模式一样）。此模式包含更多值，因为它必须描述实体在数据存储区中与哪个表相关，对其字段的约束（如外键，非空约束等）。</p><p>此模式将在名为的模块中定义：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">&quot;kong.plugins.&lt;plugin_name&gt;.daos&quot;</div></pre></td></tr></table></figure></p><p>一旦该模块返回您的实体模式，并假设您的插件由Kong加载（请参阅<code>kong.conf</code>中的<code>plugins</code>属性），DAO Factory将使用它来实例化DAO对象。</p><p>以下是如何定义用于在其数据库中存储API密钥的模式的示例：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line">-- daos.lua</div><div class="line">local SCHEMA = &#123;</div><div class="line">  primary_key = &#123;&quot;id&quot;&#125;,</div><div class="line">  table = &quot;keyauth_credentials&quot;, -- 数据库中的表</div><div class="line">  fields = &#123;</div><div class="line">    id = &#123;type = &quot;id&quot;, dao_insert_value = true&#125;, -- DAO本身要插入的值（想想序列号和这里所需的唯一性）</div><div class="line">    created_at = &#123;type = &quot;timestamp&quot;, immutable = true, dao_insert_value = true&#125;, -- DAO本身也有所涉及</div><div class="line">    consumer_id = &#123;type = &quot;id&quot;, required = true, foreign = &quot;consumers:id&quot;&#125;, -- Consumer&apos;s id 的外键</div><div class="line">    key = &#123;type = &quot;string&quot;, required = false, unique = true&#125; --  唯一 API key</div><div class="line">  &#125;</div><div class="line">&#125;</div><div class="line"></div><div class="line">return &#123;keyauth_credentials = SCHEMA&#125; -- 这个插件只产生一个自定义DAO， 名为 `keyauth_credentials`</div></pre></td></tr></table></figure><p>由于您的插件可能必须处理多个自定义DAO（在您要存储多个实体的情况下），此模块必须返回一个键/值表，其中键是DAO Factory中可用的自定义DAO的名称。</p><p>您将注意到架构定义中的一些新属性（与<a href="https://docs.konghq.com/1.1.x/plugin-development/plugin-configuration/" target="_blank" rel="external">schema.lua</a>文件进行比较）</p><table><thead><tr><th>属性名</th><th>LUA TYPE</th><th>描述</th></tr></thead><tbody><tr><td><code>primary_key</code></td><td>Integer indexed table</td><td>每个部分column family的主键的数组,它还支持复合键，即使所有Kong实体当前都使用简单<code>ID</code>来管理<a href="https://docs.konghq.com/1.1.x/admin-api/" target="_blank" rel="external">Admin API</a>的可用性。如果主键是组合键，则只包含分区键的组成部分。</td></tr><tr><td><code>fields.*.dao_insert_value</code></td><td>Boolean</td><td>如果为true，则指定此字段由DAO（在base_dao实现中）自动填充，具体取决于其类型。id类型的属性将是生成的uuid，时间戳是具有第二精度的时间戳。</td></tr><tr><td><code>fields.*.queryable</code></td><td>Boolean</td><td>如果为true，则指定Cassandra在指定column上维护索引。允许查询由该column过滤的column family 。</td></tr><tr><td><code>fields.*.foreign</code></td><td>String</td><td>指定此column是另一个实体列的外键。格式为：<code>dao_name:column_name</code>，这使得Cassandra不支持外键。当父行将被删除时，Kong还将删除包含父列值的行。</td></tr></tbody></table><p>您的DAO现在将由DAO Factory加载并作为其属性之一提供。因为DAO工厂是由插件开发工具包的kong global(参见kong)公开的。dao，可以这样检索:<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">local key_credential, err = kong.dao.key_credentials:insert(&#123;</div><div class="line">  consumer_id = consumer.id,</div><div class="line">  key = &quot;abcd&quot;</div><div class="line">&#125;)</div></pre></td></tr></table></figure></p><p>可以从DAO Factory访问的DAO名称（keyauth_credentials），取决于您在daos.lua的返回表中导出DAO的键。</p><p>您可以在<a href="https://github.com/Kong/kong/blob/master/kong/plugins/key-auth/daos.lua" target="_blank" rel="external">Key-Auth daos.lua</a>文件中看到此示例。</p><h2 id="缓存自定义实体"><a href="#缓存自定义实体" class="headerlink" title="缓存自定义实体"></a>缓存自定义实体</h2><p>有时每个request/response都需要自定义实体，每次都会在数据存储区上触发查询。这样效率非常低，因为查询数据存储会增加延迟并降低request/response速度。并且由此导致的数据存储区负载增加可能会影响数据存储区性能本身，进而影响其他Kong节点。</p><p>当每个请求/响应都需要自定义实体时，最好通过利用Kong提供的内存缓存API将其缓存在内存中。</p><p>下一章将重点介绍如何缓存自定义实体，并在数据存储区中更改时使它们失效：缓存自定义实体。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;原文地址：&lt;a href=&quot;https://docs.konghq.com/1.1.x/plugin-development/custom-entities/&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;https://docs.konghq.com/1
      
    
    </summary>
    
      <category term="与代码" scheme="http://git.102no.com/categories/%E4%B8%8E%E4%BB%A3%E7%A0%81/"/>
    
    
      <category term="Kong" scheme="http://git.102no.com/tags/Kong/"/>
    
      <category term="Kong中文文档" scheme="http://git.102no.com/tags/Kong%E4%B8%AD%E6%96%87%E6%96%87%E6%A1%A3/"/>
    
      <category term="Kong插件" scheme="http://git.102no.com/tags/Kong%E6%8F%92%E4%BB%B6/"/>
    
      <category term="Kong插件开发" scheme="http://git.102no.com/tags/Kong%E6%8F%92%E4%BB%B6%E5%BC%80%E5%8F%91/"/>
    
  </entry>
  
  <entry>
    <title>openSSL 的加密方式与证书制作</title>
    <link href="http://git.102no.com/2019/04/01/what-is-openssl/"/>
    <id>http://git.102no.com/2019/04/01/what-is-openssl/</id>
    <published>2019-03-31T16:00:00.000Z</published>
    <updated>2019-04-03T15:55:53.939Z</updated>
    
    <content type="html"><![CDATA[<h2 id="openSSL是什么"><a href="#openSSL是什么" class="headerlink" title="openSSL是什么"></a>openSSL是什么</h2><p>OpenSSL项目是安全套接字层（secure sockets layer，SSL）和传输层安全（transport layer security，TLS）协议的一个实现，是大家共同努力开发出的代码可靠、功能齐全、商业级别的开源工具集。除了OpenSSL之外的其他TLS实现方式，如GnuTLS、Mozilla的网络安全服务（NSS）和Windows平台的TLS。</p><p>项目由遍布世界的志愿者所组成的社区进行管理，他们通过互联网进行沟通、计划和开发OpenSSL工具集以及相关的文档。现在几乎所有的服务器软件和很多客户端软件都在使用OpenSSL，其中基于命令行的工具是进行密钥、证书管理以及测试最常用到的软件了。</p><h2 id="公钥算法与私钥算法"><a href="#公钥算法与私钥算法" class="headerlink" title="公钥算法与私钥算法"></a>公钥算法与私钥算法</h2><h3 id="私钥算法"><a href="#私钥算法" class="headerlink" title="私钥算法"></a>私钥算法</h3><p>私钥加密算法，又称 对称加密算法，因为这种算法解密密钥和加密密钥是相同的。也正因为同一密钥既用于加密又用于解密，所以这个密钥是不能公开的。常见的有《DES加密算法》、《AES加密算法》。</p><h3 id="公钥算法"><a href="#公钥算法" class="headerlink" title="公钥算法"></a>公钥算法</h3><p>公钥加密算法，也就是 非对称加密算法，这种算法加密和解密的密码不一样，一个是公钥，另一个是私钥：</p><ul><li>公钥和私钥成对出现</li><li>公开的密钥叫公钥，只有自己知道的叫私钥</li><li>用公钥加密的数据只有对应的私钥可以解密</li><li>用私钥加密的数据只有对应的公钥可以解密</li><li>如果可以用公钥解密，则必然是对应的私钥加的密</li><li>如果可以用私钥解密，则必然是对应的公钥加的密</li><li>公钥和私钥是相对的，两者本身并没有规定哪一个必须是公钥或私钥。</li></ul><ol><li><p>当该用户发送文件时，用私钥签名，别人用他给的公钥解密，可以保证该信息是由他发送的。即数字签名。</p></li><li><p>当该用户接受文件时，别人用他的公钥加密，他用私钥解密，可以保证该信息只能由他看到。即安全传输。  </p></li></ol><ul><li>加密: 公钥用于对数据进行加密，私钥用于对数据进行解密  </li><li>签名: 私钥用于对数据进行签名，公钥用于对签名进行验证</li></ul><p>由于在非对称算法中，公钥加密的数据必须用对应的私钥才能解密，而私钥又只有接收方自己知道，这样就保证了数据传输的安全性。</p><p><img src="https://img-blog.csdn.net/20150502122610368" alt=""></p><h3 id="openssl生成公私钥对文件进行加解密"><a href="#openssl生成公私钥对文件进行加解密" class="headerlink" title="openssl生成公私钥对文件进行加解密"></a>openssl生成公私钥对文件进行加解密</h3><p><strong>生成一个私钥</strong>   </p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">openssl genrsa -out rsa-private.key 1024</div><div class="line">- genrsa：生成RSA参数</div><div class="line">- -out：输出文件</div></pre></td></tr></table></figure><p><strong>由私钥生成一个公钥</strong>     </p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">openssl rsa -in rsa-private.key -pubout -out rsa-public.key</div></pre></td></tr></table></figure><ul><li>-pubout: 输出公钥文件</li></ul><p><strong>随便新建一个文件</strong>     </p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">echo &quot;this is a test message balbalabalabalbalabala&quot; &gt; test.log</div></pre></td></tr></table></figure><p><strong>使用公钥加密这个文件</strong>     </p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">openssl rsautl -encrypt -inkey rsa-public.key -pubin -in test.log -out encrypt-test.log</div><div class="line">- rsautl：用于完成RSA签名、验证、加密和解密功能</div><div class="line">- -pubin：检查待处理文件是否为公钥文件</div></pre></td></tr></table></figure><p><strong>使用私钥解密这个文件</strong>     </p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">openssl rsautl -decrypt -inkey rsa-private.key -in encode-test.log -out decrypt-test.log</div></pre></td></tr></table></figure><h2 id="使用openssl获取证书"><a href="#使用openssl获取证书" class="headerlink" title="使用openssl获取证书"></a>使用openssl获取证书</h2><p>显示所有证书<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">openssl s_client -connect www.baidu.com:443 -showcerts</div></pre></td></tr></table></figure></p><ul><li><code>Certificate chain</code> 证书链</li><li><code>s:</code>证书本身主体</li><li><code>i:</code>证书颁发机构</li></ul><p>服务器查看并解析证书 </p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">openssl x509 -in baidu.pem -text -noout</div></pre></td></tr></table></figure><ul><li><a href="https://zh.wikipedia.org/wiki/X.509" target="_blank" rel="external">X.509</a> 是密码学里公钥证书的格式标准。</li></ul><h2 id="使用openssl制作证书"><a href="#使用openssl制作证书" class="headerlink" title="使用openssl制作证书"></a>使用openssl制作证书</h2><h3 id="第一步：创建非对称密钥对"><a href="#第一步：创建非对称密钥对" class="headerlink" title="第一步：创建非对称密钥对"></a>第一步：创建非对称密钥对</h3><p>可以选择 RSA DSA ECDSA 的都可以，其中RSA 在浏览器的兼容是最好的。<a href="https://blog.csdn.net/sszgg2006/article/details/25478269" target="_blank" rel="external">三者区别</a></p><ul><li><p><strong>rsa key</strong>  </p><p>  和 openssl生成公私钥对文件进行加解密 中一样</p></li><li><p><strong>dsa key</strong>  </p>  <figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">openssl dsaparam -genkey 2048 | openssl dsa -out dsa.key</div><div class="line">- dsaparam：本指令用来生成和操作dsa参数。</div><div class="line">-  -genkey：生成dsa密钥。</div></pre></td></tr></table></figure></li><li><p><strong>ec key</strong>  </p>  <figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">openssl ecparam -genkey -name secp256r1 | openssl ec -out ec.key</div></pre></td></tr></table></figure></li></ul><h3 id="第二步：创建证书签名请求-CSR-文件"><a href="#第二步：创建证书签名请求-CSR-文件" class="headerlink" title="第二步：创建证书签名请求 CSR 文件"></a>第二步：创建证书签名请求 CSR 文件</h3><blockquote><p>注：Certificate Signing Request,即证书签名请求，这个并不是证书，而是向权威证书颁发机构获得签名证书的申请，其核心内容是一个公钥(当然还附带了一些别的信息)，在生成这个申请的时候，同时也会生成一个私钥,私钥要自己保管好。做过iOS APP的朋友都应该知道是怎么向苹果申请开发者证书的吧。</p></blockquote><p>新建csr<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">openssl req -new -key rsa-private.key -out rsa.csr</div></pre></td></tr></table></figure></p><p>查看csr<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">openssl req -text -in rsa.csr -noout</div></pre></td></tr></table></figure></p><h3 id="第三步：-自签名证书-（提交）"><a href="#第三步：-自签名证书-（提交）" class="headerlink" title="第三步： 自签名证书 （提交）"></a>第三步： 自签名证书 （提交）</h3><p> 生成 csr 文件之后，需要提交到权威的 CA 数字认证中心 VeriSign 后者其他，这里是讲自己认证。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">openssl x509 -req -days 365 -in rsa.csr -signkey rsa-private.key -out rsa_yugang.cer</div></pre></td></tr></table></figure><p>查看证书</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">openssl x509 -noout -text -in rsa_yugang.cer</div></pre></td></tr></table></figure><h2 id="证书格式"><a href="#证书格式" class="headerlink" title="证书格式"></a>证书格式</h2><ul><li>.pem – Privacy Enhanced Mail,打开看文本格式,以”—–BEGIN…”开头, “—–END…”结尾,内容是BASE64编码.</li><li>.cer, .crt, .der – 通常是DER二进制格式的，但Base64编码后也很常见。</li></ul><h2 id="相关资料"><a href="#相关资料" class="headerlink" title="相关资料"></a>相关资料</h2><ul><li><a href="http://www.178linux.com/48764" target="_blank" rel="external">OpenSSL 的使用详解</a></li><li><a href="https://juejin.im/entry/59f6c30ef265da43085d4e82" target="_blank" rel="external">OpenSSL开发学习总结</a></li><li><a href="https://www.openssl.org" target="_blank" rel="external">openssl 官网</a></li><li><a href="http://seanlook.com/2015/01/15/openssl-certificate-encryption/" target="_blank" rel="external">OpenSSL 与 SSL 数字证书概念贴</a></li><li><a href="http://www.cnblogs.com/LittleHann/p/3741907.html" target="_blank" rel="external">OPENSSL编程入门学习</a></li><li><a href="https://jin-yang.github.io/reference/linux/OpenSSL.pdf" target="_blank" rel="external">OpenSSL使用指南</a></li><li><a href="http://www.ituring.com.cn/book/download/338e1e55-fd94-4ac3-9e21-e0bf04984b3f" target="_blank" rel="external">OpenSSL攻略</a></li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;openSSL是什么&quot;&gt;&lt;a href=&quot;#openSSL是什么&quot; class=&quot;headerlink&quot; title=&quot;openSSL是什么&quot;&gt;&lt;/a&gt;openSSL是什么&lt;/h2&gt;&lt;p&gt;OpenSSL项目是安全套接字层（secure sockets layer，
      
    
    </summary>
    
      <category term="与代码" scheme="http://git.102no.com/categories/%E4%B8%8E%E4%BB%A3%E7%A0%81/"/>
    
    
      <category term="openSSL" scheme="http://git.102no.com/tags/openSSL/"/>
    
  </entry>
  
  <entry>
    <title>微服务 API 网关 Kong 单元测试中文文档</title>
    <link href="http://git.102no.com/2019/03/28/kong-test-doc/"/>
    <id>http://git.102no.com/2019/03/28/kong-test-doc/</id>
    <published>2019-03-27T16:00:00.000Z</published>
    <updated>2019-04-03T15:58:29.140Z</updated>
    
    <content type="html"><![CDATA[<p>原文链接: <a href="https://docs.konghq.com/1.0.x/plugin-development/tests/" target="_blank" rel="external">https://docs.konghq.com/1.0.x/plugin-development/tests/</a><br>（如有翻译的不准确或错误之处，欢迎留言指出）<br>集成测试：<a href="https://docs.konghq.com/1.0.x/plugin-development/tests/#write-integration-tests" target="_blank" rel="external">https://docs.konghq.com/1.0.x/plugin-development/tests/#write-integration-tests</a></p><h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><p>如果你认真对待你写的插件，你可能想为它编写一些测试。Lua的单元测试很简单，并且可以使用许多<a href="http://lua-users.org/wiki/UnitTesting" target="_blank" rel="external">测试框架</a>。但是，您可能还想编写集成测试。Kong可以再次为您提供支援。</p><h2 id="编写集成测试"><a href="#编写集成测试" class="headerlink" title="编写集成测试"></a>编写集成测试</h2><p>Kong的首选测试框架是<a href="http://olivinelabs.com/busted/" target="_blank" rel="external">busted</a>，它与resty-cli解释器一起运行，但如果您愿意，可以自由使用另一个。在Kong存储库中，可以在<code>bin/busted</code>找到 busted 的可执行文件。</p><p>Kong为您提供了一个帮助程序，可以在测试套件中从Lua启动和停止它：<code>spec.helpers</code>。此助手还提供了在运行测试之前在数据存储区中插入fixtures的方法，以及删除，以及各种其他helper。</p><p>如果您在自己的存储库中编写插件，则需要复制以下文件，直到Kong测试框架发布：</p><ul><li><code>bin/busted</code>： 与resty-cli解释器一起运行的 busted 的可执行文件</li><li><code>spec/helpers.lua</code>：Kong的helper函数 可以从busted中 启动/关闭kong</li><li><code>spec/kong_tests.conf</code>：使用helpers模块运行的Kong实例的配置文件</li></ul><p>假设您的<code>LUA_PATH</code>中有<code>spec.helpers</code>模块，您可以使用以下Lua代码来启动和停止Kong：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div><div class="line">37</div><div class="line">38</div><div class="line">39</div><div class="line">40</div><div class="line">41</div><div class="line">42</div><div class="line">43</div><div class="line">44</div><div class="line">45</div><div class="line">46</div><div class="line">47</div><div class="line">48</div><div class="line">49</div><div class="line">50</div><div class="line">51</div><div class="line">52</div><div class="line">53</div><div class="line">54</div><div class="line">55</div><div class="line">56</div><div class="line">57</div><div class="line">58</div></pre></td><td class="code"><pre><div class="line">local helpers = require &quot;spec.helpers&quot;</div><div class="line"></div><div class="line">for _, strategy in helpers.each_strategy() do</div><div class="line">  describe(&quot;my plugin&quot;, function()</div><div class="line"></div><div class="line">    local bp = helpers.get_db_utils(strategy)</div><div class="line"></div><div class="line">    setup(function()</div><div class="line">      local service = bp.services:insert &#123;</div><div class="line">        name = &quot;test-service&quot;,</div><div class="line">        host = &quot;httpbin.org&quot;</div><div class="line">      &#125;</div><div class="line"></div><div class="line">      bp.routes:insert(&#123;</div><div class="line">        hosts = &#123; &quot;test.com&quot; &#125;,</div><div class="line">        service = &#123; id = service.id &#125;</div><div class="line">      &#125;)</div><div class="line"></div><div class="line">      -- start Kong with your testing Kong configuration (defined in &quot;spec.helpers&quot;)</div><div class="line">      assert(helpers.start_kong( &#123; plugins = &quot;bundled,my-plugin&quot; &#125;))</div><div class="line"></div><div class="line">      admin_client = helpers.admin_client()</div><div class="line">    end)</div><div class="line"></div><div class="line">    teardown(function()</div><div class="line">      if admin_client then</div><div class="line">        admin_client:close()</div><div class="line">      endhttps://github.com/Kong/kong/tree/master/spec/03-plugins/09-key-auth</div><div class="line"></div><div class="line">      helpers.stop_kong()</div><div class="line">    end)</div><div class="line"></div><div class="line">    before_each(function()</div><div class="line">      proxy_client = helpers.proxy_client()</div><div class="line">    end)</div><div class="line"></div><div class="line">    after_each(function()</div><div class="line">      if proxy_client then</div><div class="line">        proxy_client:close()</div><div class="line">      end</div><div class="line">    end)</div><div class="line"></div><div class="line">    describe(&quot;thing&quot;, function()</div><div class="line">      it(&quot;should do thing&quot;, function()</div><div class="line">        -- send requests through Kong</div><div class="line">        local res = proxy_client:get(&quot;/get&quot;, &#123;</div><div class="line">          headers = &#123;</div><div class="line">            [&quot;Host&quot;] = &quot;test.com&quot;</div><div class="line">          &#125;</div><div class="line">        &#125;)</div><div class="line"></div><div class="line">        local body = assert.res_status(200, res)</div><div class="line"></div><div class="line">        -- body is a string containing the response</div><div class="line">      end)</div><div class="line">    end)</div><div class="line">  end)</div><div class="line">end</div></pre></td></tr></table></figure><blockquote><p>提醒：通过test Kong配置文件，Kong运行在代理监听端口9000(HTTP),9443 (HTTPS)和端口9001上的Admin API。</p></blockquote><p>如果想看一下真实的例子，可以来这里看一看  <a href="https://github.com/Kong/kong/tree/master/spec/03-plugins/09-key-auth" target="_blank" rel="external">Key-Auth plugin specs</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;原文链接: &lt;a href=&quot;https://docs.konghq.com/1.0.x/plugin-development/tests/&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;https://docs.konghq.com/1.0.x/plug
      
    
    </summary>
    
      <category term="与代码" scheme="http://git.102no.com/categories/%E4%B8%8E%E4%BB%A3%E7%A0%81/"/>
    
    
      <category term="api网关" scheme="http://git.102no.com/tags/api%E7%BD%91%E5%85%B3/"/>
    
      <category term="Kong" scheme="http://git.102no.com/tags/Kong/"/>
    
      <category term="Kong教程" scheme="http://git.102no.com/tags/Kong%E6%95%99%E7%A8%8B/"/>
    
      <category term="Kong中文文档" scheme="http://git.102no.com/tags/Kong%E4%B8%AD%E6%96%87%E6%96%87%E6%A1%A3/"/>
    
  </entry>
  
</feed>
